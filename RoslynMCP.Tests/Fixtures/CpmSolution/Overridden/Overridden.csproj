<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <!-- VersionOverride pins this project away from the central version, and is the reason the
         update path must not rewrite Directory.Packages.props for it. -->
    <ItemGroup>
        <PackageReference Include="Newtonsoft.Json" VersionOverride="13.0.1" />
    </ItemGroup>

</Project>
