{
  "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
  "name": "MSBuild",
  "scopeName": "text.xml.msbuild",
  "comment": "A project file is XML, and only the language id needs to be its own so the server is told the buffer opened. Highlighting is delegated to the grammar VS Code already ships; what MSBuild adds on top of XML — $(properties), @(items), %(metadata) — is injected by msbuild.expression.json rather than reimplemented here.",
  "patterns": [
    {
      "include": "text.xml"
    }
  ]
}
