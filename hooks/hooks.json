{
  "description": "Keep the RoslynSense dotnet tool installed and up to date.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "dotnet tool update --global RoslynSense || dotnet tool install --global RoslynSense || exit 0",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
