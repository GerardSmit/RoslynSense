# What Rider and Visual Studio give a C# developer

1093 capabilities across 25 domains, taken from JetBrains and Microsoft primary documentation and each product's 2024–2026 release notes. This is the measuring stick, not the verdict — where RoslynSense stands against it is [ide-gap-analysis.md](ide-gap-analysis.md).

`rider` / `vs` / `both` marks who ships it. Where one product's version is materially better, the description says so; paid-tier-only features are flagged in place.

## Contents

- [Code editing, completion and typing assistance](#code-editing-completion-and-typing-assistance) — 52
- [Navigation and search](#navigation-and-search) — 58
- [Refactorings and code generation](#refactorings-and-code-generation) — 57
- [Code inspection, quick-fixes and quality tooling](#code-inspection-quick-fixes-and-quality-tooling) — 50
- [Debugging](#debugging) — 61
- [Unit testing and coverage](#unit-testing-and-coverage) — 49
- [Profiling, performance and memory diagnostics](#profiling-performance-and-memory-diagnostics) — 47
- [Project, solution, build and package management](#project-solution-build-and-package-management) — 49
- [Web development: ASP.NET Core, Razor, Blazor, WebForms, JS/TS](#web-development-aspnet-core-razor-blazor-webforms-jsts) — 50
- [Data, Entity Framework and database tooling](#data-entity-framework-and-database-tooling) — 45
- [Designers and desktop/mobile UI: WPF, XAML, WinForms, MAUI, Avalonia, Unity](#designers-and-desktopmobile-ui-wpf-xaml-winforms-maui-avalonia-unity) — 49
- [Assemblies, decompilation, source generators and low-level tooling](#assemblies-decompilation-source-generators-and-low-level-tooling) — 44
- [Version control, collaboration and general IDE workflow](#version-control-collaboration-and-general-ide-workflow) — 50
- [Cross-platform support and remote/containerised development](#cross-platform-support-and-remotecontainerised-development) — 39
- [Tool self-diagnosis, health, logging and solution-load reporting](#tool-self-diagnosis-health-logging-and-solution-load-reporting) — 38
- [Settings, configuration and team-shared style](#settings-configuration-and-team-shared-style) — 45
- [Preprocessor directives, build configurations and multi-targeting in the editor](#preprocessor-directives-build-configurations-and-multi-targeting-in-the-editor) — 30
- [C# Interactive, scripting, scratch files and ad-hoc evaluation](#c-interactive-scripting-scratch-files-and-ad-hoc-evaluation) — 40
- [T4, custom tools, single-file generators and designer regeneration](#t4-custom-tools-single-file-generators-and-designer-regeneration) — 40
- [Non-C# files inside a C# solution: MSBuild, XML, config, resources](#non-c-files-inside-a-c-solution-msbuild-xml-config-resources) — 29
- [Service references, WCF, SOAP and connected services](#service-references-wcf-soap-and-connected-services) — 35
- [Debugger visualizers, the exception helper, and thread control](#debugger-visualizers-the-exception-helper-and-thread-control) — 36
- [Workspace trust, analyzer/generator sandboxing and supply-chain safety](#workspace-trust-analyzergenerator-sandboxing-and-supply-chain-safety) — 34
- [Scale, performance and responsiveness on large solutions](#scale-performance-and-responsiveness-on-large-solutions) — 32
- [The AI layer, framed as a competitive axis](#the-ai-layer-framed-as-a-competitive-axis) — 34

---

## Code editing, completion and typing assistance

| Feature | IDE | What it is |
| --- | --- | --- |
| Basic (symbol) code completion | both | Ctrl+Space suggests classes, methods, variables, keywords and other symbols valid at the caret. Rider/ReSharper's basic completion also folds in 'Smart' and 'Import Symbol' completion results by default and supports CamelHumps abbreviation matching (e.g. 'AS' matches ArticleSummary). VS's default IntelliSense list is a plainer alphabetical/relevance-sorted list without CamelHumps abbreviation… |
| Smart (type-matching) completion | rider | Ctrl+Alt+Space (ReSharper) / Ctrl+Shift+Space (Rider) filters the completion list to only symbols, expressions, or generated constructs (lambdas, anonymous delegates, out-var locals, unassigned initializer members) matching the expected type at the caret, and puts the most-recently-used matching symbol first. |
| Second basic completion / type-name completion | rider | A follow-up Ctrl+Space press (or Ctrl+Alt+Space) suggests types and extension methods from referenced assemblies even when their namespace isn't imported yet, auto-inserting the required using/import when selected. Also offers type-based variable name suggestions when declaring locals. |
| CamelHumps navigation and completion matching | rider | Every ReSharper/Rider completion and Go-to popup matches on the uppercase/camel-case letters of an identifier — typing 'NPE' or 'npe' can match 'NullPointerException'-style names — in addition to substring and prefix matching. |
| Import completion (usings for referenced/NuGet types) | both | Both IDEs can complete a type or extension method that isn't yet imported and add the using directive automatically. In VS this is a Quick Action driven by two separate Tools > Options > C# > Advanced toggles ('Suggest usings for types in reference assemblies' / 'in NuGet packages'); same-solution types are suggested without opting in. In Rider it's built into second basic completion and Import… |
| IntelliCode whole-line completions | vs | An on-device transformer model (trained on ~500k public GitHub repos) predicts the rest of the current line as grayed-out inline ghost text; Tab accepts (or Tab-Tab when an IntelliSense item is also selected). Configurable to trigger on typing, on Enter for new lines, only after a pause, or manually; works offline/air-gapped since the model runs locally. As of VS 2026 (April 2026 update), native… |
| IntelliCode API usage suggestions in IntelliSense | vs | IntelliCode reorders the standard IntelliSense completion list, placing a starred 'Recommended' segment at the top based on patterns learned from thousands of highly-rated open-source repos plus your own code context. |
| Parameter Info / Signature Help | both | Shows overload signatures and XML-doc parameter descriptions while typing inside call parentheses, highlighting the best-matching overload and graying out incompatible ones. Rider: Ctrl+P, reappears automatically as you move the caret or add a comma. VS: Ctrl+Shift+Space (Parameter Info) with the same overload highlighting. |
| Quick Documentation / Quick Info | both | Inline popup with the rendered XML doc comment, signature, and (Rider) containing-assembly/namespace info for the symbol under the caret. Rider: Ctrl+Q (pinnable, supports navigating to related symbols inside the popup via Ctrl+Q again). VS: hovering shows Quick Info tooltips; Ctrl+K,Ctrl+I forces it, and structured XML doc formatting (param/returns/exception/see) renders inline. |
| Live templates (code snippets) | both | Abbreviation-triggered, parameterized code snippets with tab-stops and live-updated placeholder text (e.g. typing 'for' + Tab expands a full for-loop with editable bounds). Rider/ReSharper ships hundreds of built-in live templates plus a template editor (macros like 'suggest variable name', 'guess type') and custom scopes (in a method body, in a class, etc). VS's classic Code Snippets… |
| Postfix templates | rider | Type a completed expression, then '.' + a keyword to wrap/transform it without moving the caret back — e.g. `expr.if` wraps in `if(expr)`, `expr.not` negates a boolean, `expr.var`/`.field`/`.prop` introduces a variable/field/property, `expr.foreach`/`.for`/`.forr` iterates, `expr.cast`/`.typeof`/`.throw`/`.null`/`.notnull`/`.await`/`.return`/`.new`. Available for C#, VB, JS/TS, and C++. |
| Surround templates | rider | Select a code fragment, press Ctrl+E,U (or Alt+Enter on the selection) to wrap it in a template construct such as try/catch, try/finally, using, #region, if, checked/unchecked, lock. Fully customizable/definable by the user like live templates. |
| File templates | both | Templates that scaffold a whole new file (class/interface/enum/unit test/razor component etc.) with pre-filled boilerplate, namespace, and using directives. Rider: File Templates settings let you edit/add templates used by 'New File' and 'Create New Element' actions, including per-file-kind templates. VS: New Item templates (.cs class/interface item templates) plus the newer 'File > New >… |
| CodeLens (references, changes, tests, work items) | vs | Inline indicators above methods/classes/properties showing reference count (click to peek), authors/change history, linked Azure DevOps work items/bugs, linked code reviews, and associated unit-test pass/fail status — all without leaving the editor. References indicator is available in Community; the source-control history, linked-bug/work-item, and code-review indicators require VS Enterprise… |
| Code Vision | rider | Inline solution-wide-analysis annotations above type/member declarations showing usages count, inheritors/implementations count, and (with plugins) unit-test status, dotTrace/dotMemory runtime data, and F#-specific info; clicking navigates or opens a popup list. Requires solution-wide analysis to be enabled (shows '?' until calculated). |
| Inline parameter name hints | both | Shows the formal parameter name inline before each call argument (e.g. `Foo(count: 5)`). Rider/ReSharper: on by default for ambiguous/literal args, configurable per-argument-kind. VS: off by default, enabled via Tools > Options > Text Editor > C# > Advanced > 'Display inline parameter name hints'; can be toggled temporarily by holding Alt+F1. |
| Inline type hints (implicit var, lambda param/return types) | both | Shows the inferred type inline for `var` locals, lambda parameters, and deconstructed tuple elements. VS: Tools > Options > Text Editor > C#/Basic > Advanced > 'Display inline type hints' (off by default). Rider: Inlay Hints settings (Editor \| Inlay Hints \| C#) offer the same plus additional hint categories. |
| Additional Rider/ReSharper inlay hint categories | rider | Beyond parameter/type hints, Rider's Inlay Hints (Editor \| Inlay Hints, per-language) can show: range/index operator value previews, implicit numeric/reference conversion markers, nullable-context annotations, closure-captured variable markers, LINQ chain intermediate-type hints, and heap-allocation indicators — each independently toggleable. |
| Next Edit Suggestions (NES) | both | AI-driven prediction of the *next* edit you're likely to make elsewhere in the file (not just the next token), shown as an inline diff-style suggestion you can Tab through. JetBrains AI Assistant added this to Rider/ReSharper in the 2025.3.1 release. Visual Studio/GitHub Copilot ship an equivalent 'Copilot Next Edit Suggestions' feature that proposes and lets you tab-navigate between related… |
| GitHub Copilot native agent integration | both | VS 2022/2026: GitHub Copilot Completions (ghost-text, now with syntax-highlighted suggestions and partial-accept-to-cursor) and Copilot Chat are bundled/recommended components installed by default with any workload since 17.10, with inline chat via right-click > Chat, agent mode for multi-file edits, and slash commands. Rider 2026.2: GitHub Copilot is now a natively integrated agent… |
| JetBrains AI Assistant / Junie | rider | JetBrains' own AI stack: AI Assistant provides chat, inline completion, and code generation grounded in the IDE's live project model; Junie (bundled with AI Pro+ subscriptions) is an autonomous multi-step/multi-file coding agent (refactors, test generation, feature implementation) that runs inside the Rider session using Rider's own semantic index rather than reconstructing context from… |
| Multi-caret / multiple cursors | both | Both support placing multiple simultaneous carets for parallel editing. VS: Ctrl+Alt+Click to add a caret; Ctrl+Alt+Up/Down to add above/below; Shift+Alt+. adds the next matching-text occurrence as a selection, Shift+Alt+; adds all matches; Esc exits multi-caret mode; primary caret renders red, secondaries light-blue. Rider/ReSharper: Alt+J (Add Selection for Next Occurrence, IntelliJ keymap) /… |
| Column (box) selection | both | Rectangular block selection across multiple lines for aligned edits. VS: hold Alt while dragging or arrow-key-selecting. Rider: hold Alt while dragging, or Alt+Shift+Insert to toggle persistent column-selection mode. |
| Expand/Shrink selection (syntax-aware selection) | both | Grows or shrinks the current selection outward/inward along syntax-tree boundaries (token → expression → statement → block → member → type). Rider/ReSharper: Ctrl+W to extend, Ctrl+Shift+W to shrink back (context-sensitive, remembers the path taken). VS: Shift+Alt+= to expand, Shift+Alt+- to shrink ('Edit.ExpandSelection'/'Edit.ContractSelection'), introduced as a semantic selection feature for… |
| Structural navigation (Tab/Shift+Tab through template stops and code structure) | rider | Once a live template finishes or in certain constructs, Tab/Shift+Tab moves the caret through logical 'hot spots' (matching braces, next argument, next statement) instead of inserting a literal tab, as long as the caret isn't in leading whitespace (where Tab still indents). |
| Complete Statement | vs | Ctrl+; (or Ctrl+Shift+Enter, VS 2022 rebindable to Ctrl+;) finishes the current line — inserting the trailing semicolon (or braces for blocks like if/for/foreach/switch/try) in the correct place — and moves the caret to a new line, letting you type a statement's contents first (e.g. arguments) without worrying about closing punctuation position. |
| Smart Enter / Smart Backspace / Smart Home | rider | Shift+Enter ('Smart Enter') completes the current statement or block (adds missing braces/semicolons/parens) and positions the caret on a sensible new line, even from the middle of an incomplete construct like an unclosed `if(`. Smart Backspace deletes back to the previous meaningful indent level rather than one character. Smart Home moves to the first non-whitespace character, then to column 0… |
| Auto-insert / typing assistance for braces, quotes, brackets | both | Both auto-close brace/quote/bracket pairs, auto-indent on Enter inside braces, and support typing over an auto-inserted closer. Rider adds smarter behavior for generic `<>` matching in type contexts and doesn't auto-close inside plain string literals unless configured. VS's brace completion is configurable at Tools > Options > Text Editor > C# > Advanced ('Automatic brace completion'). |
| XML doc comment auto-continuation | both | Typing `///` above a member auto-generates a `<summary>`/`<param>`/`<returns>`/`<typeparam>` skeleton matching the member's signature; pressing Enter inside an existing doc comment continues the `///` prefix on the next line. Both IDEs support this; Rider additionally offers a 'Copy documentation comment from base member' quick-fix and keeps `<param>` tags in sync when you reorder/rename… |
| Comment reformatting / paragraph reflow | rider | ReSharper/Rider can reflow (word-wrap) long comment text to fit a configured line-length when editing, similar to a text editor's paragraph-fill, and keeps `//` / `/* */` / `///` prefixes intact across the rewrap. |
| Formatting engine and per-construct formatter options | both | Both ship a full reformat-document command (VS: Ctrl+K,Ctrl+D; Rider: Ctrl+Alt+L) with hundreds of granular style knobs (brace placement, blank-line rules, wrapping/alignment for long argument lists, chained method calls, LINQ query clauses, etc.). ReSharper's formatter has historically offered more fine-grained wrapping/alignment rules (e.g. per-construct 'wrap if long' with configurable… |
| Code Cleanup | both | One-click/on-save application of a configurable set of fixes (remove unused usings, sort usings, apply code style, add/remove braces, use var/explicit type, apply naming rules, etc.) across a file or whole solution. VS: Analyze > Code Cleanup, profiles configurable at Tools > Options > Text Editor > Code Cleanup, with 'Run Code Cleanup on Save' available since VS 2022 17.1. Rider/ReSharper:… |
| EditorConfig support depth | both | .editorconfig drives formatting, code-style, and naming-rule enforcement in both IDEs and takes precedence over IDE-global option pages. VS: `.editorconfig` powers Roslyn code-style analyzers (IDE#### diagnostics with severity levels enforceable at build time via `dotnet_diagnostic.IDEnnnn.severity`), integrated with the Quick Actions light bulb. Rider/ReSharper: recognizes both the standard… |
| Naming style enforcement | both | Configurable per-identifier-kind naming rules (capitalization, prefix/suffix, access-modifier variants) with live inspection warnings and a bulk 'Inspect whole solution for naming style compliance' action (Rider/ReSharper). VS: Tools > Options > Text Editor > C# > Code Style > Naming, backed by Roslyn naming-style analyzers (IDE1006) with severity configurable per-rule in `.editorconfig`. |
| Generate: Constructor | both | Generates a constructor from a chosen subset of fields/properties. VS: Quick Actions (Ctrl+.) > Generate constructor, with a picker to select/reorder members. Rider: Alt+Insert > Constructor, same member-picker plus 'generate constructor and use it to initialize readonly fields' and null-check insertion options. |
| Generate: Equality members (Equals/GetHashCode/operators) | both | Generates `Equals`, `GetHashCode`, and optionally `==`/`!=` operators and `IEquatable<T>` from selected fields. VS: Quick Actions > Generate Equals/GetHashCode. Rider: Alt+Insert > Equality Members, with extra choices for base-class equality chaining, `IEquatable<T>` implementation, and record-style value equality. |
| Generate: ToString / Override members | both | Generates a `ToString()` override listing selected members. Rider: Alt+Insert > ToString(), with a member picker and format-string customization templates. VS: available as part of the general 'Override members' Quick Action / generate flow (implement/override picker includes `ToString` when overriding `object`). |
| Generate: Implement/Override interface & base members | both | Lists missing interface members or overridable base-class members and inserts stub implementations. VS: light bulb 'Implement interface' / 'Implement interface explicitly' Quick Action, and 'Override members' via Alt+Insert-equivalent in the type-member dropdown at the top of the editor. Rider: Alt+Insert > Implement Members / Override Members, with checkboxes per member and an option to… |
| Generate: Delegating members | rider | Alt+Insert > Delegating Members lets you pick a private field/property and specific members on its type, then generates public wrapper members on the containing type that forward calls to it (implements the decorator pattern mechanically). |
| Generate: IDisposable / Dispose pattern | both | Scaffolds the full or simplified Dispose pattern (`IDisposable`, optional finalizer, `Dispose(bool)` protected virtual method, disposed-flag guard). VS: available via 'Implement interface' Quick Action when adding `IDisposable`, offering a 'with Dispose pattern' variant. Rider: Alt+Insert > Implement Members with the same disposable-pattern expansion, plus a dedicated inspection nudging you to… |
| 'Complete Statement'-adjacent Quick Actions (add braces, add null-check, add missing switch cases) | vs | A family of light-bulb Quick Actions that flesh out partially-typed constructs: 'Add braces' around a single-line if, 'Add null check' for a constructor-assigned parameter (`?? throw new ArgumentNullException(...)`), 'Fill switch/Add missing cases' plus default case for an enum switch, 'Add argument name' for positional call arguments. |
| Code transformation Quick Actions (interpolated strings, object/collection initializers, pattern matching, local functions, tuple deconstruction, sync/async conversion) | both | Both IDEs offer one-click rewrites: string.Format → interpolated string, sequential assignment → object/collection initializer, type-check+cast → pattern matching (`is T x`), anonymous function → local function, tuple `.Item1` access → explicit tuple names, full property → auto property (and back), block-bodied member → expression-bodied member, `async` method with no `await` → synchronous and… |
| Structural Search and Replace (SSR) | rider | Pattern-based search/replace over the syntax tree using placeholder variables (e.g. find every `$obj$.ToString() == $str$` regardless of variable names) rather than plain text/regex; patterns can be saved as custom, solution-shareable code inspections with their own quick-fix replacement, severity, and description — authorable entirely through the IDE UI. |
| TODO Explorer | rider | A dedicated tool window aggregating all TODO/FIXME/BUGBUG-style comments (pattern list is user-configurable, including custom regex patterns and per-pattern highlight color) across the whole solution, with filtering and grouping by file/folder. |
| Integrated spell checking (ReSpeller) | rider | Built-in spell checker (ReSpeller) that checks identifiers (splitting camelCase/PascalCase/snake_case into words), comments, and string literals against configurable dictionaries; flagged typos get Alt+Enter quick-fixes (accept word, add to dictionary, or — for identifiers — trigger a full Rename). Severity is configurable from a hint up to an error-level highlight. A dedicated 'Typo in… |
| Clipboard history / Paste from History | both | Recall and paste from a ring of recently-copied snippets rather than only the last one. Rider: Ctrl+Shift+V opens 'Paste from History' with a preview list, cleared when the IDE closes. VS: Ctrl+Shift+V cycles directly through the clipboard ring in place (no preview list, native since VS2017 Update 8) — pressing it repeatedly steps through prior entries. |
| Local History | rider | Automatic, always-on local version control that snapshots every meaningful change (edits, test runs, deploys, external file changes) independent of Git, browsable per-file or per-folder with diff/revert to any past revision, persisting across IDE sessions (cleared only on IDE reinstall). |
| Actions on Save | rider | Editor \| General \| Actions on Save settings page lets you enable, per-save: reformat code, optimize imports, rearrange code (per formatter rules), run Code Cleanup with a chosen profile, and (for supported languages) run a linter/formatter — all automatically on every file save, independent of VS-style explicit 'Code Cleanup on Save' opt-in scope. |
| Rearrange code (member reordering rules) | rider | The formatter can automatically reorder type members (fields before properties before constructors before methods, public before private, alphabetically within a group, etc.) according to fully configurable rearrange-code rules, invocable on demand (Ctrl+Alt+Shift+L for 'Reformat and rearrange') or via Actions on Save. |
| IntelliSense/Copilot priority and ordering control | vs | Starting with the April 2026 Visual Studio 2026 update, when the built-in IntelliSense completion list is open, its suggestions now take priority over IntelliCode/Copilot inline ghost-text suggestions, resolving a longstanding conflict where AI ghost text could visually collide with or override the traditional completion popup. |
| Code completion / parameter-info popup UI overhaul | rider | Recent ReSharper/Rider releases (2025.3–2026.2 cycle) redesigned the completion, parameter-info, and quick-doc popups for a cleaner, more consistent look, and added proper support for editor font zoom so popups scale with the zoomed editor font instead of staying fixed-size. |
| Extension-method completion after dot | both | Both IDEs surface applicable extension methods (LINQ, custom) in the member-completion list after typing `.` on a compatible expression, even from namespaces not yet imported, auto-adding the using on acceptance. Rider explicitly documents this as part of Import Symbol Completion; VS surfaces it through standard IntelliSense combined with the 'suggest usings' Quick Action options. |

---

## Navigation and search

| Feature | IDE | What it is |
| --- | --- | --- |
| Search Everywhere | rider | Double-tap Shift opens a single unified search box covering types, members, files, recent files, plain text, IDE actions, tool windows, run configurations, settings/preferences, and Git branches/commits/tags. Results are tabbed (All, Classes, Files, Symbols, Actions, Text, Everything else); a gear icon lets you toggle inclusion of non-project items, and it automatically broadens to referenced… |
| Go To All / All-In-One Search | vs | Ctrl+T (or Ctrl+,) opens Code Search for lines/files/types/members/symbols; Ctrl+Q or Ctrl+Shift+P opens Feature Search for menus, options and commands. Since 17.6 both live in one 'All-In-One Search' surface with filter prefixes (f:, t:, m:, x:), adjustable per-filter scope (document/project/solution/external), a dockable window mode, and a toggleable preview pane. |
| AI-assisted search fallback (Ask Copilot / Did You Mean) | vs | In Feature Search, an 'Ask Copilot' button interprets queries that don't literally match a menu name (e.g. 'prettify file' -> Format Document). In Code Search, a Copilot-powered 'Did You Mean' (preview) suggests corrected search terms when results are empty or the top hit looks wrong, toggleable under Tools > Options > GitHub > Copilot > Search. |
| Go to File | both | Rider: Ctrl+Shift+N opens a dedicated file-name search popup. VS: type or prefix 'f:' in Code Search, or press Ctrl+Shift+T / Ctrl+1,Ctrl+F, to search filenames across the solution (with an 'external documents' toggle). |
| Go to Type | both | Rider: part of Go to Symbol (Ctrl+Alt+Shift+N) filtered to types, or Navigate \| Go to Type Declaration. VS: 't:' prefix or Ctrl+1,Ctrl+T (legacy Ctrl+T shortcut before 17.6 opened this directly). |
| Go to Member / File Member | both | Rider's Go to File Member (Ctrl+F12) lists methods/properties/fields of the *current file only* as a fast popup. VS's Go to Member ('m:' prefix, Alt+\) searches members solution-wide by default, with a configurable per-filter scope (current document, project, solution, or external). |
| Go to Symbol | both | Rider: Ctrl+Alt+Shift+N searches any code symbol (type or member) across the solution and *all referenced assemblies*. VS: '#' prefix or Ctrl+1,Ctrl+S searches symbols; scope for the symbol filter is configurable independently of the type/member filters. |
| Go to Action / Feature Search | both | Rider: Ctrl+Shift+A (Find Action) searches every IDE command, setting and tool window by name. VS: Ctrl+Q or Ctrl+Shift+P (Feature Search) does the same, additionally showing the menu path and shortcut for each hit, and (17.13+) can hand off unmatched queries to Copilot. |
| Go to Declaration / Definition | both | Rider: Ctrl+B or Ctrl+click; single result navigates directly, multiple results drop down a picker. VS: F12, right-click > Go To Definition, or Ctrl+click; opens the defining file in a new editor window/tab and focuses it. |
| Go to Implementation(s) | both | Rider: Ctrl+N,K (or Navigate \| Implementation(s)) jumps from an interface/abstract member to its concrete implementations, auto-navigating if there's exactly one. VS: Ctrl+F12 does the same, listing multiple hits in the Find Symbol Results window. |
| Go to Base Symbols / Go to Base | both | Rider: Ctrl+U (Navigate \| Base Symbols) walks up the inheritance/override chain from a member or type. VS: Alt+Home (Go To Base) does the equivalent, listing multiple base candidates in a Go To Base window. |
| Go to Derived Symbols | rider | Ctrl+Alt+B (Navigate \| Derived Symbols) lists types/members that inherit from or override the current symbol, as a command distinct from Go to Implementation (which targets interface implementers). |
| Go to Type Declaration (type-of-symbol) | rider | Ctrl+Shift+B jumps from a variable, expression, or member usage directly to the *declaration of its type*, distinct from Go to Declaration (which would go to the variable's own declaration). |
| Peek Definition | vs | Alt+F12 (or Ctrl+click configured for peek) opens an inline, editable, expandable region under the current line showing the definition — without leaving the file. Supports chained/nested peeks with a breadcrumb-dot trail (Ctrl+Alt+-/= to move between them), F8/Shift+F8 for multiple results (e.g. partial classes), full editing with autosave-to-tab, and Ctrl+Alt+Home to promote the peek into a… |
| Quick Definition popup | rider | Alt+F12 opens a read-only floating popup showing the symbol's definition without changing editor focus; unlike VS's Peek, it is not inline-editable and does not support chaining nested popups. |
| Find Usages with grouping, filtering, preview | rider | Alt+F7 finds all usages in the solution and referenced assemblies; the Find window's tree can be grouped by usage kind (declaration, invocation, parameter, return, etc.), filtered to read/write-only occurrences or by typed name, and a 'Preview Source' toggle shows the surrounding code without opening the file. |
| Find All References window (Group by / Kind filter) | vs | Shift+F12 opens Find All References with a 'Group by' dropdown (definition, project, file, etc.) and a Kind column whose header exposes a filter (Read, Write, Reference, Name, Namespace, Type) — but only in the tree view, not the plain list view. F8 cycles through results inline. |
| Show Usages popup | rider | Ctrl+Alt+F7 shows the same usage list as Find Usages but as a transient popup with inline context, navigable by arrow keys/Enter and filterable by typing; it expands into the full Find tool window if the search runs long, and can pivot into a type dependency diagram. |
| Persistent search-result tabs | both | Rider: every new Find Usages/Find in Files search opens a new tab in the Find window; tabs persist until the solution closes. VS: 'Keep Results' preserves up to five prior Find in Files result sets as tabs (oldest is recycled past five), with an option to always keep results by default, plus a 'Modify Find' button to rerun a tweaked version of a prior search and a 17.12+ refresh button. |
| Recent Files | both | Rider: Ctrl+E lists recently opened files in the current solution (toggle to 'recently changed only'), persisted per-solution across sessions. VS: recent files surface as the 'r:' filter/Ctrl+1,Ctrl+R in Go To, and as an unqueried list shown when Code Search opens with no text typed (17.13+). |
| Recently Changed Files / Recent Edits | rider | Ctrl+N,E lists files or symbols you've recently *modified* (as opposed to just viewed) in the current solution, with a toggle to widen it back to all viewed files. |
| Recent Locations (with code preview) | rider | Ctrl+Shift+E opens a popup listing recently visited caret positions, most-recent first, each shown with a snippet of surrounding code; searchable by code text, filename, or breadcrumb, and filterable to changed-code-only. Deleting an entry also removes it from the Back navigation stack. |
| Navigate Backward / Forward | both | Rider: Ctrl+Alt+Left/Right. VS: Ctrl+- / Ctrl+Shift+- (toolbar buttons available), retaining the last 20 caret locations, cycled through on repeated presses. |
| Go to Last Edit Location | both | Rider: Ctrl+Shift+Backspace returns the caret to the last place you edited code, and can be pressed repeatedly to step back through prior edits. VS: the identically-shortcut (Ctrl+Shift+Backspace) 'Go To Last Edit Location' command under Edit > Go To does the same for the single most recent edit. |
| Bookmarks | both | Rider: F11 toggles an anonymous bookmark on the current line; Shift+F11 opens the Bookmarks tool window (Alt+2). VS: Ctrl+K,Ctrl+K toggles a bookmark and opens the Bookmark Window; bookmarks are also visible/navigable from there. |
| Mnemonic / numbered bookmarks | rider | Ctrl+F11 assigns a bookmark a mnemonic (digit 0-9 or letter A-Z) via a popup where you can also type a description; up to 10 numbered bookmarks can coexist, jumped to directly with Ctrl+<digit>, toggled with Ctrl+Shift+<digit>, and listed via Ctrl+Shift+F11. |
| Task List (code-linked TODO tracking) | vs | Comments with recognized tokens (TODO, HACK, UNDONE, custom-defined ones) are auto-collected into the Task List window (View > Task List, Ctrl+\,T). Separately, Edit > Bookmarks > Add Task List Shortcut (Ctrl+K,Ctrl+H) creates an explicit, differently-iconed 'code shortcut' bookmark that also appears there. |
| File Structure popup / Structure tool window | rider | Ctrl+F12 pops up a filterable list of the current file's members (narrows as you type); Alt+7 opens the persistent Structure tool window showing the same tree synced to the editor, with a 'From Related Files' toggle to show inherited members and options to link selection bidirectionally with the caret. |
| Navigation bar (breadcrumb-style dropdowns) | vs | Dropdown boxes atop the code window for project / type / member, letting you jump directly to any of them; Ctrl+F2 focuses it, Tab moves between boxes, Esc returns focus to code without selecting. Configurable per-language under Tools > Options > Text Editor. |
| Type Hierarchy window | rider | Ctrl+H opens a tree showing both base types and inheritors of the selected type (including from referenced libraries), each node navigable by click; you can re-root the tree on any node ('Base on This Type'), filter by typed/CamelHumps name, and invoke refactorings directly on hierarchy members (Ctrl+Alt+Shift+T). |
| Call Hierarchy window | both | VS: Ctrl+K,Ctrl+T (or right-click > View Call Hierarchy on a method/property/constructor) opens a window showing callers and callees, expandable to multiple levels. Rider: has a native Call Hierarchy view (incoming/outgoing calls, toggleable) plus a separate 'Call Tracking' feature that highlights call sites directly in the gutter/editor as you navigate. |
| Class View | vs | A tool window listing every class/type across the solution (or per-project) in a flat/namespace-nested tree, distinct from Solution Explorer's file-based view; selecting a type shows its members in a lower pane. |
| Object Browser | vs | Browses namespaces/types/members across .NET assemblies, COM components, and type libraries referenced by the solution, independent of any specific project — opened via View > Object Browser. |
| Assembly Explorer | rider | A dedicated tool window for browsing the contents of compiled assemblies referenced by the solution (or opened standalone), reachable from Navigate To (Ctrl+Shift+G) > Assembly Explorer on a symbol; lets you drill into namespaces/types/members of any referenced .dll. |
| Navigation to decompiled sources (editor-time, default-on) | rider | Navigate \| Decompiled Sources (part of the Navigate To menu) opens a full decompiled view — with method bodies, not just signatures — of any type from a referenced assembly, generated by Rider's built-in decompiler (dotPeek engine) automatically, with no setting to enable and no legal-disclaimer prompt. |
| Metadata view / decompiled sources (editor-time, opt-in) | vs | By default, Go To Definition on a symbol with no source shows a metadata-only stub (declarations, no method bodies), tab-labeled '[from metadata]'. Enabling Tools > Options > Text Editor > C# > Advanced > 'Enable navigation to decompiled sources' switches this to full ILSpy-based decompiled source (first use requires accepting a legal/licensing disclaimer). Internal-only members are never shown… |
| Debug-time autodecompilation of external code | vs | Since VS 2022 17.7, stepping into external .NET code during debugging (with Just My Code off) or double-clicking an external frame in the Call Stack window ('Show External Code') auto-decompiles it via ILSpy and drops you at the current execution point, with breakpoints settable in the generated code; results populate an 'External Sources' node in Solution Explorer. This is separate from, and… |
| Solution-wide dependency diagram | rider | Navigate to Architecture \| Project Dependency Diagram to build a graph of all project-to-project references in the solution, computed from project files without a compile step; toggle to show transitive dependencies, and distinguish solid (compile-time) vs. other link styles. |
| Type Dependency Diagram | rider | Introduced in Rider 2023.3, lets you add arbitrary types — even from different projects or compiled assemblies — onto a single diagram and visually study how they depend on each other at the type level, one level more granular than the project-level diagram. |
| Code Map | vs | Enterprise-only. Right-click any method/type and choose 'Show on Code Map' to add it to a DGML diagram of code relationships; Architecture \| Generate Code Map for Solution builds a whole-solution map. During debugging, you can visualize the live call stack as a map. Community/Professional can *open* (view) maps generated in Enterprise but cannot create or edit them. |
| Dependency (layer) diagrams with Live Dependency Validation | vs | Enterprise-only. Create a logical layer/dependency diagram, map solution artifacts onto its layers, then validate actual code dependencies against the diagram — errors surface live in the Error List as you code, for C# and Visual Basic. |
| NuGet dependency tree in project view | both | Both Solution Explorer (VS, under a project's Dependencies > Packages node) and Rider's Explorer/Dependencies node let you expand a package to see its transitive NuGet dependencies inline in the tree, without opening a separate package manager UI. |
| Unified 'Navigate To' menu | rider | Ctrl+Shift+G opens a single menu aggregating ~18 context-sensitive navigation targets for the symbol under the caret: Declaration, Implementation, Usages, Base Symbols, Derived Symbols, Type Declaration, Extension Methods, Exposing APIs, Consuming APIs, Implementing Members, Overriding Members, Member Overloads, Generic Substitutions, Parameter Declaration, Control Flow Target, Function Exits,… |
| Navigate to Extension Methods | rider | From the Navigate To menu, jumps from a type to the extension methods applicable to it, wherever they're declared in the solution or referenced assemblies. |
| Navigate to Exposing / Consuming APIs | rider | From the Navigate To menu on a type/member, jumps to the public API surface that exposes it (e.g. which public methods return this type) or that consumes it (e.g. which public methods accept it as a parameter). |
| Find Usages of Related Entities | rider | Extends Find Usages to related symbols in one pass — e.g. finding usages of a virtual member together with all of its overrides across derived classes, or of a type together with usages of its subclasses' constructors. |
| Generic-type disambiguation in Find Usages | rider | Running Find Usages on a closed generic type/member (e.g. IFoo<string>) prompts a popup letting you choose whether to search all IFoo<> usages generically or only exact IFoo<string> matches. |
| Sticky scroll / sticky lines | both | Rider ('Sticky Lines', 2024.1+, Settings \| Editor \| General \| Appearance, default 5 lines max) and VS ('Sticky Scroll', 17.5+ Preview, Tools > Options > Text Editor > General) both pin enclosing class/method/namespace headers to the top of the editor as you scroll past them, clickable to jump to that scope. |
| Inheritance margin glyphs | both | Rider shows gutter icons (HasImplementations, HasOverrides, Implements, Overrides, Hides, and combinations) next to declarations; hovering summarizes targets, clicking navigates directly or lists multiple. VS's Inheritance Margin (Tools > Options > Text Editor > C# > Advanced > 'Show inheritance margin') adds analogous icons for implementations/overrides in the left margin, with a… |
| Alt+Enter navigation actions | rider | On lines flagged with an inheritance/gutter marker, Alt+Enter surfaces navigation actions (e.g. 'Navigate to base') alongside the usual quick-fixes/refactorings in the same context-action popup, so navigation and code-fixing share one entry point. |
| Reference/symbol highlighting in current file | both | Selecting a symbol highlights all its occurrences (declaration + references) in the open file. Rider: Ctrl+Shift+F7 (Highlight Usages in File), cycle with F3/Shift+F3-equivalent 'Next/Previous Occurrence'. VS: automatic on selection, cycle with Ctrl+Shift+Down/Up, color configurable under Fonts and Colors. |
| CodeLens | vs | Inline indicators above class/method declarations showing reference counts (with hover-to-peek referencing code, click to open Find Symbol Results, or Code Map), plus — requiring Professional/Enterprise and a Git or Azure DevOps/TFVC-backed repo — code-change/authorship history, linked work items, linked bugs, linked code reviews, and associated-unit-test status/results, all inline. Community… |
| Structure Visualizer (brace/indent guide lines) | vs | Vertical dashed guide lines connecting matching braces, toggleable at Tools > Options > Text Editor > General > 'Show structure guide lines', making block boundaries visually traceable at a glance. |
| Enhanced/map-mode scrollbar | vs | The scrollbar can render a miniature code preview ('map mode') that pops a larger preview thumbnail as you hover up/down it, plus change/error/bookmark markers along its length. |
| Sync tool window with active editor | both | Rider: Explorer's 'Always Select Opened File' option (or manual 'Select Opened File' action) highlights the current file's node. VS: Solution Explorer's 'Sync with Active Document' toolbar button does the same, jumping the tree selection to match the active editor tab. |
| Solution-wide Find and Replace with regex and preview | both | Rider: Ctrl+Shift+F/R search/replace across solution, project, a directory tree, or a custom scope, with full regex + backreferences ($n), a live preview pane that behaves like a real (read/write) mini-editor where Ctrl+F and Alt+Enter still work, 'Preserve Case' on replace, and wildcard file-mask scoping. VS: Ctrl+Shift+F/H open Find/Replace in Files with regex + an Expression Builder helper,… |
| CamelHumps / camel-case search | both | Typing only the capitalized letters of a long identifier (e.g. 'CVM' for CredentialViewModel, or 'FSS' for FolderSizeScanner) narrows results to matches in both Rider's Search Everywhere/Go to Symbol and VS's Go To/Code Search. |
| Go To Next/Previous Issue in File | vs | Alt+PgDn / Alt+PgUp jump the caret between the next/previous compiler error, warning, or analyzer diagnostic in the current file, listed under Edit > Go To. |

---

## Refactorings and code generation

| Feature | IDE | What it is |
| --- | --- | --- |
| Refactor This (context refactoring menu) | rider | Ctrl+Alt+Shift+T opens a menu listing only the refactorings applicable at the current caret position or selection, so the user doesn't need to memorize individual shortcuts. |
| Rename | both | Solution-wide rename of any symbol (type, member, local, parameter, namespace) with all references updated automatically. Rider: Shift+F6, in-place inline editing with a popup offering to also update comments/string literals/overload chains; Visual Studio: Ctrl+R,Ctrl+R, inline rename box with checkboxes for comments/strings and an offer to rename the containing file to match. |
| Change Signature | both | Add, remove, reorder, or retype parameters and update every call site. Visual Studio: Ctrl+R,Ctrl+V opens a Change Signature dialog with an Add Parameter sub-dialog (required vs. optional/default, named-argument or TODO-variable insertion at call sites, or omit optional args entirely). Rider: Ctrl+F6 opens an equivalent dialog and additionally propagates through delegates and base/derived… |
| Extract Method | both | Turns a selected code fragment into a new method, inferring parameters and return type/value. Rider: Ctrl+Alt+M; Visual Studio: Ctrl+R,Ctrl+M or via Quick Actions and Refactorings (Ctrl+.), with immediate inline rename of the new method. |
| Extract Local Function | vs | Turns a selected code fragment into a local function nested inside the enclosing method, for logic that's only ever called from that one method. Invoked via Ctrl+. > Extract local function. |
| Extract Interface | both | Generates an interface from a subset of a class/struct's public members and makes the source type implement it. Visual Studio's dialog (Ctrl+R,Ctrl+I) lets you pick the new interface name, target filename (or same file), and which members to include; Rider offers the same via Refactor This. |
| Extract Class | rider | Splits a chosen subset of a class's members off into a brand-new class, replacing the extracted members in the original class with delegating calls to an instance of the new class (composition, not inheritance). |
| Extract Superclass / Extract Base Class | both | Pulls selected members up into a brand-new base class/superclass, with an option to mark them abstract. Visual Studio: Ctrl+. on a member > "Pull member(s) up to new base class" opens an Extract Base Class dialog with a per-member "Make abstract" column; Rider: Extract Superclass via Refactor This. |
| Introduce Variable | both | Replaces a selected expression with a new local variable (optionally replacing all occurrences of that expression), inferring the type from usage. Rider: Ctrl+Alt+V; Visual Studio: Ctrl+. > "Introduce local for (all occurrences of) 'expression'". |
| Introduce Field | rider | Creates a new field from a selected expression, letting the user choose whether it's initialized inline or from the constructor, then replaces occurrences of the expression within the type with references to the field. Shortcut Ctrl+Alt+F. |
| Introduce Parameter | both | Converts a local expression or variable into a new parameter of the enclosing method, updating every call site. Rider: Ctrl+Alt+P; Visual Studio added this as a Quick Action in VS 2022 with multiple call-site update strategies. |
| Introduce Constant | rider | Variant of Introduce Variable/Introduce Field that creates a `const` (local, field, or type-level) from a selected literal or expression and replaces matching occurrences with the new constant. |
| Inline Method/Variable/Field/Parameter/Class | both | Replaces usages of a symbol with its definition and (optionally) removes the original declaration. Rider's single Inline refactoring (Ctrl+Alt+N) covers methods, variables, fields, parameters, and whole classes; Visual Studio ships Inline Method (with "inline and keep" vs. "inline and remove" options) and Inline Temporary Variable as separate, narrower commands. |
| Move (type to file/namespace/folder, static members, instance method) | both | Rider's F6 "Move" family: Move Type to Another File, Move Type to Another Namespace, Move Type to Folder, Move to Another Type (relocate nested types/static members into an existing or new type), and Move Instance Method to Another Type (choosing a new receiver from the method's own parameter/field types). Visual Studio covers Move type to matching file and Move to namespace as Quick Actions,… |
| Pull Members Up | both | Moves selected members from a derived type up to a base class or interface via a checkbox dialog, with a "make abstract" option per member. Visual Studio: Ctrl+. > "Pull members up to base type"; Rider: same result via Refactor This. |
| Push Members Down | rider | The inverse of Pull Members Up: moves members from a base type down into one or more derived types. |
| Safe Delete | rider | Deletes a symbol, file, project, or assembly reference only after scanning all usages; if any usage can't be safely removed, Rider lists them for manual resolution instead of deleting blindly. For polymorphic members it offers to delete just the selected member, the member plus all overrides, or the whole hierarchy. Shortcut Alt+Delete. |
| Encapsulate Field | both | Wraps a field in a property and rewrites usages to go through it. Visual Studio (Ctrl+R,Ctrl+E) offers "Encapsulate field (and use property)" vs. "...but still use field"; Rider offers the same choice via Refactor This. |
| Convert Property ↔ Method(s) | both | Converts a property (and its get/set) into explicit Get/Set methods, or the reverse. Visual Studio: Ctrl+. > "Replace method with property" / "Replace property with methods"; Rider: "Convert Property to Method(s)" / "Convert Method to Property" refactorings. |
| Convert Auto-Property ↔ Full Property | both | Toggles a property between the auto-implemented form and an explicit backing-field form. Visual Studio: Ctrl+. > "Convert to full property" / "Use auto property"; Rider offers the identical pair of context actions. |
| Convert Indexer ↔ Method | rider | Dedicated refactorings that turn an indexer (`this[...]`) into an equivalently-named method, and vice versa, rewriting all call sites. |
| Convert Abstract Class ↔ Interface | rider | Rewrites a type declaration (and every implementer/subclass) to switch between an `abstract class` and an `interface`. |
| Convert Static Method ↔ Extension Method | rider | "Convert Static to Extension Method" turns a static helper's first parameter into the `this` receiver (and updates call sites to instance-call syntax); "Convert Extension Method to Plain Static Method" reverses it. |
| Convert Anonymous Type to Named Type | rider | Generates a named class/record from an anonymous-type literal (`new { A = 1, B = 2 }`) and rewrites usages to reference it. |
| Convert to Global Using / Non-Global Using | both | Promotes a per-file `using` directive to a solution-wide `global using` (or the reverse). Rider ships this as a dedicated refactoring; Visual Studio/Roslyn exposes the equivalent as a Ctrl+. code action once the project targets C# 10+. |
| Convert to File-Scoped Namespace | both | Converts a block-scoped `namespace Foo { ... }` declaration to C# 10's file-scoped `namespace Foo;` form (and back). Visual Studio ships this as a named Quick Action (called out explicitly in the VS 2022 productivity release notes); Rider offers the matching context action. |
| Transform Parameters | rider | Restructures a method's parameter list — e.g., grouping several parameters into a new parameter object/tuple/struct — rewriting the declaration and every call site to match. |
| Make Method/Property Static / Non-Static | both | Toggles a member's `static` modifier and fixes up call sites (adding/removing an instance receiver as needed). Visual Studio: Ctrl+. > "Make static" for members, plus a separate "Make local function static" that can pass captured variables in explicitly; Rider: "Make Method/Property Static" and "Make Method Non-Static" refactorings covering the same ground. |
| Use Base Type Where Possible | rider | Widens a variable's, parameter's, or member's declared type to the least-derived compatible base type or interface still usable at every point it's referenced, run at a chosen scope (member, file, project, or solution). |
| Replace Constructor with Factory Method | rider | Converts a public constructor into a private constructor plus a static factory method, updating every call site to go through the new factory. |
| Invert Boolean | rider | Renames a boolean field/property/parameter/variable to its logical opposite (e.g. `IsEnabled` → `IsDisabled`) and rewrites every read and assignment site to preserve behavior. |
| Extract Members to Partial Type / Copy Type | rider | "Extract Members to Partial" splits selected members of a class out into a new `partial` declaration of the same type; "Copy Type" duplicates an entire type (optionally including its file) under a new name as a starting point. |
| Adjust Namespaces (solution-wide sync) | both | Aligns namespace declarations with actual folder structure and fixes every affected `using`. Rider: Refactor \| Adjust Namespaces, runnable at file/folder/project/solution scope, backed by a live "namespace doesn't match folder" inspection. Visual Studio: the "Sync Namespaces" command on a project node in Solution Explorer runs the project-wide equivalent, plus a single-namespace "Change… |
| Sync Type and Filename | vs | Bidirectional Quick Action: "Rename file to <TypeName>.cs" or "Rename type to <Filename>", keeping a type and its containing file in sync. |
| Move Type to Matching File | both | Moves a type out of a multi-type file into its own new file named after the type. Visual Studio: Ctrl+. > "Move type to TypeName.cs"; Rider: "Move Type to Another File", part of the F6 Move family. |
| Move Declaration Near Reference | vs | Moves a local variable's declaration down to just above its first use, narrowing its visible scope. Invoked via Ctrl+. > "Move declaration near reference". |
| Remove Unreachable Code | both | Deletes code the compiler's flow analysis proves can never execute (e.g. statements after an unconditional `throw`/`return`). Available as a Quick Action in Visual Studio and an equivalent quick-fix in Rider. |
| Remove Unused References (solution-wide) | vs | For SDK-style projects, a Solution Explorer command ("Remove Unused References" on the project or Dependencies node) analyzes and proposes removing unused ProjectReferences and NuGet packages via a review dialog with per-item Keep/Remove. |
| Add Null Checks for All Parameters | vs | Single Quick Action that inserts a null-check guard clause for every nullable, unchecked parameter of a method in one pass, rather than one at a time. |
| Unused value/variable/parameter cleanup | both | Fades out unused parameters, flags unused expression values, and offers a Quick Action/quick-fix to delete redundant assignments or replace an unused out-value with a discard (`_`). Core to both engines' "unused code" inspection sets — ReSharper/Rider's inspection engine is generally considered the more exhaustive of the two here. |
| Sort/Organize Usings | both | Alphabetizes and can remove redundant `using` directives. Visual Studio: Edit > IntelliSense > Sort Usings (configurable in Tools > Options); Rider: "Optimize/Sort usings" plus a configurable silent code-cleanup profile that can run it automatically on save. |
| Use Tuple to Swap Values | vs | Rewrites the classic three-statement temp-variable swap (`var t=a; a=b; b=t;`) into a single tuple-deconstruction swap (`(a, b) = (b, a);`). |
| Simplify Property Pattern | vs | Flattens a nested property pattern like `is { Foo: { Bar: x } }` into C# 10's extended dotted form `is { Foo.Bar: x }`. |
| Prefer Null Check Over Type Check | vs | Suggests replacing an `is SomeType` check that's really only testing for null with the more idiomatic `is not null`. |
| Enable Nullable Reference Types (project quick action) | vs | One-click Quick Action that turns on `#nullable enable` for a file or the whole project (.NET Core 3.1+), surfacing the resulting nullability warnings immediately rather than requiring manual `.csproj`/pragma edits. |
| Generate From Usage / Generate Code from Usage | both | Reference a not-yet-declared class, method, property, field, local, or parameter and get a code-fix that generates the matching stub, inferring types from how it's used. Visual Studio: Ctrl+. on the squiggle offers Generate method/property/field stub, Generate class, Generate new type; Rider: Alt+Enter offers the same stub kinds, with generation behavior (throw `NotImplementedException`, return… |
| Generate Constructor | both | Generates a constructor by picking which fields/properties to initialize. Visual Studio's light bulb offers this with base-constructor-chaining support; Rider's Alt+Insert > Constructor dialog additionally offers an access-modifier picker and an option to auto-generate null checks for the initialized parameters, plus live templates (`ctor`, `ctorf`, `ctorp`) that expand directly in the editor… |
| Generate Equals()/GetHashCode() | both | Implements structural equality for a type from selected fields/properties. Visual Studio's Quick Action generates `Equals`/`GetHashCode` (and `IEquatable<T>` in recent versions); Rider's Alt+Insert > Equality Members dialog exposes explicit checkboxes for which members participate plus optional `==`/`!=` operator generation and `IEquatable<T>`. |
| Generate Properties from Fields (bulk, multi-select) | rider | Alt+Insert > Properties/Read-only Properties generates public accessor properties for several selected fields in a single dialog pass, with options to copy each field's XML doc comment and mark the generated properties virtual. |
| Generate Formatting Members (ToString()) | rider | Alt+Insert > "ToString() method" builds a formatted `ToString` override from chosen fields/properties, offering several output templates. |
| Generate Delegating Members | rider | Alt+Insert > Delegating Members: pick a field/property and which members of its type to re-expose through the containing class; Rider generates the forwarding (wrapper) methods automatically. |
| Implement/Override Members | both | Stub out interface members or override base-class members. Visual Studio: "Implement interface" / "Implement interface explicitly" / "Implement abstract class" Quick Actions, with Document/Project/Solution links in the preview window to fan generated stubs out across every implementer at once; Rider: Alt+Insert > Implement Members / Overriding Members dialog lets you multi-select exactly which… |
| Structural Search and Replace (SSR) | rider | Pattern-based, syntax-tree-aware find-and-replace using placeholder variables and wildcards, runnable across a file, project, or the whole solution, and savable as a reusable custom pattern (which can even be wired into ReSharper's own inspection/quick-fix system as a custom code smell). |
| Preview Changes window / conflict detection / cross-file undo | both | Before committing a multi-file refactoring, both IDEs show a diff-style preview: Visual Studio's Preview Changes window lists every affected line with a checkbox and Document/Project/Solution scoping, and detects naming conflicts (e.g. a rename colliding with an existing symbol) before applying; Rider's refactoring dialogs include an equivalent Preview toggle. Both apply the whole multi-file… |
| Wrap and Align formatting rules | both | Configurable code-formatting rules that automatically wrap and align long constructs — multi-line argument/parameter lists, chained method calls, binary/logical expressions — to column or per-item thresholds, applied via Format Document or on save. Visual Studio: Tools > Options > Text Editor > C# > Code Style > Formatting > Wrapping; Rider: Settings > Editor > Code Style > C# > Wrapping and… |
| Convert to Primary Constructor / Convert to Record (C# 12) | both | Quick action/refactoring that rewrites a class's conventional field-plus-constructor pattern into a C# 12 primary constructor, and a related conversion from a class to a `record`/`record struct`, introduced in Visual Studio 2022 17.8 and the corresponding Rider 2023.3 release alongside C# 12 support. |
| "Fix with Copilot" / "Implement with Copilot" (AI-assisted code actions) | vs | From the Quick Actions light bulb, "Fix with Copilot" opens inline GitHub Copilot Chat with a proposed fix for issues the deterministic Quick Action can't fully resolve; "Implement with Copilot" (added in a VS 2022 17.14-era update) turns a method stub that throws `NotImplementedException` into a Copilot-generated implementation. Related: "Adaptive Paste" restructures pasted code… |

---

## Code inspection, quick-fixes and quality tooling

| Feature | IDE | What it is |
| --- | --- | --- |
| 2,500+ built-in code inspections | rider | ReSharper/Rider ship over 2,500 C#/VB/XAML/ASP.NET/build-script inspections, far exceeding Roslyn's built-in analyzer set. They run live in the editor as you type, independent of any NuGet package being installed. |
| Inspection category taxonomy | rider | Inspections are grouped into categories such as Potential Code Quality Issues, Common Practices and Code Improvements, Redundancies in Code, Language Usage Opportunities, Constraints Violations, Code Notifications, Code Style, Compiler Warnings, and Spelling, each with its own default severity tier. |
| Five-tier inspection severity model | rider | Every inspection can be set to Error, Warning, Suggestion, Hint, or Do not show, editable per-inspection or per-category on the Inspection Severity options page, with a one-click reset to JetBrains defaults. |
| Settings layers for per-solution/per-file severity | rider | Inspection severity and every other option can be scoped to 'This computer', 'This solution (personal)', 'This solution (team-shared, .sln.DotSettings)', or a specific file, so different overrides can coexist without conflict. |
| Solution-Wide Error Analysis (SWEA) | rider | Rider continuously analyzes every project built in the current solution configuration in the background, surfacing a live count of files with errors/warnings via a colored circle in the status bar (green/orange/red) that opens the Problems tool window. |
| SWEA scope narrowing and pause controls | rider | Solution-wide analysis can be paused/resumed from the status-bar menu, restricted with include/exclude file masks and folders, filtered by target framework, and configured to suppress warning-monitoring so only errors count once all errors are fixed. |
| Live/background code analysis scope setting | vs | Visual Studio's Roslyn analyzers run design-time as you type; the analysis scope can be set to Current Document, Open Documents, or Entire Solution under live code analysis configuration, trading CPU cost for solution-wide coverage. |
| InspectCode command-line tool | rider | A free, cross-platform CLI (jb inspectcode) that runs the full ReSharper inspection engine against a solution or specific projects outside the IDE, emitting an XML/SARIF report; supports --include/--exclude file masks and works without a ReSharper/Rider license. |
| CleanupCode command-line tool | rider | Companion CLI to InspectCode that applies a named Code Cleanup profile across a solution from the command line, fixing style violations in bulk. |
| Code Cleanup profiles | rider | Three built-in profiles (Reformat Code; Reformat & Apply Syntax Style; Full Cleanup) plus unlimited custom profiles built by duplicating and editing a task list (reformat, remove redundant code, arrange usings, apply naming style, update file header, etc.), run via Ctrl+E,C or on save. |
| Code Cleanup (EditorConfig-driven fixers) | vs | Ctrl+K,Ctrl+E (or the broom icon at the bottom of the editor) runs a configurable, ordered list of Roslyn fixers — e.g. 'Apply file header preferences', 'Sort usings', 'Remove unnecessary usings', 'Apply naming rules' — against the current document or the whole solution, with the fixer list and order editable in Tools > Options. |
| Structural Search and Replace (SSR) | rider | A pattern language (with $placeholder$ tokens matching typed expressions, statements, or nodes) lets you find or rewrite arbitrarily complex code shapes across the whole solution via ReSharper \| Find \| Search with Pattern, independent of Roslyn's Find/Replace regex. |
| Custom patterns as persistent custom inspections | rider | Any SSR pattern can be saved to the Custom Patterns catalog with its own configurable severity (Error through Hint), turning it into a live, always-on inspection that highlights matches in the editor and ships in the shared .DotSettings file. |
| JetBrains.Annotations nullability/purity/contract attributes | rider | A small attribute library ([NotNull], [CanBeNull], [ItemNotNull]/[ItemCanBeNull], [Pure], [MustUseReturnValue], [ContractAnnotation] for input/output correlation, [StringFormatMethod], [InstantHandle], [MustDisposeResource]) that the inspection engine consumes to tighten null/purity/resource-leak analysis. |
| Control-flow value & nullability analysis | rider | Rider's own control-flow engine tracks possible null dereferences, redundant null checks, and always-true/false conditions; it runs in 'optimistic' or 'pessimistic' mode, and in C# 8+ projects it additionally reuses the compiler's NRT results rather than duplicating them. |
| Naming Style rule engine | rider | A dedicated settings page (per identifier kind: local, parameter, private/protected/public field, static field, property, interface, type parameter, etc.) configures capitalization, required prefixes/suffixes, and accessibility-specific variants; violations are flagged as the 'Inconsistent Naming' inspection with an auto-fix. |
| Solution-wide naming style compliance check | rider | A dedicated command batch-runs the naming inspection across every file in the solution and lists every violation in one results window, rather than requiring you to open each file. |
| .NET naming conventions via EditorConfig | vs | dotnet_naming_rule/_symbols/_style entries in .editorconfig define naming conventions (e.g. interfaces must start with 'I', private fields are _camelCase) enforced by the built-in IDE1006 analyzer with a code-fix. |
| Suppression comments | rider | // ReSharper disable once <InspectionId> suppresses one occurrence; // ReSharper disable <InspectionId> ... restore suppresses a whole block/file. ReSharper also highlights and offers a quick-fix to remove suppression comments that no longer match a live warning. |
| SuppressMessage attribute suppression | both | [SuppressMessage("ReSharper", "PossibleNullReferenceException")] suppresses a ReSharper inspection at method/class scope; the standard [SuppressMessage("Category","CAxxxx")] / #pragma warning disable CAxxxx suppresses Roslyn analyzer diagnostics the same way in VS. |
| EditorConfig-driven analyzer severity | vs | Any Roslyn analyzer's severity (CA and IDE rules alike) can be set per rule ID in .editorconfig (dotnet_diagnostic.CAxxxx.severity = warning\|error\|suggestion\|silent\|none), via the light-bulb 'Configure severity' quick action, or from the Analyzers node under Dependencies in Solution Explorer — .editorconfig wins over ruleset/Solution Explorer settings. |
| .NET code-style analyzers (IDExxxx) | vs | Built into Visual Studio/Roslyn itself (no package needed), these enforce stylistic preferences — var usage, expression-bodied members, pattern matching, using-directive placement — with severities configurable via .editorconfig and enforceable as build warnings/errors since .NET 5. |
| .NET code-quality analyzers (CAxxxx) | vs | Shipped with the .NET 5+ SDK and enabled by default, covering Design, Globalization, Interoperability, Maintainability, Naming, Performance, Reliability, Security, and Usage rule categories (e.g. CA1822 mark members static, CA2100 SQL-injection risk). |
| Third-party Roslyn analyzer support | both | Both IDEs run any analyzer distributed as a NuGet analyzer package (StyleCop.Analyzers for style/doc-comment/ordering rules, Roslynator for several hundred simplification/quality rules, SonarAnalyzer.CSharp, xunit.analyzers/Meziantou.Analyzer, etc.); VS additionally supports installing analyzers as a solution-wide VSIX extension. |
| Code metrics (Calculate Code Metrics) | vs | Analyze > Calculate Code Metrics (For Solution/For Project) computes cyclomatic complexity, maintainability index (color-coded green ≥20, yellow 10-19, red 0-9), depth of inheritance, class coupling, and lines of source/executable code per type/member, exportable and also runnable from the command line. |
| Error List with Build/IntelliSense/scope filters | vs | View > Error List (Ctrl+\,E) tabs errors/warnings/messages and lets you filter the source (Build, IntelliSense, or both) with a right-hand dropdown, plus a left-hand scope dropdown (Entire Solution, Open Documents, Current Project, Current Document). |
| Problems tool window | rider | Rider's equivalent results list (Alt+6) shows every SWEA-detected issue with filters by severity and target framework and an 'All Solution Files' tab, opened from the status bar indicator or View > Tool Windows > Problems. |
| Spelling inspection (ReSpeller) | rider | A built-in spellchecker inspection flags typos in identifiers, string literals, and comments across all supported languages; custom words can be added to a machine-local or project-shared .dic/Hunspell dictionary directly from the quick-fix menu, and the inspection can be run solution-wide via Run Inspection by Name. |
| Dead code / unused symbol detection | rider | Detects not just simple 'never referenced' cases but subtler dead code — a field that's only ever assigned, never read; a private method reachable only from other dead code — grouped under the Redundancies in Code category with an auto-fix to delete. |
| Unused value/parameter/unreachable-code fading | vs | VS fades out unreachable code, unused parameters, and dead value assignments directly in the editor with an accompanying warning and a Quick Action to remove the assignment or convert an unused variable to a discard. |
| Remove Unused References | vs | For SDK-style projects, right-click a project or its Dependencies node and choose Remove Unused References; VS analyzes actual source usage and opens a dialog with unused PackageReference/ProjectReference entries pre-selected for removal (toggle any to Keep). |
| Unused assembly reference removal | rider | Rider (porting a longstanding ReSharper capability, added to Rider in 2020.2) detects assembly/project references with no actual usage in code and offers a fix to remove them. |
| TODO Explorer | rider | A dedicated tool window (Alt+6 in Rider, Ctrl+Alt+D in ReSharper) lists every TODO-style comment across C#, VB, JS/TS, CSS, and Razor files; custom regex patterns beyond the default 'todo' keyword can be defined, and the list can be exported to text/HTML/XML for feeding into an issue tracker. |
| Task List | vs | View > Task List surfaces // TODO, // HACK, and // UNDONE comments (custom tokens configurable under Tools > Options > Environment > Task List) as a flat, double-click-to-navigate list scoped to the whole solution. |
| CodeVision inline metrics | rider | Inline hints above type/member declarations show usage count, inheritor count, and (in VCS-enabled projects) the latest contributor and edit history, fed by solution-wide analysis; clicking a metric jumps straight to the underlying references/inheritors list. |
| CodeLens code-health/history indicators | vs | Inline indicators above C#/VB members show reference count, linked unit tests with pass/fail status, and (Professional/Enterprise + Git or Azure DevOps) full change history, linked work items, linked bugs, and code reviews, togglable under Tools > Options > Text Editor > All Languages > CodeLens. |
| IL Viewer | rider | Tools \| IL Viewer (or Navigate To > IL Code) opens a synchronized panel showing the compiled IL for the method under the caret, with hoverable tooltips explaining each opcode, alongside the ability to view decompiled symbols at different C# sugar levels. |
| Roslyn Syntax Tree Visualizer (native) | rider | Added in Rider 2025.1 EAP, a built-in tool window renders the live Roslyn syntax tree of the active C# file, synchronized with the caret, supporting node expand/collapse, property inspection, and click-to-highlight-in-editor. |
| Roslyn Syntax Visualizer | vs | An official tool window (View > Other Windows > Roslyn Syntax Visualizer, requires installing the '.NET Compiler Platform SDK' optional workload component) that shows the syntax tree, syntax nodes/tokens/trivia, and semantic model (symbol/type info, constant values) for the active file. |
| Source generator debugging | vs | Selecting the source-generator project in the debug-target dropdown and setting a breakpoint inside the generator lets VS's debugger step into Generate() during a build; the generator project must be rebuilt/VS restarted after generator code changes to pick up the new version. |
| Source-generated file browsing | both | Both IDEs surface source-generator output as a virtual node in the solution tree (VS: Dependencies > Analyzers > [generator] under a project; Rider: an equivalent generated-files node) so generated .g.cs files can be opened, searched, and navigated like real files without touching disk. |
| Dependency (layer) diagrams with live validation | vs | Enterprise-only: organize solution items into abstract 'layers' on a diagram, draw allowed-dependency arrows between them, then right-click > Validate Architecture; live validation (C#/VB) reports violations directly in the Error List as you code, and validation can also run as part of a CI build. |
| Code Maps | vs | Enterprise-only: Architecture > Generate Code Map for Solution (or for a selection/CodeLens references) draws an interactive graph of the de facto dependencies and relationships among the code's actual components, distinct from the hand-authored layer diagrams. |
| Live Unit Testing | vs | Enterprise-only: runs affected unit tests continuously in the background as you type, showing inline pass/fail glyphs and per-line coverage indicators (covered/not-covered/partially-covered) directly in the editor gutter, without a separate 'run coverage' step. |
| Code Coverage analysis | vs | Enterprise-only: Test > Analyze Code Coverage runs the test suite and produces a coverage report (by assembly/namespace/class/member, with a color-coded editor overlay), exportable to standard coverage formats for CI dashboards. |
| dotCover coverage highlighting & Continuous Testing | rider | Rider bundles dotCover: Continuous Testing re-runs affected tests in the background on save and shows pass/fail status inline, and coverage runs highlight covered/uncovered/partially-covered lines in the editor gutter, comparable in spirit to VS Enterprise's Live Unit Testing but included at Rider's standard price tier. |
| Fix in scope (batch quick-fix) | rider | Many quick-fixes offer a 'Fix all occurrences in: this file / this project / this solution' option, applying the same automated fix to every matching instance of an inspection in one action, previewable in a diff view first. |
| Fix All Occurrences | vs | The light-bulb Quick Actions menu for a Roslyn diagnostic includes 'Fix all occurrences in: Document / Project / Solution', applying the code fix everywhere the diagnostic appears within the chosen scope with a preview changes dialog. |
| Qodana CI code-quality platform | rider | A separate JetBrains product built on the same inspection engines (including the C# one shared with Rider/ReSharper) that runs as a Docker-based CI step, uploads results to Qodana Cloud, and can enforce a quality gate (fail the build past a threshold) with baselines for pre-existing issues. |
| Contract annotations for flow-sensitive inspections | rider | [ContractAnnotation("value:null => false")]-style declarations tell the inspection engine how a method's return value correlates with its inputs (e.g. 'returns false only when the input is null'), which downstream call sites then use to eliminate false-positive null/unreachability warnings. |

---

## Debugging

| Feature | IDE | What it is |
| --- | --- | --- |
| Line breakpoints with rich visual states | both | Click the left gutter or press F9/Ctrl+F8 to toggle a breakpoint on executable code. Rider shows distinct icons for enabled/disabled/conditional/dependent/tracepoint/invalid/muted states directly in the gutter. |
| Conditional breakpoints (Is true / When changed) | both | Attach a boolic expression to a breakpoint so it only fires when true, or (VS only) fires when the expression's value changes since the last check. Rider: right-click line > Add Conditional Breakpoint; VS: right-click breakpoint > Conditions > Conditional Expression. |
| Hit-count breakpoints | both | Break only after N hits, or on every Nth hit. VS supports equal-to / multiple-of / greater-than-or-equal comparisons in the Breakpoint Settings dialog; Rider exposes the same via the breakpoint's Hit Count property. |
| Filter breakpoints (machine/process/thread expressions) | vs | VS breakpoint Conditions > Filter accepts boolean expressions like MachineName="x" & ProcessId=1234 \| ThreadName="worker" combined with &, \|\|, !. Rider has per-breakpoint thread-suspend targeting but not the same combinable filter DSL. |
| Dependent (chained) breakpoints | both | A breakpoint stays inactive until a designated prerequisite breakpoint is hit first. VS: hover breakpoint > Settings > "Only enable when the following breakpoint is hit", or right-click margin > Insert Dependent Breakpoint. Rider: breakpoint's More settings. |
| Function/method breakpoints | both | Break on entry to a named function without knowing its source location. VS: Debug > New Breakpoint > Function Breakpoint, supports fully-qualified names, overload disambiguation by parameter types, module qualifier (App.dll!Method), and even a memory-address-bound instance method in native C++. Rider: Breakpoints dialog, specify type + method name. |
| Data breakpoints (break when a value changes) | both | For .NET Core 3+/.NET 5+, right-click a property/field in Locals/Autos/Watch and choose Break When Value Changes (VS) or Set Data Breakpoint (Rider). Doesn't work on static fields, DebuggerTypeProxy-decorated classes, struct fields, or non-expandable values. |
| Tracepoints / logpoints | both | Convert any breakpoint into a non-suspending log statement. VS configures a Print message action with expression interpolation logged to the Output window; Rider clears the breakpoint's "Suspend" checkbox and logs to Debug Output, and can turn line, method, or exception breakpoints into tracepoints. |
| Exception breakpoints with granular settings | both | VS's Exception Settings window (Debug > Windows > Exception Settings) lets you check individual exception types or whole categories (CLR, Win32, JS...), search/filter by namespace, and toggle "Continue When Unhandled in User Code" per type. Rider sets exception breakpoints (global, not line-bound) from the Breakpoints dialog. |
| Exception conditions scoped by module | vs | In Exception Settings, Edit Conditions lets you restrict a checked exception to break only when thrown from (or excluded from) specific modules, using wildcards and Equals/Not Equals, combinable with AND. |
| Breakpoint grouping and labels | both | Rider: select breakpoints in the Breakpoints dialog (Ctrl+Shift+F8) and "Move to group \| Create new"; also supports free-text descriptions per breakpoint. VS: right-click a breakpoint > Edit labels, then use "Group Breakpoints By > Label" in the Breakpoints window. |
| Breakpoint import/export | vs | The Breakpoints window can export selected (or all) breakpoints to an XML file and re-import them later or on another machine. Rider has no equivalent export command; breakpoints only persist with the local solution/workspace. |
| Temporary (one-shot) breakpoints | both | A breakpoint that removes/disables itself immediately after firing once. VS: breakpoint Settings > "Remove breakpoint once hit", or Shift+Alt+F9,T. Rider: Ctrl+Alt+Shift+F8. |
| Thread-specific breakpoints | both | Restrict a breakpoint to suspend only when hit on a designated thread, configured from the breakpoint's settings in either IDE. |
| Force Run to Cursor / Force run to click | both | VS 2022: right-click a line > "Force Run To Cursor", or Shift+hover for the double green arrow "Force run execution to here" — skips all breakpoints and first-chance exceptions on the way. Rider's equivalent is "Run to Cursor Non-Stop" (Ctrl+Alt+F9). |
| Run to Cursor / Run to click | both | Resume execution and stop at the selected line, equivalent to a temporary breakpoint, without leaving one behind. VS: Ctrl+F10 or hover the green arrow; Rider: Alt+F9. |
| Step Into / Step Over / Step Out | both | Standard statement-level stepping (F11/F10/Shift+F11 in VS; F7/F8/Shift+F8 in Rider). Both step by logical statement, not physical source line — an inline `if(x==42) s="y"` is two steps. |
| Force Step Over (ignore inner breakpoints) | rider | Alt+Shift+F8 steps over the current call while deliberately ignoring any breakpoints set inside the called method(s). No documented VS equivalent — standard Step Over in VS still respects breakpoints hit inside the stepped-over call. |
| Smart Step Into | both | When a line chains multiple calls (e.g. `Foo(Bar(Baz()))`), Smart Step Into lets you pick exactly which call to enter instead of always diving into the innermost one. Rider: Shift+F7, with a "Always do smart step into" setting. VS offers the equivalent via the Step Into Specific popup on chained calls. |
| Step over properties and operators (managed code) | both | VS: Debug > Options > Debugging > General > "Step over properties and operators (Managed only)", enabled by default so F11 doesn't dive into every auto-property getter/operator overload. Rider exposes an equivalent stepping filter in its Debugger settings. |
| Set Next Statement (move execution pointer) | both | Drag the yellow execution-pointer arrow (or Ctrl+Shift+F10 in VS) to re-run or skip statements within the current stack frame without restarting the app. Rider offers the same via "Skip to Cursor" (Ctrl+Alt+Shift+F9) or by dragging the pointer. |
| DataTips: pin, float, comment, import/export | vs | Hovering a variable shows an inline DataTip; it can be pinned to a source line (persists across debug sessions, draggable), floated anywhere in the IDE, annotated with multiline comments, made transparent, and the whole set exported/imported as XML. Rider's nearest analog is persistent Inline Watches, without pinned comments or import/export. |
| Autos and Locals windows | both | Locals shows variables in the current scope; Autos shows variables used on the current/preceding lines (VS) with expandable trees, hex-display toggle, search with "search deeper" nesting control, and Pinnable Properties to bubble a nested property to the top. Rider's Threads & Variables pane covers the same ground in one unified view. |
| Watch window with format specifiers | both | VS Watch/Autos/Locals accept a mini-language of format specifiers appended to an expression, e.g. `,nq` (no quotes), `,d`/`,x` (decimal/hex), `,ac` (ANSI), applied per-entry. Rider's Watch window evaluates persistent expressions saved with the solution but doesn't offer the same comma-specifier syntax, relying on a "View As" context menu instead. |
| Immediate window | vs | A REPL-style window (Ctrl+Alt+I) that executes arbitrary statements and expressions against the paused process (`? varA`, calling functions, even at design time for some project types), with command history and IntelliSense. Rider has no direct equivalent — its Evaluate Expression dialog (Alt+F8) evaluates one expression at a time without persistent REPL history. |
| DebuggerDisplay / DebuggerTypeProxy attribute support | both | Both debuggers honor `[DebuggerDisplay("{Expr}")]` (with format specifiers like `,nq`) to customize an object's summary text, and `[DebuggerTypeProxy]` to substitute a proxy type's members in the variable tree, in Watch/Locals/DataTips alike. |
| Just My Code / non-user-code hiding | both | Steps over and collapses framework/library frames into `[External Code]` in the Call Stack. VS classifies via loaded PDBs, optimization state, and the DebuggerNonUserCode/DebuggerHidden/DebuggerStepThrough attributes, plus C++-only .natjmc/.natstepfilter customization files; Rider offers a simpler on/off toggle plus the same three attributes. |
| Object IDs / Make Object ID | vs | In the Locals window, right-click a reference-typed instance and choose Make Object ID to get a weak-referenced `$1`-style handle usable in later conditional-breakpoint expressions (e.g. `item == $1`) across the same debug session. No confirmed Rider equivalent. |
| Evaluate on hover with method-call evaluation | both | Hovering a variable or expression shows its live value, including invoking properties/methods (subject to "Enable property evaluation" in VS). Rider's editor tooltips do the same, expanding child members on click. |
| LINQ query debugging | both | VS 2022 17.14+ shows per-clause DataTips when hovering segments of a LINQ query, with a Copilot "explain this query" button. Rider shows a magnifying-glass "View" on IEnumerable/query results to inspect the materialized sequence in a tabular popup. |
| Inline values / value origin tracking | both | Rider auto-adds a stepped-over call's return value as an inline watch in the editor and can trace where a given value came from ("value origin"). VS shows inline return values next to the call and inline evaluation results for if/loop conditions and parameters (17.12+). |
| Parallel Stacks window (Threads / Tasks / Method views) | both | VS's diagram shows all threads' call stacks with a Threads view, an async/await-aware Tasks view, and a Method view pivoted on a chosen method, plus (17.6/17.7+) Copilot-generated thread descriptions. Rider's Parallel Stacks tab renders a similar thread-stack diagram but without the dedicated Tasks pivot. |
| Parallel Watch window | vs | Evaluates one watch expression across every thread simultaneously in a grid, one row per thread. No dedicated Rider equivalent. |
| Async call stack reconstruction | both | Both debuggers stitch an async method's awaited continuations back into one logical call stack instead of showing raw state-machine/thread-pool frames. |
| Map methods on the call stack (code map) | vs | Visual Studio Enterprise-only: generates a live, navigable diagram of the current call stack as a code map, updated as you step. Not available in Professional/Community and has no Rider equivalent. |
| GC roots / retention-path analysis from the live debugger | both | Rider: right-click a variable during a break and Analyze GC Roots to see the retention graph (stack/static/handle roots) keeping it alive, or Alt+Enter on a type to "Show Instances". VS's closest equivalent is the separate Diagnostic Tools > Memory Usage tool, which compares two heap snapshots and shows Paths to Root — a profiling-style workflow rather than a right-click-a-variable one. |
| Memory window (raw address-level bytes) | vs | Up to four independent Memory windows show raw bytes at a given address/expression, alongside the Disassembly and Registers windows, with configurable column width and per-byte-size display. Requires address-level debugging enabled. |
| Managed heap / instance browser (Memory View) | rider | Rider's Memory View tab (opened from the Debug window at a breakpoint) browses live managed-heap instances grouped by type, and "Show <Type> Instances" can be launched directly from the editor via Alt+Enter. This is an object-instance browser rather than VS's raw-bytes memory window. |
| Modules window | both | Lists loaded modules with symbol-load status, and (VS) flags which modules Just My Code treats as user code; right-click a module to force-load its symbols mid-session. |
| Disassembly window | both | Shows the JIT-compiled native instructions behind the current managed statement (not raw MSIL), with optional address offsets, source interleaving, code bytes, and symbol names. VS: Ctrl+Alt+D, requires "Enable address-level debugging". Rider surfaces disassembly primarily in its native/core-dump debugging flow. |
| IL (MSIL) presentation of compiled code | rider | Rider's assembly explorer/decompiler lets you switch a compiled type's presentation between decompiled C#, IL, and metadata-only views (Tools > External Symbols settings). VS has no built-in raw-IL viewer in the debugger UI. |
| Registers window | vs | Shows CPU/CLR register contents alongside the Disassembly window, letting you check or edit individual register values while stepping through native instructions. Available primarily for address-level/native debugging. |
| Call Stack with collapsible external code + auto-decompile | both | Non-user frames collapse into a grayed `[External Code]` entry; VS 2022 17.7+ lets you double-click it to auto-decompile the .NET assembly on the spot (no manual symbol setup). Rider decompiles automatically the moment you step into external code. |
| Attach to Process — local | both | Attach the debugger to an already-running local process (VS: Ctrl+Alt+P; Rider: Ctrl+Alt+F5), useful when the app wasn't launched under the debugger, with process-name filtering in both. |
| Attach to Process — remote (Windows remote tools / SSH) | both | VS requires installing the version-matched "Remote Tools for Visual Studio" (msvsmon.exe) on the target Windows machine, or uses SSH for .NET Core on Linux. Rider connects over SSH out of the box for its supported remote scenarios, without a separate remote-tools package. |
| Attach to Process — Docker container | both | VS has long-standing, dedicated documentation for attaching to processes in both Linux and Windows containers. Rider (since 2023.3) auto-installs remote debugger tooling into the container via Attach to Process, but is documented as primarily covering ASP.NET Core apps in Linux containers with narrower coverage than VS. |
| Attach to Process — IIS (w3wp.exe), local and remote | both | Both can attach to the IIS worker process; VS has guided, dedicated walkthroughs for remote IIS debugging (including Session 0 / vsjitdebugger.exe workarounds for locked-down Remote Desktop scenarios). |
| Reattach to Process | both | Quickly reconnect to the last-attached process(es) without reopening the picker. VS: Shift+Alt+P, matches by PID then by name. Rider tracks recent processes for fast reattachment. |
| Auto-attach to child/grandchild processes | rider | Rider can automatically attach the debugger to child and grandchild processes spawned by the process it's already debugging. Stock VS has no built-in equivalent for arbitrary apps — the comparable behavior requires Microsoft's separate "Child Process Debugging Power Tool" extension. |
| Attach to multiple processes; switch active via Debug Location toolbar | vs | VS can be attached to several processes at once, with only one "active" for stepping/inspection at a time, switchable from the Debug Location toolbar or Processes window. |
| Managed dump file analysis ("Debug Managed Memory") | vs | Opens a full or minidump and lets you inspect Locals, Call Stack, and Threads exactly as in a live session (statically — no execution). JetBrains itself documents Rider's dump support as more limited than VS's, mainly geared at native/core dumps via a "Native Core Dump Debug" run configuration rather than full managed-dump analysis. |
| IntelliTrace / historical debugging | vs | Visual Studio Enterprise-only. Records events and (optionally) full snapshots at every breakpoint/step, then lets you "Activate Historical Debugging" to inspect call stack and locals at any past recorded point without rerunning or re-hitting breakpoints. Not available in Professional/Community; no Rider equivalent. |
| Time Travel Debugging (TTD) | vs | Visual Studio Enterprise-only. Records a full instruction-level trace of a process and replays it both forward and backward, including live Azure VM recording. Adds significant runtime overhead while recording. No Rider equivalent. |
| Snapshot Debugger (Application Insights) | vs | An Azure Monitor/Application Insights capability, inspected from Visual Studio: automatically captures a minidump snapshot the first time (by default, second occurrence) a top-throwing exception happens in a live production app, with rate limits (1/10min, 50/day, 15-day retention), then lets you debug the captured snapshot in VS. No Rider equivalent. |
| Edit and Continue vs. Hot Reload | both | Edit and Continue (legacy, break-mode-only, also available for native C++ with /INCREMENTAL) applies code edits while paused at a breakpoint. Hot Reload (.NET 6+) applies edits while the app is running, with no break required, and can auto-restart the process on an unsupported "rude" edit instead of ending the session. Rider's equivalent unifies both under "Hot Reload", applying in both paused… |
| Decompile-and-debug external/third-party code | both | Rider automatically decompiles any library code you step into (or navigate to) to readable C#, and breakpoints can be set directly in the decompiled view. VS 2022 17.7+ added the same on-demand auto-decompilation from the Call Stack / step-into flow, previously requiring manual symbol setup. |
| Source Link support | both | Both fetch the exact source revision matching a built binary's embedded commit hash from GitHub/Azure DevOps/etc. instead of decompiling, when the assembly was built with Source Link enabled. |
| Symbol server and symbol cache configuration | both | Both let you configure remote symbol servers (Microsoft Symbol Server, NuGet.org Symbol Server, private/custom URLs), a local symbol cache directory, and proxy settings for fetching PDBs on demand. VS: Tools > Options > Debugging > Symbols. Rider: Settings > Build, Execution, Deployment > Debugger > External Symbols. |
| Debugging unit tests from the test runner | both | Set breakpoints in test methods and launch a debug session directly from the test list: VS's Test Explorer (right-click > Debug) or Rider's Unit Tests window, both stepping seamlessly between test code and the code under test. |
| Test profiling from Test Explorer | vs | Right-click a test and choose Profile to run it under the Performance Profiler (any tool, not just Instrumentation, since VS 2022 17.8), isolating a single unit of work for CPU/memory analysis and re-measuring after changes. |
| Unity / Mono debugging | both | Rider: attach to the Unity Editor or a remote/Android player over UDP/ADB, with Unity-specific extras — Pausepoints (pause the Editor at frame end without suspending code, for Inspector browsing), tracepoints to the Unity Console, GameObject/component/ECS-entity inspection, and texture preview at a breakpoint. VS gets equivalent attach/debug support through the separately-installed "Visual… |

---

## Unit testing and coverage

| Feature | IDE | What it is |
| --- | --- | --- |
| Unit Tests window (central test hub) | rider | A dedicated 'Unit Tests' tool window (Alt+Shift+8) lists every discovered test across the whole solution regardless of framework, with a tree grouped by project/namespace/class, live pass/fail icons, and a details pane for output. |
| Test Explorer (core window) | vs | Test > Test Explorer (Ctrl+E,T) lists tests from any framework that ships a Test Explorer adapter (MSTest, xUnit, NUnit built in; others via NuGet adapter packages), with grouping, filtering, playlists, and a details pane. |
| Broad framework/runner coverage | rider | Beyond NUnit/xUnit/MSTest (V1 Windows-only and V2 cross-platform), Rider discovers and runs TUnit and other Microsoft.Testing.Platform-based runners, bUnit (Blazor components), Unity's test framework, and native C++ frameworks (Google Test, Catch, doctest, Boost.Test 1.59+). |
| Unit Test Sessions | rider | Tests run into named, tabbed 'sessions' in the Unit Tests window (Ctrl+;,N to create, Ctrl+;,A to append). Sessions can mix tests from different frameworks/projects, be locked against auto-additions, renamed, and exported/imported as .testsession files. Sessions persist per-solution automatically and can be wired to run after a Git/Mercurial commit. |
| Custom Group By hierarchy | vs | Test Explorer's Group By button reorders/stacks grouping levels: Project, Namespace, Class, Duration (Fast/Medium/Slow buckets), State (Failed/Skipped/Passed/Not Run), Target Framework, or any custom Trait. Multiple levels can be combined, e.g. State then Class. |
| Test grouping by project/namespace/category | rider | The Unit Tests window can regroup its tree by project, namespace, or category on the fly via a toolbar toggle, and supports quick-search filtering by substring/CamelHumps. |
| Test Explorer columns (Traits, Stack Trace, Error Message, FQN) | vs | Optional columns can be added to the Test Explorer list view — Traits, Stack Trace, Error Message, Fully Qualified Name, Duration — each independently filterable (e.g. filter icon on the Traits column header) and sortable, with Shift-click for secondary sort. |
| Qualifier-based search filters | vs | The Test Explorer search box accepts qualifiers — State:, Traits:, FullyQualifiedName:, Project:, TargetFramework:, Namespace:, Class: — combined with quoted values, exact-match `=`, and subtraction syntax like `FullName:"MyClass" -FullName:"PerfTest"`. |
| Playlists (.playlist files) | vs | Arbitrary sets of tests can be saved to a named, shareable .playlist XML file via Add to Playlist. Playlists can be static test lists or 'dynamic' (checkbox on a parent group like a class, so new tests added to that class auto-join the playlist), and can encode trait-based rules. |
| Run Selected Tests Until Failure | rider | Tests \| Run Selected Tests Until Failure (or the session context menu) repeatedly reruns the chosen tests in a loop until one fails or the user stops it manually. |
| Repeat last run / rerun failed tests | both | VS: Ctrl+R,L repeats the last run (TestExplorer.RepeatLastRun); a Rerun Failed Tests option targets only failures. Rider: Ctrl+;,F reruns only the failed tests in a session. |
| dotCover Continuous Testing | rider | A single, dedicated Continuous Testing session watches edits, recomputes which tests are 'affected' by the changed code (via coverage data), and automatically rebuilds + reruns only those tests in the background. A status-bar icon shows current state (including a '?' badge on affected-but-not-yet-rerun tests) with quick actions to toggle it, jump to coverage, or view failures. Trigger… |
| Live Unit Testing (LUT) | vs | Test > Live Unit Testing > Start builds an instrumented shadow workspace and continuously runs impacted tests as you type, painting inline glyphs in the left margin: green check (covered, passing), red x (covered, failing test present), blue dash (not covered). Hovering a glyph lists the covering tests and lets you run/debug just those from the tooltip; a round 'updating' badge shows stale… |
| LUT setup wizard & workspace controls | vs | First run of LUT opens a wizard to set the repository root, a separate workspace root (a full clone where instrumented builds happen), and a .lutignore file (gitignore-style) controlling what's copied. Build options include 'Generate PDBs' (off by default, needed for stack-trace navigation) and multi-core builds; run options include a per-test-case timeout and 'use multiple processors' for… |
| LUT include/exclude playlist | vs | LUT scope is controlled by a playlist editable from the LUT tool window, the code editor's right-click menu (Include/Exclude/Exclude All But <method>), Solution Explorer (per-project include/exclude), or programmatically via `[ExcludeFromCodeCoverage]` or framework-specific `SkipWhenLiveUnitTesting` category/trait attributes. |
| LUT vs explicit run differences | vs | Per Microsoft's own docs, LUT runs instrumented binaries from the default AppDomain sequentially per test assembly, whereas an explicit Test Explorer run uses a fresh AppDomain and can run assemblies in parallel — so LUT and a manual run can behave slightly differently for AppDomain- or parallelism-sensitive tests. |
| Parallel test execution toggle | vs | A settings-menu toggle on the Test Explorer toolbar turns parallel execution on/off (equivalent to MaxCpuCount=0 vs 1); finer control (exact core count, per-DLL isolation) comes from a `.runsettings` file's `MaxCpuCount`/`RunConfiguration` section auto-detected at the solution root or selected via Test > Select Settings File. |
| Test Runner settings / .runsettings support | both | VS auto-detects a `.runsettings` at the solution root or lets you pick one explicitly; settings there (parallelism, target platform, data collectors, timeouts) apply to all runs while selected. Rider's Tools \| Unit Testing \| Test Runner options page exposes the equivalent per-framework settings (default target framework, architecture, session-level overrides) and can point at a… |
| Test case timeouts | both | VS Live Unit Testing has an explicit 'Test case timeout' option in its setup wizard that auto-cancels a hung test. Outside LUT, both IDEs honor framework-level timeout attributes (MSTest `[Timeout]`, NUnit `[Timeout]`, xUnit context timeouts) surfaced through Test Explorer / Rider's Unit Tests window as a failed/timed-out result. |
| Gutter run/debug icons with live status | rider | Clicking the gutter icon next to a test method or class opens a Run/Debug/Cover/Profile/'Select in Test Tree' menu. After a run, the gutter icon itself turns into a pass/fail status glyph, and 'Select in Test Tree' jumps straight to that test's node in the Unit Tests window. |
| Navigate between tests and tested code | rider | Ctrl+T,G from a class or member jumps to its related test(s) and back, using naming-convention heuristics (configurable) to link production code to its test class. |
| Test details pane with stack trace + Open Test | vs | Selecting a test in Test Explorer shows source file/line, elapsed time, and on failure the framework's failure message plus full stack trace. Right-click > Open Test (or F12) jumps to the test method's source; double-clicking a stack frame navigates to that line. |
| Parameterized/data-driven test rows | both | Both IDEs expand a parameterized test (xUnit `[Theory]`/`[InlineData]`, NUnit `[TestCase]`, MSTest `[DataTestMethod]`/`[DataRow]`) into individually named, independently runnable/debuggable child rows showing the actual argument values in the name. Rider's discovery of dynamically-generated parameters (data sourced at runtime) can require switching Test Runner to 'Test Runner discovery' mode… |
| Traits/categories and predefined attributes | vs | MSTest exposes `[TestCategory]`, `[Owner]`, `[Priority]`, and generic `[TestProperty(name,value)]` as first-class traits that Test Explorer can group/filter/sort by; other frameworks map their own category attributes (NUnit `[Category]`, xUnit `[Trait]`) the same way. |
| Built-in Code Coverage (Enterprise, 2022) | vs | Test > Analyze Code Coverage for (Selected/All) Tests runs an instrumented pass and opens the Code Coverage Results window with hit percentages by line, function, class, namespace, and module; 'Show Code Coverage Coloring' highlights covered code (light blue by default) directly in the editor. |
| Code coverage no longer Enterprise-only (VS 2026) | vs | As of Visual Studio 2026 (GA Nov 2025), the built-in code-coverage tool from the item above is available in Community and Professional as well as Enterprise — previously an Enterprise-exclusive differentiator. |
| Fine Code Coverage (free VS extension) | vs | A free, open-source extension (works on Community/Professional too) that runs coverage automatically after test runs using one of three engines (Coverlet, OpenCover, or Microsoft's own free coverage engine via the `RunMsCodeCoverage` option), paints editor lines green/amber/red (covered / partially covered / covered-by-failing-test), adds a dedicated coverage tool window with per-class progress… |
| dotCover editor coverage highlighting | rider | After running tests with coverage, statements are marked in the gutter: red (not covered), green (covered, all covering tests passing), yellow (covered but at least one covering test failing). Markers fade/gray out once the underlying code changes to signal the data is stale. A coverage 'shield' icon in the top-right of the editor shows quick stats, jumps between uncovered regions, and offers… |
| Coverage Tree window with search & navigation | rider | The Unit Test Coverage window presents a searchable (CamelHumps/wildcard) tree of covered code items; 'Navigate from Editor' jumps the tree to the symbol under the caret and back; per-node context menu can exclude a node from the current snapshot (with an option to persist it as a permanent filter), instantly recalculating stats. |
| dotCover Hot Spots view | rider | A separate visualization ranks methods by a risk score combining high cyclomatic complexity with low test coverage, rendering riskier types/methods in proportionally larger font in a treemap-like view, accessible via a 'Hot Spots' toggle in the Coverage Results Browser / Unit Test Coverage window. |
| dotCover coverage filters | rider | Global or solution-specific filters can exclude/include by project, namespace, type, or member name pattern, plus attribute-based filters (include/exclude code marked with a given attribute, e.g. auto-generated code or `[ExcludeFromCodeCoverage]`-style markers). |
| Coverage report export (HTML/XML/JSON/NDepend) | rider | Coverage snapshots can be exported to HTML (shareable static report), plain XML, JSON, or an NDepend-compatible XML format for downstream tooling. |
| Continuous Testing + coverage combined | rider | When Continuous Testing is active, coverage highlighting keeps updating from the same background runs, but uses deliberately paler colors when the data is based on a fast/optimized rebuild scope, to visually flag 'possibly outdated' coverage versus a full recalculation. |
| Profile Unit Tests | rider | Tests can be run directly under a performance profiler (sampling, tracing, or line-by-line/timeline mode depending on what's installed) straight from the gutter or Unit Tests window context menu, producing a snapshot viewable in the same profiler UI used for regular app profiling; a companion 'dotMemory Unit' integration lets tests assert on memory traffic/allocations. |
| 'Profile with Copilot' test command | vs | New in Visual Studio 2026 v18.4, Test Explorer gains a 'Profile with Copilot' command that profiles a specific test with one click and (per the release notes) uses Copilot to help interpret/act on the results. |
| Create Unit Test wizard | rider | Ctrl+T,C (or Generate \| Unit Test, Alt+Insert) opens a dialog to scaffold a test for the class under the caret: pick or create the destination test project, pick the framework (auto-detecting versions already referenced), configure the generated test class/method naming convention, and choose the destination namespace/folder. |
| AI Assistant test-body generation | rider | With JetBrains AI Assistant enabled, the Create Unit Test flow can generate actual test method bodies/assertions inferred from the target class's logic, not just an empty stub; generated code may need Alt+Enter to resolve missing imports. |
| GitHub Copilot testing for .NET | vs | GA in VS 2026 v18.3 (requires a paid Copilot subscription). Right-click a method > Copilot Actions > Generate Tests (or a Copilot Chat icebreaker) generates a new, separate test project's worth of tests in whatever framework the solution already uses (MSTest/NUnit/xUnit; defaults to MSTest if none exists). Generation is described as deterministic and grounded in the C# compiler/language… |
| IntelliTest (legacy, .NET Framework) | vs | An older VS feature (descended from 'Pex') that symbolically explores a method's code paths to auto-generate input-driven test cases; effectively limited to .NET Framework projects and largely superseded in practice by GitHub Copilot Testing for .NET. |
| Live template test stubs | rider | Outside the AI-assisted path, Rider/ReSharper ship framework-specific live templates that scaffold a test method skeleton (attributes, arrange/act/assert shape) which the developer completes by typing a method name and tabbing through placeholders. |
| Debug unit tests with breakpoints and Diagnostic Tools | vs | Debug Selected Tests (or context-menu Debug) launches the normal VS debugger against the test host: breakpoints, stepping, and the live Diagnostic Tools window (CPU/memory 'events' timeline) all work exactly as in a regular debug session; LUT-triggered debugging additionally breaks automatically on an unexpected `Assert` result. |
| Single-session debug limitation | rider | Rider allows many simultaneous run sessions but only one test session can be under the debugger at a time. |
| Run Tests After Build | vs | A Test Explorer settings-menu toggle automatically kicks off the relevant tests every time the affected project(s) finish a local build, without needing Live Unit Testing. |
| Audio cues for test run outcome | vs | Since VS 2019 16.9, Test Explorer can play a distinct system sound for 'test run succeeded' vs 'test run failed', configurable through the standard Windows 11 Sound control panel (Microsoft Visual Studio sound category). |
| Flaky test detection tooling | vs | Microsoft has an in-progress/preview 'flaky test detection' capability discussed on the VS Developer Community (tracking issues from 2022 onward about a dedicated flaky-test list/tool), separate from Azure DevOps' pipeline-level 'rerun failed tests' feature which flags tests that fail then pass on retry as flaky in build results. |
| Run-until-failure as flaky-repro workflow | rider | In the absence of automatic flaky-test statistics, Rider's Run Selected Tests Until Failure is JetBrains' documented recommended technique for reproducing and diagnosing sporadic/flaky failures locally. |
| TeamCity build/test status integration | rider | Rider's Services tool window and Git/VCS integration surface TeamCity build and test results (including per-test outcome) tied to commits and branches, and can trigger remote runs from the IDE. |
| Azure DevOps parallel VSTest tasks | vs | Azure Pipelines' VSTest task can fan out across multiple agents/jobs for parallelism, honoring the same `.runsettings`/MaxCpuCount concepts used locally in VS, and reports rerun-of-failed-tests results back as build annotations. |
| Sub-test debugging from LUT tooltip | vs | Hovering a Live Unit Testing glyph shows the list of tests covering that line; selecting one or more of them from the tooltip lets you Run or Debug just those specific tests without leaving the code editor or reopening Test Explorer. |

---

## Profiling, performance and memory diagnostics

| Feature | IDE | What it is |
| --- | --- | --- |
| Diagnostic Tools window (debug-integrated) | vs | Opens automatically on F5 (or Debug > Windows > Show Diagnostic Tools, Ctrl+Alt+F2). Shows live CPU Usage / Memory Usage / .NET Counters graphs plus an Events list, all synced to breakpoints and step operations. |
| CPU Usage tool (debugger-integrated + post-mortem) | vs | Debug > Windows > Diagnostic Tools > 'Record CPU Profile', or standalone via Debug > Performance Profiler (Alt+F2). Shows Top Functions, a Hot Path call stack, and a Call Tree with Total CPU / Self CPU columns. Can also open traces collected externally by dotnet-trace. |
| Memory Usage tool with snapshot diffing | vs | 'Take Snapshot' in Diagnostic Tools/Performance Profiler captures the managed+native heap. Selecting the Objects (Diff) or Heap Size (Diff) link between two snapshots opens a differential report sorted by growth, plus Paths to Root and Referenced Types trees. |
| .NET Object Allocation tool | vs | Debug > Performance Profiler (Alt+F2) > '.NET Object Allocation Tracking' (post-mortem only). Has Allocation, Call Tree, Functions and Collection (per-GC: generation, reason, pause duration, LOH/POH size, survived/collected counts) views, with an adjustable sampling rate. |
| Memory Usage Insights (auto-detected) | vs | An Insights tab in the Memory Usage / managed-types report auto-flags Duplicate Strings (with total wasted bytes), Sparse Arrays, and Event Handler Leaks (added 17.9). |
| LOH allocation performance insight | vs | When frequent Gen2 GCs are detected, the .NET Object Allocation tool shows an 'Investigate' link that jumps to the Allocation view filtered to Large Object Heap allocations. |
| Instrumentation profiling | vs | Performance Profiler (Alt+F2) > Instrumentation. Injects probes for exact call counts and wall-clock time, unlike CPU Usage's statistical sampling. |
| .NET Async tool | vs | Performance Profiler > '.NET Async'. Lists every async Task/activity chronologically with start time, end time, duration; flags unfinished work as 'Incomplete'; right-click > Go To Source File. |
| Database tool | vs | Performance Profiler > Database (.NET Core/5+, ADO.NET or EF Core). Records every query with start time and duration in a sortable list; Go To Source File per row; can sync to a selected CPU Usage time range. |
| File I/O tool | vs | Performance Profiler > File I/O. Tabbed list view of individual file read/write operations and their timing. |
| .NET Counters tool | vs | Performance Profiler > .NET Counters (16.7+, .NET Core/5+). Live list view of EventCounters/Meter data such as CPU% and GC heap size, equivalent to the dotnet-counters CLI but inside the profiler UI. |
| GPU Usage tool | vs | Performance Profiler > GPU Usage (C++/Direct3D apps). CPU/GPU activity timeline with Present-call popups plotted against VSync lines to tell CPU-bound from GPU-bound frames. |
| Events Viewer (generic ETW) | vs | Performance Profiler > Events Viewer. Generic ETW event list (module load, thread start, system config, etc.) with Provider/Event filters showing % of total events, an Additional Properties pane, and Stacks; you can register your own custom ETW provider via the gear icon to see app-specific events. |
| Application Timeline tool (XAML) | vs | Performance Profiler > Application Timeline (WPF/UWP). Visual Throughput and UI Thread Utilization graphs; a Timeline details pane breaks a selected range down into Layout/Render/network/disk activity per UI element (e.g. a Grid's Layout event taking 57.53ms). |
| UI Analysis (UWP) | vs | Enable the 'UI Analysis' checkbox in Diagnostic Tools while debugging a UWP app; scans for common performance and accessibility issues and lists them as Events with remediation text. |
| BenchmarkDotNet integration | vs | VS 2022 17.9+: add the Microsoft.VisualStudio.DiagnosticsHub.BenchmarkDotNetDiagnosers NuGet package and diagnoser attributes (CPUUsageDiagnoser, DatabaseDiagnoser, DotNetCountersDiagnoser, EventsDiagnoser, FileIODiagnoser, or BenchmarkDotNet's own [MemoryDiagnoser]) to a benchmark class; running it emits a .diagsession you open directly in VS with a Benchmarks tab plus the matching profiler… |
| PerfTips | vs | Inline elapsed-time tooltip next to the instruction pointer after any break or step (F10/F11); toggled via Debug > Options > 'Show elapsed PerfTip while debugging'. Same durations are recorded in the Diagnostic Tools Events view's Duration column. |
| Flexible multi-tool profiling + command-line profiler | vs | The Performance Profiler's 'Flexible' tab lets you run several compatible tools together (e.g. CPU Usage + Database) in one session; 'Standalone' tools like .NET Object Allocation must run alone. A command-line profiler (VSDiagnostics) supports the same scenarios outside the IDE, e.g. for CI. |
| Profiling on WSL, Docker and Linux targets | vs | Alt+F2 > select WSL as the target, or attach to a running Linux/Docker process, to run CPU Usage and other supported tools against non-Windows targets from inside VS. |
| Copilot-assisted profiling (Auto insights, Profiler Agent, Ask/Analyze with Copilot) | vs | With GitHub Copilot enabled: a 'Recommended' tools tab suggests profiler tools from source analysis; per-tool 'Auto insights' (CPU Usage, .NET Object Allocation, Instrumentation) surface likely issues; an 'Analyze with Copilot' / 'Ask Copilot' button lets you query a collected trace in natural language; an agent-directed Profiler Agent can drive a profiling session on its own; CodeLens can… |
| Custom timeline user marks | vs | Apps can programmatically emit custom marks that render as icons on the CPU/Memory Usage timeline graphs. |
| IntelliTrace / Historical Debugging | vs | Visual Studio Enterprise only. Tools > Options > IntelliTrace records events, method calls, and (optionally) full application-state snapshots at every breakpoint/step; 'Activate Historical Debugging' rewinds the Call Stack, Locals and other debugger windows to any recorded point without rerunning the app. |
| Snapshot Debugger (Application Insights) | vs | Visual Studio Enterprise only. A collector running alongside a production ASP.NET/Azure app takes a minidump when a monitored exception recurs (default: on the 2nd occurrence); the resulting .diagsession is downloaded from the Azure Portal and opened in VS. Per Microsoft, this feature is deprecated and being removed from current Visual Studio / Azure Monitor releases. |
| Tier Interaction Profiling | vs | Visual Studio Enterprise only, part of the Performance and Diagnostics Hub. Profiles time spent crossing application tiers (e.g. web server → SQL Server) to surface redundant or duplicate queries across the whole request. |
| .NET Memory Dump Analysis ('Debug Managed Memory') | vs | Visual Studio Enterprise only, per Microsoft's edition-comparison page. Open a .dmp file and choose 'Debug Managed Memory' to get the same managed-types / Paths-to-Root report the live Memory Usage tool produces, but from a static crash or production dump, including a View Instances icon and Allocation Call Stack pane. |
| Time Travel Debugging (Preview) | vs | Visual Studio Enterprise only. Preview capability to record and replay full execution history of a process. |
| dotTrace profiling types: Sampling / Tracing / Line-by-Line / Timeline | rider | Chosen in the Profiling Configurations dialog or dotTrace Profiler tool window. Sampling for general perf work (default starting point); Tracing for exact call-count measurement (e.g. algorithmic complexity); Line-by-Line for per-line timing once a hot method is already known (high overhead, use narrowly); Timeline (recommended default) for temporal call-stack + thread-state data. |
| Timeline snapshot deep views | rider | A Timeline snapshot adds a Subsystems filter (JIT, GC, file I/O, string ops, lock contention, etc.), per-thread state tracking (Running/Wait/Sleep), automatic UI-freeze detection, memory-allocation events plotted on the same timeline, and HTTP request tracing showing server-side transaction spans. |
| Call Tree / Flame Graph / Hotspots (Top Methods) | rider | dotTrace snapshot views: Call Tree with a Flame Graph rendering mode (bar width = total time), a Hotspots/Top Methods list sorted by execution time, and an inverted Backtraces mode. |
| Profile attached / running .NET process | rider | Run > Profile running process lists live managed processes to attach dotTrace without restarting: Windows covers native, .NET Framework 4.0+, .NET Core 3.0+, .NET 5+, Mono 5.10+ and Mono/Unity; Linux covers .NET Core 3.0+/.NET 5+; macOS covers .NET 5+. The profiler can detach without killing the app. |
| Profile unit tests | rider | Select tests in the Unit Tests window (or use the editor gutter icon) > Profile Selected Unit Tests, pick Sampling/Tracing/Timeline (or a custom config); dotTrace auto-captures a snapshot when the test run ends and adds it to the Profiler window's snapshot list, with Jump to Source from any hot method. |
| Remote / SSH / Docker profiling | rider | Tools > SSH Configurations plus 'Attach to Remote Process (via SSH)' profiles a process on a remote host or inside a Docker container; dotTrace also ships a standalone command-line/remote agent for Linux containers where no IDE is present. |
| dotMemory snapshots & comparison | rider | Take one or more memory snapshots from the dotMemory tool window (Rider-integrated standalone analysis since 2023.2, cross-platform) and diff any two to see exactly which types/objects grew. |
| Dominator hierarchy (sunburst / icicle chart) | rider | dotMemory renders the dominator hierarchy — objects that exclusively retain other objects — as an interactive sunburst chart (icicle-chart alternative available cross-platform); clicking a segment shows that dominator's retention path. |
| Memory allocation view (live, no snapshot required) | rider | Select any time interval on the profiling timeline (or a single snapshot, or two for comparison) and open Memory Allocation, grouped by Types, Methods, Call Tree, or Subsystems (namespace/assembly), with inverted back-traces. Two collection modes: Sampled (Windows default, tracks allocations ≥100KB aggregate/thread, low overhead, works when attaching to a live process) or Full (exact size/count… |
| Memory traffic analysis | rider | dotMemory shows allocation and garbage-collection patterns broken down by object type and allocating function over time (rate/flow view, not just point-in-time heap state). |
| Automatic memory inspections (dotMemory) | rider | Snapshot analysis auto-flags string duplicates, sparse arrays, leaking event handlers, and leaking WPF bindings. |
| Import raw Windows memory dumps | rider | dotMemory can import a raw .dmp file captured via Task Manager or Process Explorer (not only its own snapshot format) for full dominator/retention-path analysis. |
| dotMemory Unit (automated memory testing) | rider | A NuGet-based unit-testing API (e.g. Assert.That(() => ..., MemoryAllocation...)) for asserting on object counts or memory traffic inside ordinary NUnit/xUnit tests, runnable headlessly via a command-line tool for CI. |
| Dynamic Program Analysis (DPA) | rider | Requires dotUltimate / ReSharper Ultimate. Runs automatically in the background every time you start your solution (measured overhead ~0-2% in JetBrains' own testing); flags closures and LOH/SOH allocation issues, and (2023.x+) EF Core N+1 queries and Cartesian-explosion query patterns, as inline code highlights. A status-bar DPA icon turns red when issues exist; suppress a finding via… |
| Monitoring tool window | rider | Opens automatically on Run/Debug (configurable: on debug+run / debug only / never — Settings > Build, Execution, Deployment > Monitoring). A Performance tab charts live CPU/GC/Memory; a Detected Issues tab (Windows) auto-flags UI Freeze, Performance Hotspot, and High GC with one-click navigation to the offending code; Counters and Environment tabs round it out. Since 2025.2, you can jump from a… |
| ETW CPU sampling in Monitoring | rider | On Windows, the Monitoring tool can optionally run ETW-based CPU sampling in the background (toggle in Settings > Monitoring), backed by the JetBrains ETW Host Service, at a typical 1-2% (up to ~10%) overhead. |
| OpenTelemetry plugin | rider | New in Rider 2025.2 EAP. Correlates local OpenTelemetry logs, metrics, and distributed traces with source code, auto-generates architecture diagrams from captured execution traces, and offers zero-config auto-instrumentation for local development. |
| EF Core relationship diagram & query analysis | rider | ReSharper/Rider 2023.3+ can generate an Entity Relationship Diagram directly from a DbContext, alongside additional static inspections for EF Core LINQ queries that complement DPA's runtime N+1 detection. |
| Heap Allocations Viewer (plugin) | rider | Open-source JetBrains Marketplace plugin (also for ReSharper) that adds inline gutter/text highlights in the editor for every local object allocation, boxing operation, delegate instantiation, and closure-creation point as you type. Actively maintained (support through Rider/ReSharper 2026.1). |
| dottrace-analyze AI agent skill | rider | An AI agent skill that reads a collected dotTrace profiling snapshot, follows the hot path programmatically, and explains which methods are slow and why, producing text or HTML reports. |
| Cross-platform native profiling (Windows/Linux/macOS, x64+arm64) | rider | dotTrace and dotMemory run natively inside Rider on Windows, Linux and macOS (x64 on all three; arm64 on all three with some .NET-version caveats; Parallels-on-Apple-Silicon can't collect Timeline sampling events due to virtualization limits). |

---

## Project, solution, build and package management

| Feature | IDE | What it is |
| --- | --- | --- |
| Repo-wide Files view alongside Solution view | rider | A second panel in the Solution Explorer tool window shows every file in the physical repository (not just files referenced by projects), rooted at the repo root rather than the .sln directory. It sits side-by-side with the logical Solution view so you can switch between 'what MSBuild sees' and 'what's on disk' without leaving the tool window. |
| Multiple Explorer perspectives (Solution / File System / scopes) | rider | The Explorer tool window can switch between a logical Solution view (mirrors the .sln), a File System view (raw disk layout), and custom Scopes (saved file-set filters, e.g. 'all test projects'). Scopes can also drive Find in Path (Ctrl+Shift+F) so a search only runs against a chosen subset of files. |
| Show All Files toggle | both | A Solution Explorer toolbar button that reveals files/folders that exist on disk but aren't included in the project (e.g. bin, obj, or manually-added source not yet referenced), with an inline 'Include In Project' action. |
| Configurable file nesting rules | both | Rider exposes a dedicated File Nesting dialog (Explorer toolbar > Appearance > File Nesting) where you define patterns like '*.designer.cs nests under *.cs' or '*.map.ts nests under *.ts', beyond the built-in DependentUpon convention. VS auto-nests known patterns (e.g. appsettings.Development.json under appsettings.json, .min.js under .js) but has no general-purpose rules editor for arbitrary… |
| Scoped code search (Ctrl+Shift+F) | both | Both IDEs let Find-in-Files be restricted to something narrower than the whole solution. VS 17.11+ lets you set the search scope per filter (current document, current project, entire solution, or referenced external files); Rider's Find in Path dialog can target a named Scope you configured (a saved file-set query) in addition to the built-in Whole Project/Module/Directory options. |
| New Project dialog with searchable templates | both | Both IDEs present a template gallery (grouped by language/platform/project type in VS; searchable and framework-grouped in Rider) driven under the hood by the same dotnet new template engine, so any `dotnet new --install <package>` template shows up in both. |
| Custom project template installation via dotnet new | rider | Rider directly consumes the dotnet new template ecosystem: run `dotnet new --install <template-package>` (from the NuGet gallery or a local folder) and the template immediately appears in Rider's New Project wizard, no IDE-specific packaging step required. |
| Custom project/item template authoring (.vstemplate + Export Template wizard) | vs | VS has a first-class custom template authoring model: any project or item can be exported via File > Export Template into a .zip containing source files plus a .vstemplate metadata file, which then shows up automatically in Create a New Project / Add New Item once dropped in the templates folder. |
| Deep multi-tab Project Designer (property pages) | vs | Right-click a project > Properties opens a structured designer with dedicated pages: Application (output type, target framework/OS, startup object, Win32 resources/manifest), Build (general, errors/warnings, output, events, publish, strong naming, advanced), Package (nuspec metadata, license, symbols), Code Analysis (run-on-build, .NET analyzers, analysis level), Debug (launch profiles), and… |
| Rider Project Properties editor | rider | A comparable but leaner properties UI covering Application (general/language/resources/binding redirects), NuGet packing metadata, Assembly info, Signing, Reference Paths, Build Events, Inspections, Debug/Release, Imports, and a raw 'Properties' tab listing every MSBuild property defined for the project including ones from imported targets. |
| Assembly and manifest signing UI | vs | A dedicated Signing property page for strong-naming (key file selection/creation, delay-sign) and ClickOnce manifest signing (certificate selection, timestamp server) that wraps the sn.exe/mage.exe tooling. |
| Code Analysis project property page | vs | A dedicated page to toggle 'Run on build' / 'Run on live analysis', enable/disable the built-in Roslyn analyzers, set the analysis level (e.g. latest, latest-recommended), and enforce .NET code-style rules as build errors/warnings — all without touching EnableNETAnalyzers/AnalysisLevel MSBuild properties by hand. |
| Managing project, assembly, COM and framework references | both | Both IDEs offer an Add Reference dialog that browses solution projects, browsable/searchable assemblies, COM components registered on the machine, and .NET framework reference assemblies. VS's dialog is organized into fixed tabs (Projects, COM, Browse, Recent) inherited from decades of Windows/COM interop scenarios; Rider's equivalent (right-click > Add > Reference) searches a flattened,… |
| NuGet Package Manager UI: Browse / Installed / Updates tabs | both | Both IDEs provide a package browser with search-by-popularity Browse tab, an Installed tab listing current PackageReferences (with implicit/transitive packages called out separately), and an Updates tab for available newer versions, each showing README, license, owners, and dependency metadata in a details pane. |
| Solution-wide Consolidate tab | vs | 'Manage NuGet Packages for Solution' has a Consolidate tab that detects when different projects in the same solution reference different versions of the same package, and lets you pick one version and apply it across all selected projects in one Install action. |
| Upgrade Packages in Solution dialog | rider | A single dialog lists every outdated package across every project in the solution with checkboxes and a search filter, letting you batch-upgrade many packages across many projects in one operation instead of visiting each project's Installed/Updates tab. |
| Package Manager Console (PowerShell) | both | VS ships a full PowerShell host (Tools > NuGet Package Manager > Package Manager Console) with cmdlets Find-Package/Install-Package/Update-Package/Uninstall-Package/Get-Package, project/source-scoped by default, extensible by packages that add their own cmdlets (e.g. EF Core's Add-Migration/Update-Database), plus a persisted NuGet-specific PowerShell profile. Rider's NuGet tool window has an… |
| NuGet Audit vulnerability warnings | both | Both IDEs surface NuGetAudit (NU1900-series) warnings, generated during restore, when a referenced package (direct or, since NuGet 6.8+/.NET 9 SDK, transitive) has a known advisory, with a configurable severity threshold and audit source list. Rider additionally layers Checkmarx-powered advisories directly into the NuGet tool window and the Problems view. |
| Transitive dependency visibility | vs | Solution Explorer's Dependencies > Packages node has a collapsible 'transitive packages' section (introduced 17.3, expanded in later NuGet releases to bubble up vulnerability indicators from transitive packages onto the direct package and at the solution level), and the Package Manager's Installed tab warns about vulnerable transitive packages even when auditing is configured for direct packages… |
| Package deprecation and license review | both | The package details pane in both IDEs' NuGet UIs shows the license (expression or embedded license file), authors/owners, and — when the package source reports it — a deprecation notice with the recommended alternative. |
| Central Package Management (Directory.Packages.props) | both | Both IDEs fully understand CPM: when ManagePackageVersionsCentrally is set, PackageReference items in the NuGet UI resolve their version from the nearest Directory.Packages.props, edits to a package's version in either the Browse/Installed tab or the file itself keep .csproj and .props in sync, and both support per-solution or per-repo-root .props files. |
| NuGet lock file support (packages.lock.json) | both | Setting RestorePackagesWithLockFile=true generates a packages.lock.json that pins the full resolved dependency graph; both IDEs' restore pipelines respect and update this file, and it can be paired with CPM for a fully deterministic, auditable dependency set that's checked into source control. |
| NuGet package source management and source mapping | both | Both IDEs let you view/add/remove/reorder package sources (including per-solution NuGet.config discovery — Rider's Sources tab explicitly lists every NuGet.config found on disk and which one is being edited) and configure Package Source Mapping so specific package ID patterns are only ever resolved from a specific source. |
| NuGet cache / global-packages folder browsing | rider | A dedicated Folders tab in the NuGet tool window lists the machine's NuGet cache locations (global-packages, http-cache, plugins-cache, temp) with sizes, and lets you clear individual caches from the UI. VS only exposes the global packages folder path as a setting, with no built-in browse/clear UI. |
| Assembly Explorer package decompilation | rider | From the NuGet tool window you can open an installed package directly in Rider's bundled dotPeek-based Assembly Explorer to inspect the decompiled contents of its DLLs, without leaving the IDE or downloading the package separately. |
| Find Dependent Code for a package | rider | A context action on an installed NuGet package that finds every place in your own codebase that actually uses types/members from that package, distinct from ordinary Find Usages on a single symbol. |
| Solution Configuration/Platform Manager | vs | Configuration Manager (Build > Configuration Manager, or Solution Properties > Configuration Properties) lets you create/rename/delete solution configurations and platforms, map each project to a specific configuration/platform combination (including mixed ones, e.g. one project stays x86 while the rest are Any CPU), and check/uncheck which projects build or deploy for each configuration —… |
| Filtered solutions (.slnf) | vs | You can open a solution with zero projects loaded (via 'Do not load projects' or the /donotloadprojects switch), selectively load only the projects you're working on plus 'Load Direct Dependencies'/'Load Entire Dependency Tree', and save that subset as a .slnf filter file that can be committed and shared so teammates get the same fast-loading subset. |
| Unload/reload project and raw project-file editing | both | Both IDEs let you unload one or more projects (right-click > Unload), which removes them from the build/IntelliSense graph while keeping the node visible, then edit the underlying .csproj/.vbproj XML directly, and reload afterward. Rider additionally lets you persist an unload set as a solution filter, and offers 'Load Project with Dependencies' to bring back only what's needed. |
| Error List with build/IntelliSense/scope filtering | vs | The Error List window separates 'Build + IntelliSense' errors from build-only errors, can be filtered by project, severity, and a free-text search, and integrates with the Output window's Build pane for the raw MSBuild log stream. |
| MSBuild binary log (binlog) generation and viewing | vs | VS can generate a .binlog (via the optional Project System Tools extension's Build Logging window, or by passing -bl to an underlying MSBuild/dotnet build) and binlogs are inspected in the community MSBuild Structured Log Viewer, which auto-selects the failing node/errors on open and lets you drill into every target, task, and property evaluation. Rider has no bundled binlog viewer; users rely… |
| Parallel/incremental build tuning | both | Both IDEs let you cap the number of projects MSBuild builds in parallel (VS: Tools > Options > Projects and Solutions > Build and Run > 'maximum number of parallel project builds'; Rider: Settings > Build, Execution, Deployment > Toolset and Build) and both rely on MSBuild's incremental build (skip-if-up-to-date) engine underneath. |
| Full Publish wizard (folder/IIS/FTP/Azure/Docker registry) | vs | Right-click Publish opens a wizard covering: filesystem folder, FTP/FTPS server, Web Server (IIS) via Web Deploy or a Web Deploy Package zip, Azure App Service, and Docker/Azure Container Registry or Docker Hub — each producing a reusable, summary-page-backed publish profile (.pubxml) you can re-run or edit later, with import of existing IIS/Azure .publishsettings files. |
| Publish-oriented run configurations | rider | Instead of a unified wizard, Rider models publishing as run/debug configurations: 'Publish to Folder' (framework-dependent/self-contained, target runtime, ReadyToRun, trimming, single-file, target location), 'Publish to IIS' (Windows-only, MSBuild+MSDeploy via .pubxml, supports Azure Web Apps through Kudu), and 'Publish to Custom Server' (cross-platform FTP/FTPS/SFTP upload). Each is triggered… |
| ClickOnce deployment | vs | Full ClickOnce tooling: Publish wizard target, Prerequisites dialog for redistributable packages, signing via the Signing property page, and (since VS 2022 17.4+) support for .NET (non-Framework) ClickOnce apps with a configurable PublishDir separate from PublishUrl, plus command-line build support. |
| Native AOT / trimming / single-file / ReadyToRun publish options | both | Both IDEs expose the modern .NET publish knobs: 'Publish native AOT' and 'Publish trimmed' checkboxes on VS's Build > Publish property page (and in the Publish wizard's profile settings), versus Rider's 'Publish to Folder' run configuration which exposes deployment mode (framework-dependent/self-contained), target runtime, ReadyToRun compilation, trim unused assemblies, and produce-single-file… |
| Container tooling: Dockerfile generation and Containers window | vs | Add > Container Support (or Docker Support) generates a Dockerfile for ASP.NET Core/.NET console projects, with a choice (17.9+) between an explicit Dockerfile build or the .NET SDK's built-in container build; a dedicated Containers window lets you browse running containers/images, inspect env vars/logs/port mappings/filesystem, attach a debugger, or open a terminal inside the container — all… |
| Container Compose / orchestrator support | vs | Add > Container Orchestrator Support wires up multi-project Docker Compose (or, for .NET Framework/Windows-container scenarios, Azure Service Fabric) so an entire docker-compose.yml-defined group of services can be run and debugged together from one F5, with the ability to select a subset of services to start for a given debug session. |
| Docker/Docker Compose tooling | rider | A bundled-by-default Docker plugin lets Rider run and debug a project (or a docker-compose-defined group of projects) directly against an already-configured Dockerfile/compose file, with its own Docker tool window for container/image inspection. |
| .NET Aspire orchestration support | both | VS: right-click a project > Add > .NET Aspire Orchestrator Support scaffolds an AppHost + ServiceDefaults project and adds the selected project to the AppHost; running the AppHost launches the Aspire Dashboard (Projects/Containers/Executables/Logs/Traces/Metrics views) inline, bundled with the Web & Cloud workload since VS 17.9. Rider: the same AppHost model is supported through JetBrains' .NET… |
| Scaffolding wizard (MVC/API controllers, Identity, Razor CRUD) | vs | Add > New Scaffolded Item opens a wizard that generates ASP.NET Core MVC controllers (with or without views), Web API controllers, Razor Pages CRUD, and full ASP.NET Core Identity UI (registration/login/2FA pages) wired to a chosen DbContext and model class, driven by the same aspnet-codegenerator engine used from the CLI. |
| Entity Framework Core migrations UI | both | VS exposes Add-Migration/Update-Database (and Remove-Migration) as Package Manager Console cmdlets contributed by the Microsoft.EntityFrameworkCore.Tools package, plus a Connected Services-driven 'Add migration'/'Update database' flow for SQL Server Express LocalDB projects. Rider (via its bundled EF Core support, backed by the rider-efcore plugin) provides visual commands for Add Migration,… |
| Solution-wide safe rename/move with reference updates | rider | Renaming a project through Rider's refactoring (not a plain F2 file rename) updates the root namespace and every reference to it solution-wide; deleting a project or file runs a 'Safe Delete' check that scans for remaining usages/dependents before removing it, warning you if something still depends on what you're about to delete. |
| .slnx XML solution format | both | Both IDEs support the new XML-based .slnx solution format as an alternative to the classic .sln text format: VS 17.13+ can Save Solution As > XML Solution File, Rider 2024.3+ can Save As > Save as XML Solution, and the .NET CLI can migrate an existing .sln via `dotnet sln migrate` (and defaults new solutions to .slnx as of .NET 10). |
| launchSettings.json-backed launch profile UI | both | VS's Debug property page opens a visual Launch Profiles UI (add/clone/rename profiles, set command-line args, environment variables, launch browser, app URL) that reads/writes the project's launchSettings.json directly. Rider auto-generates a matching run/debug configuration per profile the first time it opens a project with launchSettings.json, and offers a 'Generate Configurations' command… |
| Multiple startup projects | both | VS: Solution Properties > 'Configure Startup Projects' lets you switch to 'Multiple startup projects', set each project's action (Start / Start without debugging / None) and launch order via up/down arrows, and (17.10+) persist this as a shareable, source-controlled Multi-Project Launch Profile rather than a solution-file-local setting. Rider: select multiple projects in Solution Explorer and… |
| Breadth of run/debug configuration types | rider | Rider's Run/Debug Configurations are a general-purpose extensible model with many built-in types beyond a plain project launch: .NET Project, .NET Executable, Docker, Docker Compose, Compound, per-framework unit test runners (xUnit/NUnit/MSTest), URL, Node.js, Attach to Process, .NET Aspire AppHost, and the publish-to-folder/IIS/custom-server types covered above — each independently configurable… |
| Live architecture Dependency Validation and Code Maps (Enterprise-only) | vs | Visual Studio Enterprise lets you define layer/dependency diagrams describing intended project or namespace-level dependencies, then validates the actual codebase against that diagram live as you code — violations appear directly in the Error List — and can also validate at build time (via a NuGet 'Dependency Validation Analyzer' package) or in CI (Azure Pipelines). Code Maps generate an… |
| NuGet 'pack project' from Solution Explorer | rider | Right-click a project > Advanced Build Actions > Pack Selected Project runs `dotnet pack` against it and produces a .nupkg without leaving the IDE or opening a terminal. |

---

## Web development: ASP.NET Core, Razor, Blazor, WebForms, JS/TS

| Feature | IDE | What it is |
| --- | --- | --- |
| LSP-based Razor editor / engine rewrite | both | VS rebuilt the .cshtml/.razor editor on a Language Server Protocol architecture (2021-22), and Rider rebuilt its tag-helper/Blazor-component analysis engine for 2025.1, cutting analysis time 5-10% on large solutions with heavy component usage and reducing false errors. |
| Tag Helper IntelliSense | both | As soon as you type an opening tag, both IDEs list matching HTML elements and tag-helper-targeted attributes (e.g. asp-for), switch to C# completion inside attribute-value quotes (showing model properties/methods), and render tag-helper-affected markup in a distinct font/color. |
| Razor component parameter/attribute completion | both | Typing a component tag in .razor markup completes the component name and its public [Parameter] properties as attributes, with type-aware value completion (e.g. enum, bool, EventCallback). |
| Go to Definition on component tags (F12) | both | Pressing F12 on a <MyComponent /> tag navigates straight to the component's .razor file; VS added this in the 2022 Razor editor rewrite, and Rider provides equivalent navigation through its Razor/Blazor support. |
| Razor/Blazor rename refactoring across markup and code | both | Renaming a component, parameter, or route parameter updates every usage across .razor markup, @code blocks, and code-behind files in one operation. |
| Razor code actions: Extract to code-behind, Create component, Add/fully-qualify usings | vs | VS's Razor editor exposes quick actions (lightbulb) to extract a code block into a .razor.cs code-behind file, generate a new component from selected markup, and add or fully-qualify @using directives for an unresolved component. |
| Live Templates for Razor | rider | Rider ships predefined live templates (code snippets with tab-stops) specifically for Razor markup and code blocks, in addition to its general C# live templates. |
| JetBrains Annotations for custom routing | rider | Attributes like RouteTemplateAttribute, AspMinimalApiDeclarationAttribute, UriStringAttribute and AspRouteConventionAttribute let library/framework authors mark their own routing APIs so Rider gives them the same completion, navigation, rename, and Endpoints-window discovery it gives built-in [Route]/minimal-API routes. |
| Route template completion, rename and usage tracking | rider | Typing a route parameter placeholder ({) offers completion from the action's parameters; renaming an attribute-route segment on a controller updates every string reference to that route, including from inside plain URI string literals passed to HttpClient. |
| Full Visual Studio Live Share support in Razor editor | vs | The rewritten Razor editor fully supports Live Share co-editing/co-debugging sessions, which the legacy editor did not. |
| Blazor project templates with render-mode wizard | both | New-project wizards for Blazor Web App / standalone Blazor WebAssembly expose Interactive render mode (Server, WebAssembly, Auto, or None), interactivity location (per-page/component vs global), and a sample-pages toggle, mapped 1:1 to dotnet new template options. |
| Blazor component creation dialog (code-behind / scoped CSS / JS file) | rider | Rider 2024.1 enhanced its 'New Blazor Component' dialog with checkboxes to simultaneously scaffold a .razor.cs code-behind file, a .razor.css scoped stylesheet, and a .razor.js JS interop file alongside the component. |
| Blazor Hot Reload (markup, C#, scoped CSS) without debugger | both | Both IDEs push .razor, .cs, and .razor.css edits to a running app without a full rebuild, including in plain 'Run' (non-debug) mode; VS added save-triggered Hot Reload and live CSS updates, Rider added run-mode Hot Reload for Blazor WASM in 2024.1 covering scoped CSS files like Counter.razor.css. |
| CSS isolation (.razor.css) authoring and bundling | both | Both editors treat .razor.css as plain CSS scoped to its matching component; the framework rewrites selectors at build time into a bundled {assembly}.styles.css. Rider explicitly hot-reloads changes to these scoped files at runtime. |
| Blazor WebAssembly debugging via Chrome DevTools Protocol proxy | both | Both IDEs implement (or drive) a debug proxy that speaks the Chrome DevTools Protocol to the browser's Mono/WASM runtime, giving breakpoints, single-step, a Locals window, expression evaluation/watches, and a call stack that spans JS-to-.NET boundaries. |
| Combined server + WASM-client breakpoints in one Blazor Web App session | vs | For a Blazor Web App using the Auto render mode, VS documents hitting breakpoints in both the .Client (WebAssembly) project and the server project's interactively- or statically-rendered components within a single F5 session. |
| Break-on-unhandled-exception configuration for Blazor WASM | vs | Because Blazor catches exceptions that would otherwise be unhandled, VS requires opting in via Debug > Windows > Exception Settings > JavaScript Exceptions (All Exceptions / Uncaught Exceptions) to break on them. |
| Symbol-server support for Blazor WASM debugging | vs | VS can resolve debug symbols for Blazor WebAssembly apps through a configured symbol server, matching the workflow used for regular .NET debugging. |
| Multi-Launch run configuration | rider | Rider 2024.1 added a run-configuration type that launches multiple projects in a defined sequence, with per-step options to start immediately, wait for the previous step to finish, or wait for a port to become available, plus auxiliary Build/Clean/Publish/Idle steps. |
| Blazor WebAssembly publish fix (browser-wasm targeting) | rider | Rider 2025.1 fixed Blazor WASM publishing through the .NET Publish \| Publish to Folder configuration so it correctly targets the browser-wasm runtime and no longer omits output files. |
| Go to View / Go to Controller navigation | vs | In classic and ASP.NET Core MVC projects, VS's Ctrl+M, Ctrl+G shortcut (and context-menu items 'Go To View...'/'Go To Controller...') jumps between an action method and its corresponding view, inferring the view name/location by convention. |
| MVC/Razor Pages Add Controller/View scaffolding with EF CRUD generation | vs | Solution Explorer's Add > New Scaffolded Item wizard generates a controller (empty, API, or 'with views, using Entity Framework') and/or Razor views wired to a chosen model and DbContext, producing full CRUD action methods and matching Create/Edit/Details/Delete/Index views. |
| ASP.NET Core Identity / Blazor Identity scaffolding wizard | vs | Add > New Scaffolded Item > Identity (or Blazor Identity for Blazor Web Apps/Blazor Server) generates Account pages (Register/Login/Logout/ResetPassword/…), a DbContext, and an optional custom user class, with an option to preserve an existing customized _Layout.cshtml; a follow-on Connected Services panel offers one-click 'Add migration'/'Update database' against LocalDB. |
| ASP.NET Core Web API project wizard (minimal vs controller-based) | vs | The 'ASP.NET Core Web API' template's Additional Information dialog has an 'Use controllers' checkbox that toggles between generating a minimal-API Program.cs and a traditional Controllers-based project, plus an 'Enable OpenAPI support' checkbox that wires up Microsoft.AspNetCore.OpenApi at creation time. |
| Endpoints tool window (multi-framework) | rider | Rider's Endpoints window discovers HTTP/WebSocket/gRPC endpoints across controllers, minimal APIs, and third-party frameworks (OpenAPI/Swagger specs, WSDL, Retrofit, OkHttp, MicroProfile, Micronaut, Quarkus, Helidon), with filtering by module/type/framework, jump-to-source (F4), Find Usages, and per-endpoint HTTP Client / OpenAPI / Documentation tabs. |
| Endpoints Explorer | vs | View > Other Windows > Endpoints Explorer lists a project's minimal-API and controller-based endpoints, discovered statically at design time and (since VS 17.11) dynamically at run time for endpoints that can't be found statically (e.g. those defined in a class library); right-click > Generate Request writes a ready-to-send request into a project .http file. |
| appsettings.json IntelliSense | vs | VS ships a built-in JSON schema for well-known ASP.NET Core configuration sections (Logging, Kestrel, AllowedHosts, etc.); Ctrl+Space inside appsettings.json completes recognized keys with no setup required. |
| appsettings*.json schema via JSON Schema Store | rider | Rider auto-downloads and applies community JSON schemas (from the JSON Schema Store) for recognized config file names, shows the active schema name on the status bar, and lets you manually pick a schema for non-standard file names or scratch files. |
| Automatic nesting of related config files | rider | Rider automatically nests environment-specific config files (e.g. appsettings.Development.json) under their base file (appsettings.json) in the Solution/Project view, mirroring the .csproj-driven nesting VS applies to the same files. |
| launchSettings.json-driven run configuration | both | Both IDEs parse Properties/launchSettings.json and populate a run/debug dropdown from its profiles (including IIS Express); VS edits it through a graphical project Properties > Debug tab, Rider auto-imports each profile as a native Run Configuration. |
| IIS Express integration | both | Both IDEs can launch and debug against IIS Express: VS natively as the default F5 profile, Rider by generating an applicationhost.config from a bundled template and verifying prerequisites (IIS Express installed, dev certificate present) before launch. |
| Publish wizard (IIS/Web Deploy, Azure App Service, Folder) | vs | VS's built-in Publish flow generates and manages .pubxml profiles targeting IIS via Web Deploy, Azure App Service, a local/network folder, or a container registry, all from a GUI wizard with profile import/export. |
| Client-Side Library Manager (LibMan) | vs | Solution Explorer > Add > Client-Side Library opens a dialog to fetch static JS/CSS assets (jQuery, Bootstrap, the @microsoft/signalr client, etc.) from cdnjs/unpkg/jsDelivr straight into wwwroot, recording the choice in a libman.json manifest. |
| WebForms Web Live Preview designer | vs | A WebView2/Edge-Chromium-based WYSIWYG designer for .aspx pages: drag-and-drop from the toolbox, double-click controls to generate event handlers, an Action Panel/Smart Tag per control, live CSS updates without saving, and a split-view layout; enabled by default in current VS 2022 for .aspx files (opt-in via Tools > Options in earlier previews). |
| WebForms text/code editing without a designer | rider | Rider can open, edit, build and run .aspx/.ascx projects (including .NET Framework 4.8 WebForms) with syntax highlighting and code-behind navigation, but has no visual designer or drag-drop toolbox for WebForms controls (tracked as an unresolved feature request, RIDER-625). |
| Framework-aware JS/TS routing and API completion | rider | Rider resolves file-system-based routing for Next.js, Nuxt, SvelteKit and Astro (autocompletion and navigation for route paths), supports Angular 17.2 signal queries and host bindings, and upgraded its Vue support to Vue Language Tools 2. |
| Zero-config TypeScript run/debug | rider | Rider can execute and debug .ts files directly (via context menu, the Run widget, or a Current File configuration) without a separate compile step being set up first. |
| Webpack-aware module resolution for JS/TS editing | rider | Rider reads a project's webpack config to resolve module imports across JS/TS files, with a choice between automatic (per-file config auto-detection) and manual (one config file for the whole project) modes. |
| TypeScript debugging with source maps | both | Both IDEs let you set breakpoints directly in .ts source rather than compiled .js, using source maps generated by tsc/webpack/babel to translate between the two during a live debugging session. |
| Task Runner Explorer for npm scripts | vs | Right-clicking package.json exposes a Task Runner Explorer listing npm scripts, runnable by double-click or context menu; a companion community extension (WebPack Task Runner) adds the same for webpack tasks. |
| .http/.rest file editor with environments, secrets, and dynamic values | vs | VS 2022 (17.8+) executes requests from .http files with @variable definitions, multi-environment http-client.env.json files (including a $shared default environment and a gitignored .user override file), request-chaining variables that extract JSON/XPath values from a prior response, secret providers for ASP.NET User Secrets/Azure Key Vault/DPAPI, and dynamic functions ($randomInt, $datetime,… |
| HTTP Client with pre-request/response-handler scripts | rider | Rider's HTTP Client (the format VS's .http editor was modeled on) supports an execution flow of an optional pre-request script, the request, and an optional response-handler script per request block, and auto-generates a temporary HTTP Request run/debug configuration each time you execute one. |
| OpenAPI/Swagger client generation via Connected Services | vs | VS's Connected Services (plus community extensions such as Unchase OpenAPI (Swagger) Connected Service, built on NSwag) generate a strongly-typed C# or TypeScript HttpClient (or server controllers) from an OpenAPI/Swagger spec, and can auto-refresh the client whenever the source spec changes; the dotnet-openapi CLI tool and <OpenApiReference> MSBuild item provide the same capability without a GUI. |
| OpenAPI spec authoring, Swagger Codegen integration, and spec diffing | rider | Rider provides completion/validation for OpenAPI YAML/JSON documents, a Swagger Codegen run configuration for generating server stubs, client SDKs, or docs from a spec, and a 'Compare OpenAPI Specifications' command that summarizes breaking changes (paths/parameters/responses) between two spec files. |
| gRPC/Protobuf build-time code generation | both | Adding a .proto file with a Protobuf MSBuild item and the Grpc.Tools NuGet package generates the C# client/service-base classes into obj/ on every build in either IDE; both editors then provide IntelliSense over the generated types. |
| Dedicated gRPC plugin and Endpoints-window gRPC support | rider | Rider's gRPC plugin (Settings > Plugins > Marketplace) adds richer .proto editing than the base Protocol Buffers syntax-highlighting plugin, and the Endpoints tool window recognizes gRPC/Protobuf services alongside REST endpoints, letting you generate and send gRPC requests from its HTTP Client tab. |
| Monitoring tool window: live ASP.NET/DB issue detection | rider | Rider 2025.3's Monitoring tool window automatically highlights slow queries, long-running MVC actions, and excessive database connections while an ASP.NET Core app runs or is being debugged. |
| IntelliTrace historical debugging | vs | Visual Studio Enterprise-only. Records a timeline of app events/calls during a debug session so you can step backward through execution history after a bug has already occurred, applicable to ASP.NET Core web apps as much as any other .NET app. |
| Snapshot Debugger | vs | Visual Studio Enterprise-only, integrated with Azure App Service/Application Insights: captures point-in-time snapshots (call stack + variable state) from a live, production ASP.NET web app without stopping it, viewable back in VS. |
| Live Unit Testing | vs | Visual Studio Enterprise-only: runs affected unit tests continuously in the background as you edit, showing inline pass/fail/coverage glyphs in the editor gutter — applicable to the test suites of ASP.NET Core/Blazor web projects like any other .NET project. |

---

## Data, Entity Framework and database tooling

| Feature | IDE | What it is |
| --- | --- | --- |
| Databases tool window & multi-engine connections | rider | DataGrip-powered Databases tool window connects to SQL Server, PostgreSQL, MySQL, Oracle, SQLite and more, plus NoSQL stores (MongoDB, Cassandra, Redis, DocumentDB, Couchbase). Data sources can be filtered by regex, grouped, toggled to show all schemas, and force-refreshed to reload cached metadata. |
| SQL Server Object Explorer | vs | An SSMS-like hierarchical tree of connected SQL Server / Azure SQL instances under a dedicated node. From context menus you can create, edit, rename and delete tables, views, stored procedures, types and functions, edit table data, compare schemas, and run queries. |
| Server Explorer "Data Connections" node | vs | A separate, generic ADO.NET/OLE DB/ODBC connections node in Server Explorer, distinct from the SQL-Server-specific SQL Server Object Explorer, used for non-SQL-Server relational sources. |
| Data editor (grid editing) | both | Rider/DataGrip's data editor lets you add, remove, clone and edit rows, navigate related rows via foreign keys, and submit/revert individual cell changes. VS's SQL Server Object Explorer offers a comparable but more basic "View Data" grid for inline edits. |
| Query consoles / query files | rider | Every data source auto-attaches a SQL query console for ad-hoc execution. The 2026.1 redesign lets you also create standalone, named/relocatable query files (grouped in a dedicated Query Files folder) alongside consoles, and switch between the two workflows. |
| In-editor T-SQL execution with execution plan | vs | The T-SQL editor opened from SQL Server Object Explorer gives IntelliSense and lets you run ad-hoc queries against the connected instance, including an SSMS-style "Include Actual Execution Plan" option. |
| Schema-aware SQL completion, inspections & refactoring | rider | Smart code completion, on-the-fly error highlighting, quick-fixes and refactorings (e.g. renaming a column) that resolve against the live connected schema, plus a customizable SQL code formatter. |
| Explain-plan viewer with flame graphs | rider | Shows the execution plan for a statement in table form (running EXPLAIN) and as a diagram (Ctrl+Alt+Shift+U). The 2026.1 release added dedicated Total Cost / Startup Cost flame-graph sub-tabs, a per-row detail panel in the Operations Tree, and quick-documentation popups for referenced tables. |
| Transact-SQL debugger | vs | SSDT provides a T-SQL debugger for stepping through stored procedures/scripts with breakpoints. Microsoft's own docs confirm it exists by calling out that it is specifically unavailable on Arm64 builds of Visual Studio 2026. |
| Database (ER) diagrams | rider | Right-click a database in the Database tool window and choose Diagrams \| Show Diagram to graph tables and their relationships; diagrams export as an internal UML format or PNG. |
| DbContext model diagram (EF Core Power Tools) | vs | The community EF Core Power Tools extension adds "Add DbContext Model Diagram", generating a DGML graph of your code-first DbContext model that opens in VS's built-in DGML viewer. |
| Schema Compare | both | Rider/DataGrip can compare schemas between two data sources and generate a migration script. VS/SSDT's Schema Compare (Tools > SQL Server > New Schema Comparison, or right-click project > Schema Compare) diffs two .dacpacs/databases/projects, lets you drill into and selectively exclude differences, and either applies changes directly or generates a script. |
| Data Compare | both | DataGrip/Rider has a data comparison tool with difference highlighting between result sets. VS/SSDT's Data Compare diffs actual row data between two databases sharing a schema and can generate a script to apply the differences. |
| Data import/export | rider | Imports/exports data to and from CSV, JSON, HTML, Markdown and Excel, and can shell out to native tools like mysqldump, pg_dump, pg_restore or psql for dump/restore workflows. |
| DDL data sources (offline schema) | rider | A data source can be created from a DDL script/dump with no live connection required, giving completion and navigation against a schema you don't have credentials for yet. |
| Version control integration for DB artifacts | rider | DDL/data-source definitions integrate with the IDE's Git/VCS support the same way source files do. |
| Wide multi-dialect and NoSQL query language support | rider | Dialect-aware highlighting/completion for T-SQL, PL/pgSQL, PL/SQL, MySQL and SQLite dialects, plus dedicated query-language support for MongoDB, Cassandra CQL, Redis commands and Couchbase queries. |
| AI natural-language-to-SQL and query explanation | rider | AI Assistant can generate SQL from a plain-language prompt and explain existing queries; the 2026.1 AI Chat tool window added Claude Agent/Codex support and auto-attaches the right data source and SQL dialect when creating files from suggestions. |
| AI agent skills / MCP tools for database management | rider | DataGrip 2026.2 ships three bundled AI agent skills (database-tools, database-connection-management, database-text-to-sql) plus CLI-based data-source management, so an AI agent can explore/query a connected DB, configure connections, and generate SQL from the AI chat. |
| Shareable data source templates via JetBrains Account | rider | Reusable connection configurations (general + advanced settings, excluding credentials) can be saved to a JetBrains Account and applied from Rider or DataGrip on any machine. |
| Language injection of SQL into C# strings | rider | Detects SQL automatically in known contexts (e.g. inside a SqlCommand() constructor) or lets you inject it manually via Alt+Enter > "Inject language or reference", giving the string literal full completion, inspections, formatting and intentions, either inline or in a dedicated "Edit <language> Fragment" editor. |
| Run injected SQL directly against a data source | rider | SQL detected/injected inside a C# string literal can be executed directly against an attached data source via Alt+Enter, without copying it out to a separate console. |
| EF-specific code inspections | rider | Eight dedicated EF/EF Core inspections: DB function called outside a query context; a function not translatable to SQL called inside a query; two N+1-query inspections (direct, and for related/navigation entities); unbounded string length as a perf warning; two variants of navigational-property usage returning incomplete data; and relationships that create a dependency loop. |
| Visual interface for EF Core commands | rider | Tools > Entity Framework Core (or right-click the project) exposes Add Migration, Remove Last Migration, Update Database, Generate SQL Script, Drop Database and Scaffold DbContext as dialogs that wrap `dotnet ef`, remember prior field values, and offer a "skip build" toggle plus diagnostic logging. Requires netcoreapp3.1+/netstandard2.1+ and dotnet-ef 5+. |
| Scaffold DbContext reuses Rider's own DB connections | rider | The Scaffold DbContext dialog's Connection field lets you pick a data source already configured in Rider's Database tool window instead of retyping a raw connection string, alongside toggles for scaffolding all/selected tables and schemas, attributes vs. fluent API, database-exact naming, pluralization and OnConfiguring generation. |
| EF Core Package Manager Console cmdlets | vs | Full cmdlet surface in Visual Studio's Package Manager Console: Add-Migration, Remove-Migration, Update-Database (with an EF Core 11 -Add flag to create-and-apply in one step), Get-Migration, Get-DbContext, Script-Migration, Script-DbContext, Scaffold-DbContext, Drop-Database, Optimize-DbContext (compiled models), and Bundle-Migration, all supporting -Project/-StartupProject/-Context targeting… |
| EF Core migration bundles | both | `dotnet ef migrations bundle` / PMC's `Bundle-Migration` produces a standalone executable (optionally self-contained with the .NET runtime) that applies pending migrations on a target machine without needing the SDK or EF tools installed. Runnable from either IDE's terminal or VS's PMC; Rider's GUI dialog list does not expose bundling directly. |
| EF Core Power Tools: reverse engineering / scaffolding UI | vs | Popular free/OSS Visual Studio extension that scaffolds a DbContext + POCOs from SQL Server, Azure SQL (incl. MFA), SQLite, PostgreSQL, MySQL, Firebird, Oracle, Snowflake, a SQL Database project, or a .dacpac. Offers per-table/column/index checkboxes, F2 renaming plus regex-based efpt.renaming.json, DataAnnotations-vs-Fluent-API, nullable reference types, schema-based folder/namespace layout,… |
| EF Core Power Tools: custom T4/Handlebars templates | vs | Generated entity/DbContext code can be fully customized via T4 templates (DbContext.t4 / EntityType.t4) or Handlebars templates, including a template that splits the DbContext into per-entity EntityTypeConfiguration classes. |
| EF Core Power Tools: stored procedure/function mapping | vs | Maps table-valued and scalar SQL functions and stored procedures (including multiple result sets, in preview) into strongly typed DbSet/method calls on the generated DbContext. |
| EF Core Power Tools: View DbContext Model SQL DDL | vs | Generates a SQL DDL script directly from the compiled DbContext model, letting you inspect or diff the code-first model's implied schema without creating a migration. |
| LINQ to SQL O/R Designer (.dbml) | vs | Adding a "LINQ to SQL Classes" item creates a .dbml file edited via the Object Relational Designer; dragging tables from Server Explorer onto the left pane and stored procedures onto the right pane generates the mapped classes and a .designer.cs/.vb file. |
| ADO.NET Entity Data Model (EDMX) designer for EF6 | vs | The Entity Designer visually creates entities, associations, mappings and inheritance and generates C#/VB object-layer code; the Entity Data Model Wizard reverse-engineers a conceptual model from an existing database (database-first), the Create Database Wizard goes model-first, and the Update Model Wizard refreshes the model after DB changes. Produces an .edmx file, which EF Core does not… |
| Typed DataSet designer + TableAdapter wizard | vs | The Data Sources window drives an end-to-end designer workflow: create a typed DataSet (.xsd) from a data source, configure it with the Dataset Designer, define fill/insert/update/delete commands per table via the TableAdapter Configuration Wizard, then drag columns onto a form to generate bound UI controls. |
| Manage User Secrets | both | Right-click the project (or run `dotnet user-secrets`) to generate a UserSecretsId and open secrets.json in the editor, keeping connection strings and credentials out of source control during local development; can later be promoted to Azure Key Vault for deployment via Connected Services. |
| Save/edit connection strings via Server Explorer | vs | The Data Connections node in Server Explorer stores and edits connection strings, with an Advanced Properties dialog for provider-specific options that get baked into the resulting connection string. |
| SQLCMD variable management | rider | SQLCMD variables used by SQL scripts and database projects can be managed directly from the Databases tool window. |
| Database Projects (.sqlproj) support | rider | Rider added native support for SQL Server Database Projects (.sqlproj), letting you build and manage them inside the same solution as your C# projects, powered by the same DataGrip engine used for connections. |
| SSDT declarative SQL database projects | vs | A SQL database project declares each object (table, view, sproc, etc.) exactly once as T-SQL; building it validates references between objects and validates syntax against a configurable "target platform" (a specific SQL Server/Azure SQL version), producing a .dacpac as the build artifact. |
| SqlPackage/.dacpac publish with diffing | vs | Publishing a .dacpac (via SqlPackage or the in-IDE Publish wizard) to a new database creates objects in dependency order (e.g. parent tables before child FKs); publishing to an existing database diffs source vs. target and emits ALTER-only statements for just what changed. One .dacpac can be republished across many target databases. |
| SDK-style SQL projects (Microsoft.Build.Sql, preview) | vs | A newer, cross-platform .NET 8+ SQL project SDK with NuGet package references between database projects and default globbing for .sql files. Available in Visual Studio 2022 only as a preview "SQL Server Data Tools, SDK-style (preview)" component — Visual Studio 2026 supports only the original .sqlproj format. It's the format used by the VS Code SQL Database Projects extension and Fabric source… |
| SQLCLR objects in SQL projects | vs | Both the original and SDK-style SQL project formats can contain SQLCLR objects, but building a project with SQLCLR objects requires targeting .NET Framework even when the rest of the project is SDK-style/.NET 8; the resulting .dacpac can still be deployed and referenced normally. |
| Customizable SQL build-time code analysis | vs | Building a SQL database project can raise non-fatal warnings for things like inconsistent object-name casing and other best-practice violations; the underlying DacFx library exposes extensibility points to author custom static-analysis rules for SQL projects. |
| Arm64 SSDT limitations | vs | On Arm64 Visual Studio 2026, T-SQL IntelliSense/completion, the T-SQL debugger, and LocalDB connectivity are all explicitly unavailable, unlike the full x64 SSDT experience. |
| EF Core generated-SQL logging surfaced in the Run tool window | rider | Configuring EF Core logging to include the Database.Command category streams the actual generated SQL for each LINQ query into Rider's Run tool window as the app executes. |

---

## Designers and desktop/mobile UI: WPF, XAML, WinForms, MAUI, Avalonia, Unity

| Feature | IDE | What it is |
| --- | --- | --- |
| XAML Hot Reload (WPF/UWP/WinUI/Xamarin) | both | Edit XAML while the app runs under the debugger and see UI changes applied to the live app without a rebuild or restart, preserving app state. Activates automatically when the debugger attaches (Debug > Windows > XAML Hot Reload options in VS). VS has supported this since 2022 v17.0 across WPF, UWP, WinUI 3 and Xamarin.Forms. Rider added the equivalent for WPF only in the 2026.2 EAP (June 2026)… |
| Live Visual Tree | vs | A dockable tool window (Debug > Windows > Live Visual Tree) showing the real-time element hierarchy of a running WPF/UWP/WinUI/MAUI app. Clicking a tree node highlights the element in the running app and vice versa; a 'Just My XAML' toggle filters out framework-internal elements. |
| Live Property Explorer | vs | Paired with Live Visual Tree, shows and lets you edit the actual runtime property values of the selected element (Margin, Visibility, Width/Height, brushes, etc.) with changes applied live to the running app. |
| XAML Live Preview | vs | Docked window (Debug > Windows > XAML Live Preview) that captures a screenshot-based live view of a running desktop/MAUI app's UI, paired with XAML Hot Reload for click-to-edit. Supports zoom/pan, draggable alignment rulers, element selection with size/color/font info tips, and multi-window app switching. Since VS 2022 17.14, available at design-time (not just while debugging) for WinUI 3 and… |
| XAML Live Preview + GitHub Copilot Vision (image-to-XAML) | vs | In the design-time XAML Live Preview for .NET MAUI, you can attach a reference image (e.g. a mockup) and have Copilot Vision generate corresponding XAML, with the preview reflecting the generated markup live. |
| XAML Binding Failures window | vs | Debug > Windows > XAML Binding Failures (or the toolbar 'Binding failures' button, which shows a live count). Surfaces binding errors that normally scroll past in Output, with sort/search/group, and double-click jumps straight to the failing binding in the XAML source. |
| WPF/UWP XAML Designer (drag-and-drop artboard) | vs | Full visual design surface (View Designer) with synchronized Design/XAML split views, snaplines, Grid rails/adorners for row/column sizing, resize/rotate/margin element handles, zoom 12.5%-800%, light/dark artboard toggle, and 'Disable project code' for design-time isolation. Drag controls from the Toolbox, set properties in the Properties window. |
| Document Outline window | vs | Tree view (View > Other Windows > Document Outline, Ctrl+Alt+T) of all elements on the XAML artboard; supports show/hide (Ctrl+H), lock/unlock (Ctrl+L), reordering by drag, and scoping into styles/templates. |
| Properties window with binding/resource markers | vs | Designer Properties window shows a marker box next to each property indicating whether it holds a default value, a local resource, or a data binding (color-coded). Clicking the marker opens the data-binding builder, resource picker, or navigates to a style definition directly. |
| XAML code editor UI (Element/Member dropdowns, split panes) | vs | XAML text editor exposes an 'Element: X' dropdown (navigate to sibling/parent tags) and 'Member: X' dropdown (navigate to attributes/child elements) in its header, plus Vertical/Horizontal Split and Pop Out XAML buttons to detach the editor from the designer pane. |
| XAML Structure Visualizer, #region, snippets, IntelliCode for XAML | vs | Vertical structure guide lines for matching tag pairs; #region/#endregion comment-based collapsible regions (auto-suggested on typing '<!'); built-in XAML snippets (ColumnDefinition, RowDefinition, Setter, Tag, etc.) via Ctrl+K,Ctrl+X; IntelliCode-starred top suggestions when typing '<'. |
| XAML light bulbs / Quick Actions (remove/sort namespaces, rename namespace) | vs | Lightbulb-driven quick fixes in the XAML editor: Remove Unnecessary Namespaces (with Document/Project/Solution scope and diff preview), Remove and Sort Namespaces, and Rename Namespace (Ctrl+R,Ctrl+R) that updates all prefix usages. |
| Go To Definition / Peek Definition for {StaticResource}/{DynamicResource} | both | F12 on a StaticResource/DynamicResource key jumps to its definition in the owning ResourceDictionary (including generic.xaml for theme resources); Alt+F12 peeks it inline. Rider (via ReSharper's XAML support) resolves StaticResource the same way through Go to Declaration/Find Usages, though DynamicResource resolution has had reported gaps in recent Rider versions. |
| Smart Completion for bindings/resources (Rider) | rider | Ctrl+Alt+Space inside a XAML attribute offers context-aware completion to generate a new StaticResource/DynamicResource, a {Binding}, or a reference to another named element, plus event-handler stub generation when invoked in an event attribute. |
| x:Bind-aware navigation and parameter info (Rider) | rider | Find Usages and Go to Declaration understand x:Bind expressions, and Rider gives code completion and parameter info for propertyPath, BindBack, and UpdateSourceTrigger inside an x:Bind. |
| WinForms Designer (.NET Framework, in-process) | both | Classic in-process drag-and-drop WinForms designer: Toolbox, Properties grid, Component Tray, typed-DataSet designer, ActiveX control hosting. Fully supported by VS for .NET Framework WinForms projects; Rider has no visual WinForms designer at all for any target — WinForms UI must be edited as generated code (InitializeComponent) by hand or via VS/Rider's shared Designer.cs partial-class editing. |
| WinForms Designer (.NET Core/.NET 5+, out-of-process) | vs | Because VS 2022 is 64-bit and can't host .NET Core/5+ runtimes or 32-bit assemblies in-process, the designer runs in a separate DesignToolsServer process matching the project's target runtime/bitness, communicating with VS via proxy objects. Supports WYSIWYG surface, Toolbox, Property Browser, Data Binding via Object Data Sources, and full Component Tray parity with custom adorners/snap… |
| WinForms .NET Core designer gaps vs .NET Framework designer | vs | Even in VS, the out-of-process .NET Core designer still lacks some legacy capabilities: no Typed DataSet Designer, limited SQL query editor type-editors, no ActiveX control support (by design), and Tab Order interaction only arrived later (17.1 Preview 3). Custom controls needing rich design-time behavior must be recompiled against the WinForms Designer Extensibility SDK. |
| WinUI 3 / .NET MAUI: no classic XAML Designer, Hot Reload + Live Preview only | both | Microsoft has stated the classic drag-drop XAML Designer is not supported for WinUI 3 or .NET MAUI in VS 2022+; instead developers rely on XAML Hot Reload plus (since 17.14) design-time XAML Live Preview. Rider likewise has no designer/previewer for MAUI XAML at all, relying purely on Hot Reload against a running emulator/device. |
| XAML Studio (open-sourced by Microsoft) | vs | A separate Microsoft tool (not built into VS) for rapid XAML/WinUI prototyping outside a full project; version 2.0 work is in progress with a Fluent-based UI and WinUI 3 component support, following community pressure over the lack of a WinUI 3 designer in VS itself. |
| .NET MAUI XAML Hot Reload (multi-platform) | both | Applies XAML edits to a running MAUI app without full rebuild, preserving UI state. On Windows/VS: Android, iOS and WinUI emulators/simulators/physical devices; on Mac: Android, iOS, Mac Catalyst. Rider supports the same scenarios in debug mode across its supported build targets per host OS (Windows host: Android + Windows desktop only; macOS host: all targets; Linux host: Android only). |
| .NET MAUI Live Visual Tree | vs | Debug > Windows > Live Visual Tree for a running MAUI app shows the UI hierarchy for both XAML- and C#-built UIs (disable 'Just My XAML' to see C#-built trees), with 'Select Element in Running Application', layout adorner overlays, and 'Preview Selected Item' to jump to source XAML. |
| .NET MAUI Hot Restart (iOS, no Mac required) | vs | Deploys a debug MAUI build directly to a locally-connected 64-bit iOS device from Windows without a full app-bundle rebuild or a Mac build host, by pushing incremental changes to the existing bundle. Available through VS 2022 (up to 17.14); Microsoft has stated Hot Restart is not carried into VS 2026. |
| Android Emulator / Device Manager integration | vs | Debug Target dropdown lets you pick an Android Emulator image directly, auto-installing the Android SDK/emulator on first use; Tools > Android > Android Device Manager manages emulator images (create, edit, start) from inside VS. |
| Android device/emulator run configs (Rider) | rider | Rider auto-generates run/debug configurations per target platform/device for a MAUI project, selectable from the toolbar; Android deployment relies on the Android SDK/emulator being separately installed, with a Logcat tool window for device logs. |
| Wireless iOS deployment/debugging via paired Mac | vs | VS can deploy and debug .NET MAUI iOS apps on a physical device wirelessly through a network-paired Mac build host, without a USB cable between the iOS device and the Mac. |
| AvaloniaRider previewer (Rider) | rider | Community/JetBrains-affiliated plugin (ForNeVeR/AvaloniaRider, on JetBrains Marketplace) adding a live AXAML previewer tool window: updates as you edit once the project has been built at least once, mouse interaction, Ctrl+Scroll-wheel zoom, choice of AvaloniaRemote (bitmap) or Html (browser) rendering backends, configurable FPS cap, working-directory and theme-injection settings, plus… |
| Avalonia support in Visual Studio | vs | VS has no built-in Avalonia project templates, previewer, or XAML tooling; Avalonia development on VS relies entirely on the community AvaloniaVS extension (separate from Microsoft), which is comparatively less maintained than AvaloniaRider. |
| Rider built-in XAML Preview (WPF) | rider | A built-in preview pane next to the XAML editor for WPF markup (Tools \| XAML Preview settings control auto-show/position); supports interacting with TabItem tabs in the preview. This is preview-only — not an editable drag-drop design surface — and is WPF-specific per Rider's own docs (no stated UWP/WinUI/MAUI coverage). |
| WPF Hot Reload in Rider (2026.2 EAP) | rider | New feature (June 2026 blog announcement): edit XAML of a WPF app running under Rider's debugger and see resource-dictionary/control-template/style/layout/color changes applied instantly with no rebuild or restart, no separate mode to enable. Complements existing C# Hot Reload for WPF desktop. |
| Localization Manager (.resx) | rider | Ctrl+Shift+A > 'Localization Manager', or right-click a .resx file > Open With Localization Manager. Scans all .resx files in the solution, groups them by resource bundle, and shows all cultures side-by-side in one editable grid for adding/editing resource entries across languages at once. |
| Localization refactorings (Rider/ReSharper) | rider | 'Move to Resource' quick-fix turns a hardcoded string literal into a resource entry (with editable name/value and target file) in one step; 'Move Resource' relocates an existing resource between .resx files; 'Rename Resource' renames a resource key and updates every declaration and code usage; 'Inline Resource' replaces usages with the literal value (optionally removing the entry); 'Safe Delete… |
| .resx grid editor | both | Both IDEs open .resx files in a spreadsheet-like grid editor (Name/Value/Comment columns, tabs for Strings/Images/Icons/Files/Other) for adding and editing resource entries, generating the strongly-typed Resources.Designer.cs accessor class. |
| Image/icon resource preview | both | Both IDEs render a thumbnail/preview of image resources (PNG, ico, etc.) — VS shows previews in the .resx grid editor's Images tab and file-open dialogs; Rider shows an inline image viewer tab (with zoom) when opening an image file directly and thumbnails in resource editors. |
| Unity play-mode control from the IDE | rider | Play, Pause, and Step buttons in Rider mirror the Unity Editor's own play-mode controls and operate the actual Unity Editor session, letting you start/stop/step play mode without switching window focus to Unity. |
| Attach to Unity Editor / Player / Android device | rider | Run configurations 'Attach to Unity Editor', 'Attach to Unity Editor & Play', and manual 'Attach to Unity Process' (by host/IP:port for remote players, or via adb device picker for Android) let Rider's debugger attach to any Unity process; temporary configs from Attach to Unity Process can be reused for quick reattachment. |
| Unity-aware debugger inspection (scene hierarchy, GameObjects, ECS entities) | rider | While debugging a Unity process, Rider's Locals/Watch views render Unity-specific data specially: scene hierarchies, GameObject parent/child relationships, attached Components, and ECS Entity component data, instead of raw .NET object dumps. |
| ShaderLab / HLSL / CG shader editing and debugging | rider | Full code insight (completion, navigation, refactoring) for .shader ShaderLab files, embedded CG/HLSL blocks, shared .cginc/.hlsl includes, and compute shaders, plus source-level shader debugging that steps through shader code and maps back to the original .shader/.cginc source ranges. |
| Unity event-function signature inspections and quick-fixes | rider | Highlights Unity event functions (Update, Start, OnCollisionEnter, etc.) with incorrect signatures/return types and offers an Alt+Enter quick-fix to correct them; flags empty event functions as dead code with a remove quick-fix; generates correct event-function stubs via completion. |
| Unity implicit-usage awareness (solution-wide analysis) | rider | With solution-wide analysis on, Rider understands that fields/methods tagged via Unity attributes or found in UnityEngine/UnityEditor/UnityEngine.Networking assemblies are implicitly used by the engine (e.g. via reflection/serialization) and suppresses false 'unused member' warnings, while still flagging genuinely dead SerializeField values. |
| Redundant/misapplied [SerializeField] inspection | rider | Flags [SerializeField] as redundant when applied to fields that can't actually be serialized (static, readonly, non-serializable containing class) and explains why serialization won't work (e.g. attribute applied to a property instead of a field), with an Alt+Enter fix to remove it. |
| Extended Find Usages across Unity scenes/assets/prefabs | rider | Find Usages is extended beyond C# code to search Unity .scene, .asset and .prefab YAML files for references to a symbol (e.g., a MonoBehaviour type or serialized field), and a Unity Explorer tool window aids asset navigation. |
| Unity Frame Viewer / performance profiling tools | rider | Rider documentation references a Frame Viewer tool and built-in profiling tools for analyzing Unity game performance from within the IDE, alongside .asmdef assembly-definition validation/refactoring, live/file templates for scripts and shaders, and a Unity Editor log window with interactive navigation from log entries to source. |
| Unity pre-commit checks and UnityYAMLMerge integration | rider | Rider can run Unity-aware checks before a commit to catch common Unity-specific mistakes, and integrates Unity's UnityYAMLMerge tool for resolving merge conflicts in scene/prefab YAML assets. |
| Rider for Unreal Engine (RiderLink/UnrealLink, C++ + Blueprint awareness) | rider | Rider opens .uproject files directly with full indexing/navigation across the engine and game code, understands Blueprints and reflection macros (UPROPERTY/UFUNCTION etc.) for completion and refactoring, and pairs the UnrealLink Rider-side plugin with the RiderLink Unreal-Editor-side plugin (auto-installed via Settings > Languages & Frameworks > Unreal Engine) for editor communication (e.g.… |
| C# Hot Reload scope difference (Rider vs VS) for desktop/mobile | both | VS's C# Hot Reload works across WPF/WinForms/UWP/WinUI desktop and, for MAUI, across the same device targets as XAML Hot Reload. Rider's C# Hot Reload for MAUI projects is stated as limited to Windows desktop (WinUI) targets only — not available when running Android/iOS/macOS MAUI targets from Rider. |
| Blend for Visual Studio | vs | A separate design-focused tool shipped alongside VS with an 'Assets' window (Blend's Toolbox equivalent) and an 'Objects and Timeline' window (Blend's Document Outline equivalent) that additionally supports creating storyboard animations directly on the design surface — not available in the plain VS XAML Designer. |
| 'Capitalize event handler method name' XAML editor setting | vs | An XAML text-editor option (Tools > Options > Text Editor > XAML > Miscellaneous), off by default, that enforces proper PascalCase naming when VS auto-generates an event-handler stub from a XAML event attribute. |
| Conditional XAML support (UWP) | vs | IntelliSense and validation for the ApiInformation.IsApiContractPresent-based conditional-XAML syntax, letting UWP developers set properties/instantiate objects in markup gated on API-contract presence, without code-behind branching. |

---

## Assemblies, decompilation, source generators and low-level tooling

| Feature | IDE | What it is |
| --- | --- | --- |
| dotPeek-engine decompiler with fine-grained settings | rider | Rider decompiles third-party/no-source code on the fly using the dotPeek decompiler engine. Settings (Settings \| Build, Execution, Deployment \| Debugger, and the dotPeek-derived options) let you toggle: show XML doc comments, show compiler-generated code, show IL-code as comments alongside the decompiled C#, show metadata tokens, skip methods over 50,000 IL instructions (perf guard), and… |
| Built-in F12/'Go to Definition' decompilation (ILSpy engine) | vs | VS 2022 ships decompilation-on-navigate enabled by default, based on the open-source ILSpy project (engine v7.1 at 2022 RTM). Navigating (F12) into an assembly with no PDB/source shows a generated C# file instead of the 'No Symbols Loaded' placeholder. |
| Auto-decompilation of external code while debugging (17.7+) | vs | Since VS 2022 17.7, stepping into external .NET code (with Just My Code disabled), or double-clicking a frame in the Call Stack window after enabling 'Show External Code', automatically decompiles the containing module and jumps straight to the current execution point — no manual 'Decompile source code' click needed. |
| 'External Sources' Solution Explorer node | vs | All modules decompiled during a debug session are collected under a virtual 'External Sources' node in Solution Explorer, so you can browse previously-decompiled framework/NuGet code like ordinary files, set breakpoints in it, and reopen it without re-triggering decompilation. |
| 'Decompile Source to Symbol File' / 'Extract Source Code' (Modules window) | vs | From the Modules window's context menu on a .NET module you can run 'Decompile Source to Symbol File' to generate a PDB with embedded decompiled source for the whole assembly, then later 'Extract Source Code' to pull those embedded sources out as files (added to Solution Explorer's Miscellaneous Files, which must be enabled under Tools > Options > Environment > Documents). |
| Assembly Explorer tool window | rider | A dedicated window (Tools menu, or the toolbar's Open/Explore Folder/Open from GAC buttons) for loading and browsing arbitrary assemblies, folders of assemblies, GAC contents, or .nupkg files — independent of any open solution. Supports saving/restoring named 'assembly lists', Search-Everywhere-style filtering while focused, and double-click-to-decompile per type/member. |
| Object Browser | vs | Tool window (View > Object Browser, or via Navigate To: Object Browser from a symbol) that lists assemblies, namespaces, types and members as signatures only — no method bodies, no decompiled implementation, no raw metadata. Can browse project output, referenced assemblies, and COM type libraries/TLBs. |
| Assembly & Portable-PDB metadata explorer | rider | Inside Assembly Explorer, a 'Metadata' node exposes the raw contents of the module: metadata tables/blobs/strings, PE headers, Debug/Resource directories, standalone and embedded Portable PDB data, embedded source files, source_link.json, and CustomDebugInformation blobs. 'Navigate To' (Ctrl+Shift+G) → 'Locate in Metadata' jumps from a symbol straight to its metadata-table row. |
| IL Viewer with C#↔IL correlation | rider | Tools \| IL Viewer, or Navigate To (Ctrl+Shift+G) → 'IL Code', opens a dedicated window showing the CIL for the selected member, highlighting the IL block that corresponds to the currently-selected C# statement and vice versa. |
| High-level / Low-level C# code view | rider | From the IL Viewer toolbar (or Navigate To → 'Low-level C# Code') you can flip a member between 'High-level C#' (readable source/decompiled form) and 'Low-level C#' (compiler-lowered form showing what a lambda/iterator/async method actually compiles to — closures as classes, state machines, etc.). |
| No built-in IL/bytecode viewer (Disassembly window is native code only) | vs | VS's Debug > Windows > Disassembly window (Ctrl+Alt+D, requires 'Enable address-level debugging') shows JIT-produced native x86/x64 instructions, not MSIL. Viewing actual IL requires an external tool (ildasm.exe from the SDK/Windows SDK) or a marketplace extension (e.g. the ILSpy 2022 VSIX). |
| Roslyn Syntax Visualizer | vs | A VS extension shipped as part of the .NET Compiler Platform SDK (an optional install component, not on by default) that opens via View > Other Windows > Syntax Visualizer. Shows the live Roslyn SyntaxTree (nodes/tokens/trivia) for whatever C#/VB file has focus, with a properties panel per node and an optional Directed Syntax Graph for a selected subtree. |
| Roslyn Syntax Tree Visualizer | rider | Added in Rider 2025.1: a built-in tool window that live-updates to show the Roslyn syntax tree of the active C# file, with expandable nodes, double-click-to-highlight-in-editor, click-for-node-properties, and search/filter by identifier name or syntax kind (Ctrl+F or Speed Search). |
| Viewing source-generated files | both | Both IDEs surface generator output as read-only files in the project tree. Rider: Solution Explorer's project node → Dependencies → a group per referenced generator listing every generated .cs artifact. VS: Solution Explorer → project → Dependencies → Analyzers → <GeneratorProject> → <GeneratorName>, listing the generated files (e.g. HelloWorld.g.cs), openable read-only. |
| Debugging a source generator itself (DebugRoslynComponent) | both | Both IDEs support the shared Roslyn-SDK convention: add a launchSettings.json to the generator project's Properties folder with "commandName": "DebugRoslynComponent" and a "targetProject" pointing at the consuming project, then hit Debug/the gutter Play button — the IDE launches a build of the target project with the debugger attached to the generator's Execute/Initialize methods so you can… |
| Debugging generated code via Step Into | both | Once generated files are visible in the project tree, you can set a breakpoint on a line inside the generated artifact (or Step Into from calling code) and debug it with full locals/watch/call-stack support, same as hand-written code, in both IDEs. |
| Generator-debugging compiler-toolset version sensitivity | rider | As documented for Rider 2023.1, DebugRoslynComponent-based generator debugging only worked reliably with specific Microsoft.CodeAnalysis.CSharp / Microsoft.Net.Compilers.Toolset versions (4.3.1) pinned in the generator project; newer compiler package versions could break it. This is a known friction point (may have since improved in later Rider releases, not independently re-verified here). |
| Open a raw .nupkg as a browsable assembly set | rider | Assembly Explorer's toolbar 'Open' accepts .nupkg files directly (in addition to .dll/.exe and folders), letting you drill into every assembly, resource, and (via the metadata node) any embedded file the package ships — build/, props/, targets/, tools/, native binaries — without unzipping anything manually. |
| No built-in NuGet package content browser | vs | VS's Manage NuGet Packages UI shows a package's description, dependency list, licenses and target-framework support, and 'Preview Changes' for what will be added to the project — but it does not let you browse the package's actual file contents (dlls per TFM, props/targets, native assets). To inspect contents you rename .nupkg to .zip and open it manually, or install the separate open-source… |
| 'Find Dependent Code' on a NuGet package | rider | Right-click any NuGet package (in the NuGet tool window or Dependencies node) and choose Find Dependent Code to get a usages-style list of every place in your solution that consumes symbols from that package. |
| Implicitly-installed (transitive) package visibility | rider | The NuGet tool window has a dedicated 'Implicitly Installed Packages' section listing transitive dependencies pulled in by your direct references, with a hover tooltip showing the version and which top-level package brought it in. |
| Package Manager Console (PowerShell) | vs | A built-in PowerShell host (Tools > NuGet Package Manager > Package Manager Console) exposing NuGet cmdlets like Install-Package, Update-Package, and Add-BindingRedirect, scriptable and scoped per-project/solution. |
| Automatic / assisted binding redirects (.NET Framework) | vs | For .NET Framework desktop projects (targeting 4.5.1+) using packages.config, VS auto-inserts binding redirects into app/web.config at build time when it detects assembly-version conflicts, and for web projects prompts you to add a redirect when it hits one. The Add-BindingRedirect PMC cmdlet does the same on demand for the whole project's output. |
| Source Link support | both | Both debuggers follow Source Link metadata embedded in a package's PDB to fetch the exact original source file (from GitHub/Azure Repos/etc. at the matching commit) instead of decompiling, including private/authenticated Source Link endpoints. VS: Tools > Options > Debugging > 'Enable Source Link support' (plus 'Enable Just My Code' interplay). Rider: Settings > Build, Execution, Deployment >… |
| Configurable symbol server list | both | Both let you add arbitrary symbol server URLs/local paths, control download-from-remote behavior, and set a local symbol cache directory. VS: Tools > Options > Debugging > Symbols (defaults include the Microsoft Symbol Server; NuGet.org's symbol server can be added). Rider: Settings > Build, Execution, Deployment > Debugger > Symbol Servers, which accepts both remote URLs (e.g.… |
| Modules window with Symbol Status | both | Both expose a Modules window (Debug > Windows > Modules in VS; Rider's Debug tool window 'Modules' tab) listing every loaded module with a symbol-status column (loaded / skipped / no PDB / disabled), a path to the resolved PDB when loaded, and a right-click 'Load Symbols' action to retry manually. |
| 'Symbol Load Information' diagnostic dialog | vs | Right-click a module in the Modules window → 'Symbol Load Information' opens a dialog enumerating every path/server the debugger actually probed for that module's PDB and why each attempt failed or succeeded. |
| 'View in Assembly Explorer' from Modules | rider | Right-clicking a loaded module in Rider's Modules view offers 'View in Assembly Explorer', jumping straight from a live debug session's loaded module into the full metadata/decompile browsing experience for that exact binary. |
| Strong-name signing UI (Signing project-properties page) | vs | Project Properties > Signing tab has a dedicated 'Sign the assembly' section: pick or create a .snk key file, toggle delay-signing, all without touching the csproj/AssemblyInfo by hand. (The same tab also handles ClickOnce manifest signing via certificate store or .pfx, separately from strong-naming.) |
| No dedicated strong-naming UI | rider | Rider has no Signing designer page equivalent to VS's. Strong-naming a project means hand-editing the csproj (<SignAssembly>, <AssemblyOriginatorKeyFile>) or shelling out to sn.exe/dotnet-based key tooling yourself; Rider's newer Project Properties editor (Application/Build/Debug/Package tabs) does not include a Signing tab. |
| SuppressIldasm override for 'protected' assemblies | both | Both decompilers honor SuppressIldasmAttribute by default (refusing to decompile) but expose a setting to override it. VS: honors the attribute with no UI override noted in docs (attribute is obsolete since .NET 6 but VS still respects it). Rider: Settings > ...>Symbol Servers has an explicit 'Decompile protected methods (ignore SuppressIldasm attribute)' checkbox. |
| Per-file multi-targeting context switcher | both | When a source file is compiled under more than one TFM (a multi-targeted csproj), both IDEs let you pick which target framework's compilation context to analyze/IntelliSense against for that file, so #if directives and TFM-conditional APIs resolve correctly instead of showing false errors. |
| 'Change .NET SDK version' solution action + global.json authoring | rider | Right-click the solution → an SDK-version action opens a dialog listing every .NET SDK Rider detected on the machine; picking one writes/updates global.json's sdk.version for you (built into Rider since 2018.1.1, no plugin required). |
| Toolset and Build settings (pick MSBuild/dotnet executable) | rider | Settings > Build, Execution, Deployment > Toolset and Build lets you point Rider at a specific MSBuild or dotnet CLI executable/version to use for builds and design-time builds, independent of whatever SDK global.json resolves for command-line dotnet. |
| VS-managed .NET SDK install via the VS Installer | vs | VS 2022 bundles and services its own copy of the .NET SDK through the Visual Studio Installer's workloads/individual components, and honors a project's global.json when loading a solution (falling back per the standard rollForward/matching rules). There's no dedicated 'pick SDK for this solution' dialog inside the IDE itself, though — SDK selection is effectively managed at the machine/installer… |
| global.json rollForward policy awareness | both | Both IDEs' build/run pipelines honor whatever rollForward policy (patch/feature/minor/major/latestPatch/latestFeature/latestMinor/latestMajor/disable) is set in global.json, since SDK resolution happens in the shared .NET SDK resolver — but neither IDE offers a form/dropdown UI to author the rollForward key itself; it's a hand-edited JSON value in both. |
| Native, first-party F# tooling | vs | VS ships officially-maintained F# tooling: F# project templates, the F# Interactive (FSI) window for REPL-driven development, debugging, and IntelliSense/refactoring maintained as a first-class language alongside C#/VB. |
| Bundled but lighter F# support | rider | Rider ships F# support as a JetBrains-maintained plugin bundled by default in the installation (not a separate marketplace install), providing syntax highlighting, completion, an FSI window, and debugging — but with noticeably fewer analyzers/quick-fixes and refactorings than Rider's ReSharper-powered C# experience, and generally regarded as behind VS's F# tooling in completeness. |
| Full-parity VB.NET tooling | vs | VB.NET in VS gets the same Roslyn-backed IntelliSense, refactorings, WinForms/WPF designer support, and analyzer tooling as C# — VB is a fully first-class language in the IDE, not a reduced mode. |
| Minimal VB.NET editing support | rider | Rider can load, build, and run VB.NET projects (via the Roslyn VB compiler) but does not extend its ReSharper-grade code-analysis/refactoring engine to VB the way it does for C# — completion and inspections for VB.NET are comparatively baseline. |
| IntelliTrace historical debugging (Enterprise-only) | vs | VS Enterprise-exclusive: IntelliTrace records a timeline of app events and (with 'IntelliTrace events and calls' mode) call information as you debug, letting you step backward through execution history — including calls made into external/framework/no-symbol code — without having pre-set a breakpoint there. |
| Architecture / Code Map dependency diagrams (Enterprise-only) | vs | VS Enterprise-exclusive Architecture tools can generate a live 'Code Map' showing dependency relationships between types/assemblies/projects, letting you visually trace what an assembly depends on or is depended on by, right-click-drag from code into the diagram. |
| Open assemblies from the Global Assembly Cache | both | Both let you browse and open GAC-registered assemblies directly rather than hunting for the physical DLL path. Rider: Assembly Explorer toolbar's 'Open from GAC'. VS: reachable via Object Browser's assembly picker / Add Reference dialog against the GAC (primarily relevant to .NET Framework projects, since .NET (Core)+ doesn't use a GAC). |
| No licensing-tier split for these features | rider | Unlike VS's Community/Professional/Enterprise split (which gates IntelliTrace, Code Map, and some CodeLens indicators behind Enterprise), every feature described above for Rider — Assembly Explorer, IL Viewer, metadata explorer, Syntax Tree Visualizer, generator debugging — ships in the single paid Rider product with no higher tier to unlock. |

---

## Version control, collaboration and general IDE workflow

| Feature | IDE | What it is |
| --- | --- | --- |
| Pre-commit checks in the Commit tool window | rider | Rider's Commit tool window (Alt+0) runs configurable checks before a commit completes: Reformat code, Rearrange code, Optimize imports, run a full Cleanup profile, run code analysis, and even 'Check TODO' or run an external tool. Configured under Settings \| Version Control \| Commit. Commit message inspections validate message quality and 'Clear initial commit message' stops the box from… |
| Git Changes window with line (hunk) staging | vs | Visual Studio's Git Changes window (View > Git Changes) tracks the working tree. Since 17.3, hovering a changed line's color margin opens a Peek Difference UI (Alt+F8) where you can 'Stage Change' for that specific hunk; since 17.6 you can unstage line-by-line the same way. The Diff editor (Ctrl+Alt+Home) offers the same staging controls full-screen. |
| Branches popup and Git widget | rider | The status-bar Git widget opens a Branches popup that auto-groups local/remote branches and tags into expandable nodes (branches with '/' in the name become folders), shows up to five Recent branches, and lets you star branches as Favorites to pin them to the top. New Branch, New Branch from Selected, and New Branch from a Log commit are all one click away. |
| Multi-repository support (up to 25 active repos) | vs | Since VS 2022 17.4, a single solution or an opened folder can activate up to 25 Git repositories at once. The Git Changes window separates pending changes by repo, the status-bar branch picker and Git Repository window let you branch/merge/rebase/rename/delete per-repo, and a single push/pull network-operations dialog lets you reorder and target pushes across repos. |
| Cross-repo branch operations | rider | For solutions backed by more than one Git root, Rider's branch operations dialogs expose an 'Execute branch operations on all roots' toggle so checkout/create/merge apply to every repo in the project simultaneously rather than one at a time. |
| In-memory interactive rebase (Edit Project History) | rider | Git \| Edit Project History (or right-click a range of commits in the Log tab) opens interactive rebase: reorder, reword, squash, drop, or 'Extract to Separate Commit'. As of 2026.1, JetBrains IDEs build the new commit tree in memory using Git plumbing (git cat-file/commit-tree/update-ref/merge-tree) instead of touching the working tree, cutting rebase time on large repos from tens of seconds to… |
| Git Repository window: squash, cherry-pick, revert, reset | vs | The Git Repository window (Ctrl+0, Ctrl+R) gives a full history graph. Multi-select commits + right-click 'Squash Commits' merges history without leaving the UI; right-click any commit for Cherry-Pick, Revert (creates an undo commit, safe for shared branches), or Reset > Delete Changes (--hard). Amending the last commit is a one-click 'Edit' + 'Amend' on the commit-details pane, or the Amend… |
| Merge Editor for conflict resolution | vs | Unmerged files listed under 'Unmerged Changes' in Git Changes open a three-way Merge Editor: check boxes to pick left/right per conflict, or press F10 (Take Incoming) / F11 (Take Current) to accept a whole side, or hand-edit the Result pane. The gear menu switches between side-by-side and vertical (inline) layouts. 'Accept Merge' finalizes the file; the window respects a `.gitconfig`-configured… |
| AI-assisted conflict resolution | rider | JetBrains AI Assistant can be invoked from the conflict resolution view to automatically merge conflicting changes based on the surrounding code context, rather than requiring the developer to pick a side line-by-line. |
| Git Blame (Annotate) with commit drill-down | vs | Right-click in the editor or on a file in Solution Explorer and choose Git > Blame (Annotate) to add a left-margin gutter showing author, date, and a link per line. Clicking a blame entry opens 'View details' - the full commit with all changed files and a compare-to-previous option. Falls back gracefully with clear guidance (use View History instead) for untracked, binary, or huge files. |
| Annotate with Git Blame gutter | rider | Right-click in the editor gutter or a file and choose Annotate with Git Blame to show per-line author/date/commit-message annotations inline, with a popup for the full commit diff. Colors group lines by commit age/author. Works alongside File History (right-click > Git > Show History) which lists every revision of the file with diffs against any prior state. |
| Shelve/Unshelve changes | rider | VCS \| Shelve Changes lets you selectively pick which modified files (not necessarily all of them) to set aside into a named, reusable shelf entry, then Unshelve later into an existing or new changelist - even onto a different branch. This is distinct from `git stash`, which is all-or-nothing and only accessible through Git itself; Rider surfaces both shelve and native stash side by side in the… |
| Patches: create and apply | rider | Any changelist, shelved change, or set of commits can be exported to a portable `.patch` file (VCS \| Create Patch) and applied to a different checkout (VCS \| Apply Patch), including patches received outside of Git (e.g. over email/Slack). |
| Smart Checkout (auto shelve/unshelve on branch switch) | rider | Checking out a branch with uncommitted local changes offers 'Smart Checkout': Rider shelves the current changes, switches branch, then unshelves and re-applies them (prompting on conflicts) instead of forcing a manual stash/pop cycle. Rider can also restore open editor tabs and breakpoints tied to the branch you switch back to. |
| Multi-branch history graph view | vs | Since 17.7, selecting multiple branches in the Git Repository window overlays their commit graphs with distinct line colors and branch-name labels. Toolbar toggles include Show First Parent Only, Show Local/Remote Branches, Show Tags, and (17.14+) Show Outgoing/Incoming Only to filter the graph to just unsynced commits. |
| Changes summary (unified multi-file diff) | vs | From View Branch History, opening a commit and selecting 'Open changes summary' shows every changed line across every file in that commit merged into one scrollable unified view, instead of paging file-by-file. |
| Copy GitHub/Azure DevOps Permalink | vs | Since 17.12, selecting code and right-clicking > Git > Copy GitHub Permalink (or Copy AzureDevOps Permalink) generates a permanent, commit-pinned URL to that exact code range, usable from the editor or from the embedded commit-details view in the Git Repository window. Requires being signed in to the hosting provider. |
| Git tags in the Git Repository window | vs | Since 17.13, tags appear alongside Branches in the Git Repository window's left pane; selecting one navigates to the repo state at that tag (e.g. `v1.0`). |
| Search and link GitHub Issues / Azure DevOps work items | vs | Since 17.6, the commit message box can search for and insert references to GitHub Issues or Azure DevOps work items directly (auto-linking `#123` style references), and issues/PRs can be referenced back from the hosting provider. |
| GitHub pull request review inside the IDE | rider | Git \| GitHub \| View Pull Requests opens a filterable/sortable PR list (by state, author, label, assignee, review status, activity). Checking out a PR branch enters Review mode: changed lines get pink gutter markers in the editor itself (not just the Diff tab, Ctrl+D/F4), gutter '+' adds single comments or starts a multi-comment review with suggestion blocks (```suggestion), and Submit lets you… |
| Pull request review in the Git Repository window | vs | Selecting Pull Requests in the Git Repository window's left pane (or Git > GitHub/Azure DevOps > View Pull Requests) opens an embedded review view without checking out the branch: description, changed files, commits, related work items, and reviewers in one pane; inline comments with reply/resolve directly in the diff; and Approve/vote, status-check visibility, and Complete/Merge (with… |
| Native Azure DevOps pull request integration | rider | Rider 2026.2 added native Azure DevOps PR support (Create-and-complete-Azure-DevOps-pull-requests / Work-with-Azure-DevOps-pull-requests docs): list, filter, review, and manage Azure DevOps PRs without leaving the IDE, on top of the long-standing separate 'Azure DevOps Integration' plugin from the JetBrains Marketplace for older versions. |
| GitLab merge request support | rider | Rider ships GitLab integration for browsing and working with merge requests, pipelines, and snippets against a GitLab remote, alongside its GitHub and Azure DevOps support. |
| GitHub Copilot code review | both | Since September 2025, Copilot code review works in both IDEs: stage your changes, click the Copilot code review button (Git Changes toolbar in VS; commit view in Rider/other JetBrains IDEs), and Copilot flags logic, security, and performance issues inline before you open a PR - the same self-review capability VS previously had, now extended to JetBrains IDEs. |
| AI-generated commit messages | both | Rider: in the Commit window (Alt+0), the AI Assistant button generates a message from the staged diff, using a prompt template you can customize with variables like $GIT_BRANCH_NAME. Visual Studio: a sparkle-pen 'Add AI Generated Commit Message' button in Git Changes calls GitHub Copilot to summarize staged file changes into a message (requires enabling Tools > Options > GitHub > Copilot chat >… |
| AI commit/PR summarization and PR description generation | rider | Beyond commit messages, AI Assistant can explain what a single commit or a range of commits did in plain language, auto-generate titles/descriptions when opening a pull or merge request, auto-title shelved changelists, and (via the GitHub plugin) summarize incoming PRs with adjustable verbosity/tone. |
| Local History (VCS-independent) | rider | Rider continuously and automatically snapshots file state as you edit, run tests, or deploy - independent of Git entirely. Right-click a file or folder > Local History \| Show History opens a diff-capable timeline that can restore a deleted file, recover a code fragment, or roll back a whole file; Local History \| Put Label lets you manually bookmark a point in time. It is not a replacement for… |
| Code With Me (collaborative editing) - being sunset | rider | Code With Me lets a host share a live Rider session (shared cursors, optional terminal/debug access, voice call) for pair programming without either party pushing branches. JetBrains announced in March 2026 that Code With Me is being discontinued: unbundled into a separate plugin at the 2026.1 release (no more new features), a security-update-only transition period through Q1 2027, and full… |
| Visual Studio Live Share - maintenance mode | vs | Live Share provides real-time collaborative editing plus shared debugging sessions, shared terminal instances, localhost port forwarding, and voice calls, while each participant keeps their own theme/keybindings/cursor. As of the current docs, Live Share is officially 'in maintenance mode, with no additional features planned' - existing capabilities (including joining from a browser with no… |
| AI Assistant chat, completion, and multi-step coding agents | rider | JetBrains AI Assistant provides context-aware chat (attach files/folders/symbols/commits as context), inline autocomplete plus 'next edit suggestions', and can hand off multi-step tasks to coding agents - Junie (JetBrains' own agent), plus Claude Agent, Codex, or GitHub Copilot - which plan, edit files, run commands/tests, and report back for review/rollback. Configurable via JetBrains AI… |
| Junie async GitHub PR management | rider | Since Rider 2025.2, Junie can connect to GitHub (Early Access) to open, update, and manage pull requests as a background/async workflow without the IDE being open, in addition to its in-IDE agent mode (write/refactor code, generate files, run tests, deep MCP-based context awareness, ~30% faster than its prior version). |
| IDE-as-MCP-server for external AI agents | rider | Rider can run as an MCP server with one-click setup, letting external AI clients (Claude, Codex, other MCP-aware tools) trigger Rider's own unit-test runner, refactoring engine, or code generation as tools - i.e. the IDE becomes a capability provider to outside agents, not just a host for JetBrains' own AI. 2026.2 extends this with agent 'skills' like a profiler-analysis skill… |
| GitHub Copilot native integration | vs | GitHub Copilot ships built into Visual Studio with native OAuth sign-in (2026.2 update removes separate extension sign-in friction), inline completions, Copilot Chat (including repo-aware chat and 'Add pull requests to Copilot Chat' workflows), and agent mode that can plan and execute multi-file changes. |
| Terminal emulator with AI agents and command completion | rider | Rider's built-in terminal supports multiple tabs and split panes, remembers tabs/working directories/shell history across restarts, treats file paths as clickable hyperlinks, and accepts drag-and-drop from the project tree. Toolbar buttons launch AI coding-agent sessions (Junie, Claude Code, Codex) directly in a terminal tab. The 'Reworked' 2025 terminal engine adds real completion (not just… |
| Integrated Terminal window | vs | View > Terminal opens a tool-window terminal supporting PowerShell, cmd, WSL, and Developer Command Prompt/PowerShell profiles, replacing the older Package Manager Console as the default shell surface, including a documented workflow for debugging Linux console apps through it. |
| Tasks & Contexts issue-tracker integration | rider | Settings \| Tools \| Tasks \| Servers connects to YouTrack, Jira, GitHub, GitLab, Trello, Asana, and generic/custom trackers (username/password or API token). Opening a task via the Open Task dialog can auto-create a matching changelist and/or feature branch from a configurable naming template (e.g. issue 'DSGN-0001 Add new icon' becomes branch dsgn-0001add-new-icon), and a bundled Time Tracking… |
| Azure Boards / work-item linking | vs | Visual Studio's issue-tracker story is narrower and centered on Azure DevOps: work items surface via Team Explorer/Azure Boards panes and can be searched and linked from commit messages (see the GitHub Issues/ADO work item linking feature above); there is no equivalent to Rider's generic multi-tracker task server model or automatic per-task branch/changelist creation. |
| Remote Development via JetBrains Gateway | rider | A thin JetBrains Client connects over SSH (from the Toolbox App, Rider's welcome screen, or Gateway) to a full Rider backend running on a remote host or inside a dev container, giving 'full access to all IDE features' remotely - source code never has to live on the local disk. Supports launching dev containers on the remote machine for any project with a devcontainer config or a cloned Git repo,… |
| Remote SSH / dev container support | vs | Visual Studio's remote/container story is narrower and split by language: SSH remote development to a Linux machine is documented for C++ projects specifically, and native Dev Container support (added 17.4) likewise only covers C++ - for .NET/C# projects there is no built-in dev-container workflow, only the separate 'Container Tools' feature for containerizing an app to run/debug locally against… |
| Settings Sync | rider | Signing in with a JetBrains Account syncs UI theme/keymap/live templates/inspection profiles/most Settings pages, and installed plugins, across machines, with a UI to review and resolve conflicts between two machines' settings. |
| Synchronized Settings | vs | Signing in with the same account syncs a defined, narrower set: the chosen Development Settings profile, user-defined command aliases, custom window layouts, and specific Tools > Options pages (Environment > General theme/casing, Fonts and Colors, Keyboard shortcuts, Tabs and Windows, StartUp, Text Editor code-style pages, XAML Designer). It's on by default; a per-machine checkbox under… |
| Keymap schemes | rider | Settings \| Keymap ships with selectable built-in schemes including Visual Studio, Visual Studio Code, IntelliJ IDEA Classic, ReSharper, Eclipse, and others, with every action individually rebindable and exportable/importable as a scheme. |
| Keyboard shortcut customization | vs | Tools > Options > Environment > Keyboard lets you remap any command, filtered by scope/context, save/load full shortcut mapping schemes, and reset to default; a separately maintained 'Visual Studio 2022 Keymap' plugin exists on the JetBrains Marketplace for people going the other direction (Rider users who want VS muscle memory), which underscores that VS itself doesn't ship a large library of… |
| JetBrains Marketplace plugin ecosystem | rider | plugins.jetbrains.com/rider lists Rider-compatible plugins spanning VCS providers (GitLab, Azure DevOps Integration), languages, frameworks, and productivity tools; because Rider shares the IntelliJ Platform, it can also run many plugins written primarily for IntelliJ IDEA/WebStorm/etc. when they don't depend on JVM-specific APIs. |
| Visual Studio Marketplace extension ecosystem | vs | marketplace.visualstudio.com hosts VSIX extensions specific to the Visual Studio family (distinct from the separate VS Code marketplace); popular examples for this domain include third-party Git blame/history extensions (e.g. 'Git Ranger', 'Better Git Line Blame') that predate or extend VS's native Git tooling. |
| Accessibility: screen readers, high contrast, audio cues | vs | Visual Studio is tested against NVDA, JAWS, and Windows Narrator; ships a 'Dark (Extra Contrast)' theme plus standard Windows High-Contrast mode support; the Show Line Annotations command set helps keyboard/screen-reader users discover and navigate error squiggles, light bulbs, and bookmarks; and (since 17.3) configurable audio cues can play when the caret lands on an error, breakpoint, warning,… |
| Performance on large solutions | rider | Rider 2026.2 reports branch switching on Roslyn-backed solutions running 2-3x faster, backend processes using 7-8% less memory, and (for game-dev users) C++ indexing for Unreal Engine projects roughly twice as fast versus 2026.1; separately, third-party 'shared indexes' tooling (e.g. via Coder) can pre-build and distribute JetBrains project indexes across a team to cut cold-start indexing time. |
| Native Roslyn workspace loading | vs | Visual Studio hosts the same Roslyn workspace APIs natively rather than through a bridge, so project/solution load and background analysis run in-process; there's no separate 'indexing' phase exposed to the user the way JetBrains IDEs surface one, though very large solutions can still show a background 'Loading...'/IntelliSense-warming period before full feature fidelity (Find All References,… |
| Feedback Tool / Report a Problem | vs | The feedback icon (or Help > Send Feedback > Report a Problem) opens Developer Community pre-authenticated, where you write a description, search for existing duplicates, optionally attach a cropped screenshot, and can 'Start recording' to capture a repro (screenshots on every click plus trace/heap-dump files) - all marked visible only to Microsoft engineers. Tickets track through a public state… |
| Usage-data sharing controls | rider | Settings \| Appearance & Behavior \| Data Sharing exposes two independent checkboxes - 'Send anonymous usage statistics' and 'Send detailed code-related data' - both off by default in release builds (on by default in EAP/Preview, and mandatory/non-configurable on non-commercial licenses). JetBrains documents that it excludes actual source code, search inputs, or queries, rounds numeric… |

---

## Cross-platform support and remote/containerised development

| Feature | IDE | What it is |
| --- | --- | --- |
| Native cross-platform desktop IDE | rider | Rider ships as a genuinely native app for Windows, macOS (Intel and Apple Silicon, native since Rider 2021.3), and Linux (separate x64 and ARM64 tarballs). |
| VS 2022 is Windows-only; VS for Mac retired | vs | Visual Studio 2022 installs and runs on Windows only. Visual Studio for Mac was formally retired on August 31, 2024 under Microsoft's Modern Lifecycle Policy and receives no further servicing. |
| VS Code + C# Dev Kit as the cross-platform editor | vs | VS Code and the C# extension/C# Dev Kit install and run natively on Windows, macOS, and Linux (x64 and ARM64) — this, not VS 2022, is Microsoft's actual cross-platform C# tool. |
| JetBrains Gateway thin-client/backend split | rider | JetBrains Gateway is a free launcher: it downloads a full IDE backend onto the remote host and connects a lightweight local 'JetBrains Client' to it over an encrypted RD-protocol connection. Indexing, build, run, debug and test all execute on the backend host, not the client. |
| Rider remote backend host requirements | rider | The SSH remote backend must be a recent glibc-based Linux AMD64 distro (e.g. Ubuntu 16.04+, RHEL/CentOS 7+); Alpine (musl libc) is explicitly unsupported. Recommended minimums: 2+ cores, 4GB+ RAM, 5GB+ disk, with SFTP enabled. Raspberry Pi and shared web hosting are called out as unsupported. |
| Toolbox App broadens Gateway targets to Windows/macOS | rider | Beyond the core Gateway SSH/Linux flow, connecting via the JetBrains Toolbox App adds support for Windows and macOS remote servers, plus built-in integrations with cloud dev-environment providers (Coder, Gitpod, GitHub Codespaces, JetBrains Space). |
| Rider remote dev still requires a Rider license | rider | JetBrains Gateway itself is free, but the remote IDE backend checks for a valid license (paid subscription or the free non-commercial license) held on the local client machine; no license data is stored on the remote host. |
| Rider Dev Containers (devcontainer.json) | rider | Gateway can build and connect to a container defined by a standard devcontainer.json — including a customizations.jetbrains.plugins block to pre-provision plugins — and supports Docker Compose-based multi-container devcontainers for dependent services like databases. |
| Rider Dev Container limitations | rider | Windows-based container images are not supported; you cannot start a Dev Container from a devcontainer.json while already inside an SSH-backend remote session (workaround: create a Docker-SSH connection and git-clone instead); and XDG_CACHE_HOME/XDG_CONFIG_HOME/XDG_DATA_HOME are reserved by the backend and can't be reassigned via devcontainer.json. |
| Rider WSL2 support via the remote-dev pipeline | rider | Since Rider 2022.3, WSL2 is treated as a remote-development target using the same Gateway/backend machinery as SSH — you browse, build, run, and debug a WSL2-hosted solution as if it were local. |
| Rider Docker/Docker Compose run-and-debug | rider | For .NET (Core) projects (not .NET Framework), Solution Explorer > Add > Docker/Docker Compose File generates the container files and a matching run configuration; Rider then builds, runs, and attaches its debugger inside the resulting container or multi-service compose solution directly from the IDE. |
| Rider Attach to Process against Docker containers | rider | Run \| Attach to Process can target a configured Docker connection (local or via SSH to a remote Docker host) and lists the dotnet processes running inside containers there for one-click attach. |
| Rider Kubernetes debugging is limited | rider | JetBrains support threads describe Rider's ability to attach to a specific pod/container inside a Kubernetes cluster as limited, with open requests to make targeting a container easier; there is no built-in traffic-redirection ('debug locally against a live cluster') feature. |
| Rider remote native-process debugging (2025.2) | rider | Rider 2025.2 added the ability to debug native (C++) processes on a remote machine using the same remote-dev machinery already used for remote .NET debugging (Windows targets). |
| Rider plugins install 'On Host' for remote sessions | rider | Plugins for a remote backend are installed specifically into that remote environment and appear labeled 'On Host' in the plugin list, separate from plugins in the local JetBrains Client — plugin state does not automatically follow you between local and remote. |
| C# extension declares extensionKind: workspace | vs | The ms-dotnettools C# extension's package.json sets "extensionKind": ["workspace"], meaning under Remote-SSH, WSL, and Dev Containers it (and C# Dev Kit, which depends on it) is installed and executed on the remote/container/WSL side, not in the local UI process. |
| remote.extensionKind override + Show Running Extensions | vs | Users can override where any extension runs via the remote.extensionKind setting (e.g. force ["ui"] for testing), and the 'Developer: Show Running Extensions' command reveals whether a given extension is actually executing locally or remotely. |
| Nested remote unsupported: devcontainer from inside Remote-SSH | vs | VS Code does not support opening a devcontainer.json from within an already-open Remote-SSH session (tracked as vscode-remote-release#2994, open since May 2020); the workaround is to SSH in, use the Docker CLI on that host, and reopen directly via a Dev Containers connection. |
| GitHub Codespaces ships C# Dev Kit free regardless of team size | vs | C# Dev Kit is included at no extra charge in GitHub Codespaces and Microsoft Dev Box, independent of the 5-developer commercial cap that applies to standalone use. |
| C# Dev Kit commercial licensing tiers | vs | C# Dev Kit is free for individual/personal, academic, and OSS use, and free for commercial teams of up to 5 developers; organizations with 6+ developers require a Visual Studio Professional or Enterprise subscription per user. It requires C# extension v2.0+ and needs write access to its own extension folder (won't run on fully read-only OS setups). |
| C# Dev Kit does not support .NET Framework | vs | C# Dev Kit explicitly does not support .NET Framework projects — only .NET (Core)/.NET 5+ web apps, console apps, class libraries, and test projects. |
| GitHub Codespaces default .NET dev container + prebuilds | vs | Codespaces' default image (mcr.microsoft.com/devcontainers/dotnet) preinstalls the current .NET SDK plus git/ssh/rsync/curl/wget; devcontainer.json can layer features, forwardPorts, and a postCreateCommand (e.g. dotnet restore) so a fresh Codespace is build-ready on first open. |
| VS 2022 Container Tools (Docker only, Windows host) | vs | VS 2022's Container Tools (backed by Docker Desktop) support ASP.NET, ASP.NET Core, and .NET Core/.NET Framework console projects; you can add either a Dockerfile or use the .NET SDK's built-in container build, plus Docker Compose or Service Fabric orchestration. Podman support was added only in the next major version (branded 'Visual Studio 2026'), not VS 2022. |
| VS 2022 Containers window | vs | Ctrl+Q > 'containers' opens the Containers window, listing running containers/images and letting you inspect env vars, logs, port mappings, and the container filesystem, attach the debugger, or open an in-container terminal — all without leaving the IDE. |
| VS 2022 Attach to Process: Docker (Linux Container) | vs | Debug > Attach to Process (Ctrl+Alt+P) offers a 'Docker (Linux Container)' connection type that can target local Docker Desktop, a remote Docker daemon over TCP, or a remote host over SSH (needs openssh-server + SFTP enabled) to list and attach to dotnet processes inside Linux containers — including containers running under WSL, if that WSL instance runs its own SSH server. |
| VS 2022 Attach to Process: Docker (Windows Container) | vs | A separate 'Docker (Windows Container)' connection type attaches to processes in Windows containers; remote targeting is Docker-daemon-only (SSH is unavailable for this connection type), and the target process architecture must match the container's. |
| VS 2022 Attach to Process over plain SSH (no container) | vs | The same Attach to Process dialog has a plain 'SSH' connection type (via Connection Manager, user@host) for attaching to .NET Core/.NET 5+ processes, or native code via GDB/LLDB, on any Linux box — bare metal or VM, container or not. |
| VS 2022 first-class WSL launch profile (F5 into WSL) | vs | ASP.NET Core projects get a generated 'WSL' launch profile (commandName: WSL, distributionName, environmentVariables) so pressing F5 builds, runs, and debugs the app directly inside a chosen WSL distro, auto-detecting and offering to install missing dependencies in that distro. Requires the ASP.NET/web or .NET Desktop workload with WSL2 support enabled at install time. |
| VS 2022 opens projects directly from the WSL filesystem | vs | VS 2022's file picker can open projects stored inside WSL directly via the \\wsl$\<distro>\... UNC path, with no extra extension required. |
| Remote Docker hosts unsupported by VS's Compose orchestration | vs | Microsoft's own docs state plainly that 'Remote Docker hosts are not supported in Visual Studio tooling' for the F5 Docker Compose orchestration feature — as distinct from the separate Attach-to-Process-over-SSH/remote-daemon path, which does reach remote hosts. |
| Bridge to Kubernetes retired for both toolchains | both | Bridge to Kubernetes (successor to Azure Dev Spaces, retired Oct 31 2023) — the tool that redirected live cluster traffic to a locally-debugged service from VS or VS Code — was itself retired April 30, 2025 and pulled from both marketplaces; Microsoft now points users to community tools like mirrord or Telepresence. |
| vsdbg is proprietary and EULA-locked to Microsoft-branded IDEs | vs | Microsoft's own cross-platform .NET debugger (vsdbg) is closed-source; its EULA permits use only 'with Visual Studio Code, Visual Studio, or Visual Studio for Mac' — specifically the Microsoft-distributed VS Code build, not community/OSS builds or any third-party IDE. |
| Rider built its own proprietary .NET debugger instead of vsdbg | rider | After Microsoft pulled the NuGet package Rider's early debugger depended on (once it became clear it wasn't licensed for third-party IDEs), JetBrains built and now maintains its own closed-source .NET debugger backend, used uniformly across local, remote-SSH, WSL, and container debugging instead of vsdbg. |
| netcoredbg: the open-source debugger alternative | both | Samsung's netcoredbg (MIT-licensed, github.com/Samsung/netcoredbg) implements GDB/MI, VS Code DAP, and CLI interfaces for CoreCLR, and is what non-Microsoft-branded C# tooling actually uses precisely because vsdbg can't be — e.g. community/free forks of the VS Code C# extension, and a MonoDevelop netcoredbg addin. |
| .NET Install Tool handles SDK discovery/acquisition for VS Code | vs | The ms-dotnettools.vscode-dotnet-runtime extension parses `dotnet --info` output to discover installed SDKs/runtimes, and can silently acquire a missing SDK/runtime channel on behalf of C# Dev Kit or Polyglot Notebooks, independent of any system-wide install. |
| Filesystem case-sensitivity mismatch across OSes | both | Windows and (by default) macOS filesystems are case-insensitive, while native Linux/WSL2 ext4 is case-sensitive; both Rider and VS Code simply inherit whatever the underlying OS/container provides rather than normalizing it, so a project that restores/builds fine on Windows or macOS can fail on a Linux remote host or Linux container purely from mismatched using-directive or path casing. |
| WSL: split client/server install, per-distro extension installs | vs | VS Code's WSL support splits into a Windows-side UI client and an independent VS Code Server installed inside each WSL distribution; workspace-kind extensions like C# Dev Kit must be (re-)installed per distro, and files can live in the Linux filesystem or under /mnt/c, with the Linux-native path recommended for performance. |
| Rider Linux ARM64: separate, historically rockier build | rider | JetBrains ships a distinct Linux ARM64 tarball for Rider/ReSharper (separate from the AMD64 build); early ARM64 attempts hit 'Unsupported OS/arch: Linux/aarch64' errors per JetBrains' own YouTrack tracker, in contrast to macOS Apple Silicon, which has been fully native since Rider 2021.3. |
| vsdbg only licensed for the Microsoft-built VS Code binary | vs | Even though VS Code itself is open source and can be rebuilt by anyone (e.g. VSCodium), vsdbg's EULA restricts it to the Microsoft-distributed VS Code binary specifically — community rebuilds cannot legally bundle or use it. |

---

## Tool self-diagnosis, health, logging and solution-load reporting

| Feature | IDE | What it is |
| --- | --- | --- |
| Error List 'Show' filter (Build + IntelliSense / Build Only / IntelliSense Only) | vs | The Error List window's Show dropdown toggles between errors IntelliSense/Roslyn finds live in open files and errors that only appear after an actual MSBuild compile completes. |
| MSBuild project build output verbosity | vs | Tools > Options > Projects and Solutions > Build and Run exposes a Quiet/Minimal/Normal/Detailed/Diagnostic dropdown controlling how much MSBuild task/target detail prints to the Output window's Build pane. |
| MSBuild project build log file verbosity | vs | A second, independent verbosity setting on the same options page writes a per-project {ProjectName}.log file to each project's obj/intermediate folder on every build. |
| Activity Log (devenv /Log) | vs | Launching devenv with /Log writes ActivityLog.xml to %AppData%\Microsoft\VisualStudio\{version}\, recording every VSPackage/extension load attempt (success or failure) plus a rolling buffer of general startup entries. |
| MEF Composition Error Log | vs | Editor-extension MEF import/export failures are recorded separately at %localappdata%\Microsoft\VisualStudio{version}\ComponentModelCache\Microsoft.VisualStudio.Default.err. |
| devenv /SafeMode | vs | Starts VS loading only the default environment, default services, and in-box packages — every third-party extension is excluded. |
| devenv /ResetSettings | vs | Restores VS to default settings, optionally from a specified .vssettings file. |
| devenv /DoNotLoadProjects | vs | Opens a solution shell without loading any of its projects. |
| Solution Explorer '(unavailable)' + Reload Project | vs | A project that fails to load is shown grayed-out with an '(unavailable)' suffix; right-click offers Reload Project. Common causes: missing workload, XML syntax error in the project file, or a project-file version newer than the installed VS. |
| Lightweight/deferred solution-load progress | vs | Large solutions defer project loads until a project is expanded in Solution Explorer; if bulk-loading will exceed ~2 minutes, VS shows a determinate progress bar counting 'N of M projects loaded.' |
| Project System Tools 'Build Logging' window | vs | A free Microsoft VSIX (VisualStudioProductTeam.ProjectSystemTools) adds View > Other Windows > Build Logging, which records both regular and design-time builds live and lets you save any of them as a .binlog. |
| MSBUILDDEBUGENGINE / MSBUILDDEBUGPATH env vars | vs | Setting these two environment variables before launching devenv.exe makes every MSBuild invocation in that session — including design-time builds — automatically dump a .binlog to the given folder. |
| MSBuild Structured Log Viewer / .binlog | both | A .binlog captured from VS, Rider, or plain `dotnet build -bl` opens in the free, cross-tool MSBuild Structured Log Viewer (msbuildlog.com) or its browser build, showing a full searchable tree of targets/tasks/properties/items. |
| Report a Problem feedback tool | vs | Help > Send Feedback > Report a Problem (or the title-bar feedback icon) opens Developer Community and can attach a screenshot, a click-by-click action recording, and trace/heap-dump files captured live. |
| Info bar (in-window 'gold bar') notifications | vs | VS surfaces IDE/solution-level problems (missing SDK, failed restore, extension prompts) via a dismissible inline info bar, sometimes paired with an indeterminate progress spinner. Microsoft's own UX guidelines reserve it for solution/project-file-level issues and say Solution Explorer decoration should be considered first for narrower ones. |
| Visual Studio Installer 'Repair' | vs | The separate Visual Studio Installer app has a per-instance Repair action that re-validates and reinstalls damaged VS components. |
| C# Dev Kit 'Projects' output channel | C# Dev Kit (VS Code) | Output panel > Projects dropdown surfaces project-system-level errors (SDK detection failures, load problems) separately from the general C# log; a fully loaded solution logs a line like 'Project system initialization finished. N project(s) are loaded, and M failed to load.' |
| C# Dev Kit 'Dotnet > Server: Trace' setting | C# Dev Kit (VS Code) | Raising the Dotnet > Server trace level to Trace in VS Code settings increases the C# language server's log verbosity for deep diagnosis. |
| '.NET: Restart Language Server' vs. Reload Window | C# Dev Kit (VS Code) | Invoking .NET: Restart Language Server with C# Dev Kit installed is documented to desync Dev Kit's project system from the restarted server, which keeps stale project info; Microsoft's own guidance is to use Developer: Reload Window instead when Dev Kit is active. |
| 'Failed to restore solution' notification | C# Dev Kit (VS Code) | When NuGet restore fails during solution load, C# Dev Kit shows a notification with a 'Show error' action opening the underlying NuGet restore output. |
| Test Explorer diagnostic verbosity | C# Dev Kit (VS Code) | Raising the C# Dev Kit Test Explorer log level from minimal to diagnostic prefixes internal messages with [dev] for deeper visibility into test discovery/run issues. |
| Invalidate Caches and Restart | rider | File \| Invalidate Caches wipes Rider's indexing/caching state (with checkboxes to also clear the system log, downloaded shared indexes, VCS log, etc.) and restarts — scoped to the current project only or to every project ever opened in that IDE version. |
| Repair IDE / Repair IDE on File | rider | File \| Cache Recovery \| Repair IDE runs a sequence of progressively more invasive recovery steps (refresh VFS, re-index, etc.) for the current project only, letting you check after each step whether the problem is fixed before continuing — no full cache wipe or restart required. 'Repair IDE on File' runs the first steps scoped to a single file via its context menu. |
| Build tool window | rider | Ctrl+W, A opens the Build tool window, streaming live build output as both a flat log and an error/warning tree; double-clicking a tree node jumps to the offending line, and Alt+F1 locates the source file in another tool window. |
| Toolset and Build — MSBuild version picker | rider | Settings \| Build, Execution, Deployment \| Toolset and Build shows which MSBuild.exe Rider auto-detected (SDK-bundled vs. a Visual Studio install) and lets you override with a custom path; a 'Use ReSharper Build' checkbox additionally reports whether Rider uses its own incremental build engine or delegates entirely to MSBuild.exe. |
| Write MSBuild log to output / to file | rider | On the same Toolset and Build page, two independent checkboxes control whether MSBuild log output streams into the Build tool window and/or writes to a file at a chosen verbosity level. |
| Design Time Build Logging (Find Action) | rider | An action reachable only via Find Action (Ctrl+Shift+A, search 'Design Time Build Logging') toggles logging of design-time builds — the builds that populate the project model/IntelliSense. After enabling it you close/reopen the solution; logs land per-project under <config dir>\log\MsBuildTask. |
| Build Selected Projects with Diagnostics | rider | Selecting one or more projects in the Explorer and using the Advanced Build Actions context-menu group builds just those projects at diagnostic verbosity, instead of the whole solution. |
| Problems tool window (Current File / All Solution Files) | rider | The Problems tool window (2021.3+, absorbing the old Errors in Solution window) has separate tabs for the current file vs. the whole solution, plus a distinct category for issues with 'missing tooling and broken environment configuration.' |
| Solution-wide analysis status-bar toggle | rider | A status-bar control enables/disables solution-wide analysis and shows its background scanning progress live; when on, Rider continuously surfaces compiler/inspection errors across the whole solution, not just open files. |
| Roslyn analyzer load-failure notification | rider | When a custom Roslyn analyzer package fails to load (bad assembly, missing dependency, SDK-version mismatch), Rider shows an 'issues were encountered while loading projects for Roslyn analyzers' notification, explicitly noting it affects only third-party analyzers and not Rider's own built-in inspections. |
| Memory Indicator + low-memory popup | rider | An optional status-bar Memory Indicator widget (right-click the status bar to enable) shows live heap usage; if free heap after a GC drops below 5% of the max heap, Rider proactively shows a warning with an 'Adjust memory options' action. |
| Help \| Change Memory Settings | rider | A dedicated Help-menu dialog for editing the JVM -Xmx heap ceiling and restarting, including for Toolbox-managed installs; if the IDE won't start at all, -Xmx can be hand-edited in the JVM options file as a fallback. |
| Background Tasks window | rider | Window \| Background Tasks \| Show lists every task currently running in the background (indexing, syncing projects, VCS operations) with per-task progress and a cancel action; the status bar shows an aggregate progress indicator you can click to open the same list. |
| Power Save Mode | rider | File \| Power Save Mode disables design-time inspections, solution-wide analysis, Code Vision, automatic completion popups, and some VCS background work (log auto-update, log indexing, annotation preloading) to cut CPU/battery use; editing, build, run, and debug keep working. It does not pause the initial project/solution indexing itself. |
| Notifications tool window | rider | Replacing the old Event Log, the Notifications tool window splits messages into 'Suggestions' and 'Timeline,' color-codes severity (blue routine / red error), and persists entries until cleared or the solution reopens; each notification event group is independently configurable (Settings \| Appearance & Behavior \| Notifications) as a Balloon, Tool-window balloon, Sticky balloon, or suppressed… |
| Collect Logs and Diagnostic Data / Show Log / Debug Log Settings | rider | Help \| Collect Logs and Diagnostic Data bundles idea.log, thread dumps, and other diagnostics into one archive for support; Help \| Show Log in Explorer opens the raw log folder (%LOCALAPPDATA%\JetBrains\<product><version>\log) directly; Help \| Diagnostic Tools \| Debug Log Settings lets you add extra logger categories (as advised by JetBrains support) without hand-editing log.xml. |
| Help \| Diagnostic Tools \| Special Files and Folders | rider | Lists every directory the current Rider install uses (config, system/cache, plugins, logs) with a jump-to-folder action for each. |

---

## Settings, configuration and team-shared style

| Feature | IDE | What it is |
| --- | --- | --- |
| Layered .DotSettings storage (This Computer / Solution team-shared / Solution personal) | rider | ReSharper/Rider settings live in a stack of named layers, applied in priority order: Solution personal > Solution team-shared > This Computer > built-in defaults. This Computer is stored at %APPDATA%\JetBrains\Rider<version>\resharper-host\GlobalSettingsStorage.DotSettings; Solution team-shared is <SolutionName>.sln.DotSettings next to the .sln; Solution personal is… |
| Smart 'Save' vs explicit 'Save To <layer>' | rider | In the Settings dialog (Ctrl+Alt+S), the default 'Save' button applies smart logic: it writes to This Computer, but if that would silently override a team-shared value it instead writes to Solution personal to avoid clobbering the team setting. A separate 'Save To' selector lets you target one specific layer directly, though it can be blocked if a higher layer already pins that value. |
| Manage Layers dialog (custom layers, import, copy between layers) | rider | The 'Manage Layers' button (bottom-left of Settings) opens a dialog where you can add custom layers nested under This Computer or under a solution layer, use the toolbar's 'Import from \| File' to load an external .DotSettings file into a chosen layer, and right-click a layer to 'Copy Settings To' another layer. |
| Commit team-shared settings, gitignore personal ones | rider | Documented workflow: check <SolutionName>.sln.DotSettings into version control (it holds the Solution team-shared layer) and exclude <SolutionName>.sln.DotSettings.user (Solution personal). New team members get the team's naming/formatting/inspection rules automatically on first VCS pull, with no manual import step. |
| Import an existing ReSharper .DotSettings file into Rider | rider | Via Settings > Manage Layers > 'Import from \| File' toolbar button, you point at a .DotSettings file exported from standalone ReSharper (Visual Studio) and load it straight into a chosen Rider layer, reusing a team's existing VS/ReSharper style configuration without re-authoring it. |
| Synchronize machine-wide Rider and ReSharper settings | rider | Tools \| ReSharper page in Rider's settings has a checkbox to keep the This-Computer-level settings aligned between a standalone ReSharper install and Rider on the same machine, since solution-scoped layers already sync automatically via the .DotSettings files themselves. |
| Backup and Sync (JetBrains Account settings sync) | rider | Bundled plugin (Settings > Backup and Sync > Enable backup and sync) that stores IDE-level preferences under your JetBrains Account and applies them to every JetBrains IDE you sign into: themes, keymaps, color schemes, UI/menu/toolbar layout, editor and completion settings, live templates, code styles, and the enabled/disabled plugin list. |
| Per-language Code Style pages with live preview | rider | Editor \| Code Style \| [Language] (separate pages per C#, HTML, XAML, C++, Less, etc.) expose every formatting/style knob with a live preview pane at the bottom of the page that re-renders a code sample as you toggle each option, so you see the effect before saving. |
| Naming Style rules UI (per language, per symbol kind) | rider | Editor \| Code Style \| [Language] \| Naming Style tab lets you configure, per identifier kind (interfaces, classes, methods, private fields, type parameters, etc.), capitalization of compound words, prefixes/suffixes, word separators, per-access-right variants, and abbreviations to ignore — seeded with Microsoft's naming guidelines out of the box. |
| Formatting rules UI with contextual scoped editing | rider | Formatting settings (spacing around operators, indentation of nested constructs, line-break/wrapping rules, blank-line limits, etc.) live on the same per-language Code Style pages with live preview. Additionally, selecting a code block and using Alt+Enter > Reformat and cleanup > Configure code style opens a scoped dialog that affects only that selection's formatting. |
| Code Cleanup profiles (built-in + custom) | rider | Editor \| Code Cleanup settings page manages multiple cleanup profiles — built-ins include 'Full Cleanup' and 'Reformat & Apply Syntax Style' / 'Reformat' — and lets you build custom profiles by checking which cleanup tasks (reformat, apply syntax style, remove redundancies, optimize usings, etc.) run together, plus assign a default profile for silent/on-save cleanup. |
| CleanupCode CLI for CI-enforced cleanup | rider | Cross-platform command-line tool that runs a named cleanup profile against a solution or file set outside the IDE. It resolves settings with precedence .editorconfig > .DotSettings > CLI parameters, and picks up a committed <Solution>.sln.DotSettings automatically — no extra CI config needed once the file is in the repo. |
| Inspection Severity settings page | rider | Editor \| Inspection Severity lists every configurable code inspection grouped by language then category, each with a severity dropdown (Error/Warning/Suggestion/Hint/Do not show, etc.) editable in bulk from one screen rather than one-by-one in the editor. |
| Inline 'Configure inspection severity' from the editor | rider | On any highlighted inspection, Alt+Enter opens the action list with an 'Inspection <name> \| Configure inspection severity' entry that lets you change severity right at the point of violation, writing back to the active layer or .editorconfig. |
| InspectCode CLI for solution-wide CI reports | rider | Command-line static-analysis tool that runs the full ReSharper/Rider inspection engine against a solution outside the IDE and emits an XML/SARIF-style report, honoring the same layered settings (including severities) as the IDE. |
| Export Code Style settings to an .editorconfig | rider | Editor \| Code Style page has an Export button that writes an .editorconfig section (choosing the target file mask) and lets you pick which categories to include: standard EditorConfig properties, cross-editor .NET conventions, and/or Rider-specific properties, resolving any conflicts with an existing file. |
| Rider-specific EditorConfig properties beyond the .NET conventions | rider | Rider's .editorconfig support layers three tiers of properties: (1) the core EditorConfig spec (indent_size, charset, etc.), (2) shared .NET coding-convention keys usable by any .NET IDE (csharp_style_var_for_built_in_types, etc.), and (3) Rider/ReSharper-only keys (resharper_csharp_*) for granular formatting, syntax style, and inspection severities that no other editor understands. |
| EditorConfig-overrides-DotSettings toggle, opt-in for naming/inspections | rider | By default an .editorconfig in scope overrides the layered .DotSettings for formatting. This can be disabled via the EditorConfig support checkbox on Editor \| Code Style \| General; separately, applying EditorConfig to naming style and code inspection configuration requires explicitly enabling 'Read settings from editorconfig'. |
| File and Code Templates | rider | Editor \| File and Code Templates page provides predefined templates for every supported new-file type plus custom ones you author; 'Save File as Template' captures an open file's body (with placeholders) and lets you name/extend it; Live Templates can be embedded inside a file template. |
| Live Templates editor | rider | Editor \| Live Templates lets you create/edit abbreviation-triggered code snippets with editable body text, typed variables/expressions (e.g. suggestVariableName()), and an 'applicable in' context scope (language, file type, surrounding code) that determines when the template is offered. |
| File and live templates travel through the settings layers | rider | Templates are layer-based settings exactly like code style and naming rules, so saving a template to the Solution team-shared layer and committing the resulting .sln.DotSettings distributes it to the whole team the same way formatting rules are distributed. |
| Keymap page with predefined schemes and custom keymaps | rider | Settings > Keymap ships several predefined, non-editable schemes (including Visual Studio, Visual Studio for Mac, IntelliJ IDEA, ReSharper) that you select wholesale, plus the ability to clone one into an editable custom keymap and rebind individual actions (right-click an action > Add Keyboard Shortcut). |
| Settings Repository (Git-based cross-machine sync, legacy) | rider | Tools \| Settings Repository page offers an older, still-available mechanism that syncs IDE settings through a user-owned Git repository rather than a JetBrains Account, for teams/users who want sync without depending on JetBrains' cloud service. |
| Code Syntax Style settings (separate from formatting) | rider | A distinct settings category from whitespace/line-break formatting: Editor \| Code Style \| C# \| Syntax Style covers preferences like modifier ordering and member-qualifier ('this.') usage, each independently configurable and independently includable as a Code Cleanup task. |
| Settings Sync tied to a signed-in Microsoft/work account | vs | Tools > Options > Environment > Accounts has 'Synchronize settings across devices and installs on this machine', enabled by default. Once signed in, changed settings propagate automatically to every other signed-in VS session (a manual Sync button forces immediate reconciliation), and sync spans side-by-side VS versions/editions and Blend for Visual Studio. |
| Import and Export Settings wizard (.vssettings) | vs | Tools > Import and Export Settings opens a wizard to export selected environment settings (themes, keyboard shortcuts, window layout, templates, etc.) to a portable, open-schema XML .vssettings file, import one back in, or reset to defaults. VS auto-backs up the current settings to a .vssettings file at %LOCALAPPDATA%\Microsoft\VisualStudio\<version>\Settings\ every time it closes. |
| Unified Settings (JSON-based settings redesign) | vs | A ground-up redesign of Tools > Options, rolled out as the default settings UI starting with Visual Studio 2026: settings are stored in a human-readable JSON file (editable directly or via UI), split into User Scope (machine-wide) and Workspace Scope (repo-specific, optionally checked in for the team), synced to the cloud, and surfaced through unified cross-category search. |
| Text Editor > C# > Code Style options page (General / Formatting / Naming) | vs | Tools > Options > Text Editor > C# > Code Style has three sub-pages: Formatting (whitespace/brace rules), General (Quick-Action-driven language preferences like var usage, expression-bodied members), and Naming. Each rule row shows a live preview and independent Preference and Severity dropdowns (Refactoring Only / Suggestion / Warning / Error), which drive both the Quick Actions light bulb and,… |
| 'Generate .editorconfig file from settings' button | vs | On Tools > Options > Text Editor > [C# \| Basic] > Code Style > General, a 'Generate .editorconfig file from settings' button converts your current personalization-account preferences from that Options page into a ready-made .editorconfig file. |
| IntelliCode-generated .editorconfig inferred from existing code | vs | Solution Explorer > right-click > Add > New EditorConfig (IntelliCode) uses IntelliCode to statistically infer the code styles already prevalent in the existing codebase and emits a pre-filled .editorconfig, as an alternative to generating from current Options-dialog settings. |
| Visual/formatted .editorconfig editor + item templates | vs | VS 2022+ renders .NET .editorconfig files in a structured, formatted editor view instead of plain text. Project > Add New Item offers two ready-made templates — 'editorconfig File (empty)' and 'editorconfig File (.NET)' (pre-populated with default whitespace, code-style, and naming conventions) — and Solution Explorer shows the file with the visual editor by default. |
| .editorconfig hierarchy, root=true, and precedence over global Options | vs | VS discovers .editorconfig files by walking up the folder tree from each open file until it reaches the filesystem root or a file with root = true. Lower-level files only override the specific properties they list; unlisted properties fall through from higher-level files. .editorconfig settings always take precedence over the global Tools > Options > Text Editor Code Style settings. |
| 'Follow project coding conventions' global toggle | vs | Tools > Options > Text Editor > General has a 'Follow project coding conventions' checkbox; clearing it stops any in-scope .editorconfig from overriding the global per-editor Options settings, useful for troubleshooting or intentionally falling back to personal prefs. |
| Naming Rules page (structured UI, not raw editorconfig text) | vs | Tools > Options > Text Editor > C# > Code Style > Naming lists naming rules as rows you add/edit/remove through dialogs: a Specification (symbol kinds + accessibility modifiers to match) and a Naming Style (capitalization, required prefix/suffix, word separator, with a live sample) combined with a Severity. Saving writes the corresponding dotnet_naming_symbols.*, dotnet_naming_style.*, and… |
| Configure Code Cleanup dialog with two swappable profiles | vs | Clicking the broom icon (or Ctrl+K, Ctrl+E) at the bottom of the editor, or Analyze > Code Cleanup > Configure Code Cleanup, opens a dialog listing dozens of individually toggleable fixers (remove unused usings, sort usings, add 'this'/'Me' qualification, apply var/pattern-matching/expression-bodied-member preferences, etc.) across two independently configurable profiles. Cleanup can run on one… |
| Set analyzer rule severity from Solution Explorer | vs | Expand References/Dependencies > Analyzers > [assembly] in Solution Explorer, right-click a specific diagnostic, and choose 'Set severity' from Error/Warning/Suggestion/Silent/None/Default. VS writes a dotnet_diagnostic.<RuleID>.severity entry into the project's .editorconfig (creating one if none exists), or into the legacy ruleset file if the project still uses one. |
| Configure severity from the light-bulb / Quick Actions menu | vs | Ctrl+. on a violation opens Quick Actions with 'Configure <rule ID> severity', 'Configure severity for all <category> analyzers', and 'Configure severity for all analyzers', each showing a live preview before writing the change into .editorconfig. |
| Bulk severity entries by rule category or globally | vs | .editorconfig supports dotnet_analyzer_diagnostic.category-<category>.severity and dotnet_analyzer_diagnostic.severity to set many default-enabled analyzer rules at once, with well-defined precedence: single-rule entry > category entry > blanket entry. Rules disabled by default still require an explicit per-ID entry to enable. |
| .globalconfig global AnalyzerConfig files | vs | A file literally named .globalconfig (or any name explicitly added via the MSBuild GlobalAnalyzerConfigFiles item) starts with is_global = true and has no [file-mask] section headers — its diagnostic-severity entries apply to every source file in every project under that folder, unlike a scoped .editorconfig section. |
| Legacy .ruleset files with one-click migration to .editorconfig | vs | Pre-16.5 projects configure analyzer severities via a CodeAnalysisRuleSet-referenced .ruleset XML file, opened in a dedicated rule set editor in VS. Since rule sets are deprecated in favor of .editorconfig, VS shows a clickable infobar in the rule set editor that migrates the file to an equivalent .editorconfig (updating the project's CodeAnalysisRuleSet reference), and a standalone… |
| Enforce IDE code-style rules at build time (EnforceCodeStyleInBuild) | vs | Setting an IDE#### code-style rule's severity to warning or error in .editorconfig, combined with the MSBuild property EnforceCodeStyleInBuild = true, makes style violations fail `msbuild`/`dotnet build` and CI, not just show squiggles in the editor; command-line builds only surface analyzer violations set to warning or error regardless of verbosity. |
| .vsconfig installation-configuration files | vs | Exported from the Visual Studio Installer (product card > More > Export configuration) as a JSON file listing installed workloads/components and, since VS 2022 17.9+, Marketplace extensions. Dropping a .vsconfig in the solution/repo root makes VS auto-detect any components a teammate's install is missing when they open the solution and prompt to install them; the same file drives unattended… |
| Code Snippets Manager + versionable .snippet XML files | vs | Tools > Code Snippets Manager (Ctrl+K, B) registers folders of custom snippets defined in .snippet XML files (namespace http://schemas.microsoft.com/VisualStudio/2005/CodeSnippet, Language="CSharp"), which can be checked into a repo and pointed at from the manager so a team shares the same insertable code snippets. |
| Keyboard shortcut customization + predefined keymap schemes | vs | Tools > Options > Environment > Keyboard lets you search for any command and bind/rebind a key combination directly. Rider mirrors the same idea from the other direction by shipping a built-in 'Visual Studio' keymap preset so migrating developers keep their VS muscle memory. |
| Per-user solution state kept out of the shared .sln/.editorconfig | vs | Visual Studio stores machine/user-specific solution state — open files, breakpoints, window layout, IntelliSense caches — in a hidden .vs\<SolutionName>\v17\.suo binary file next to the .sln, which is meant to stay out of source control, cleanly separated from the .sln/.csproj/.editorconfig files that a team actually shares. |

---

## Preprocessor directives, build configurations and multi-targeting in the editor

| Feature | IDE | What it is |
| --- | --- | --- |
| Inactive #if code dimmed/grayed in editor | vs | Visual Studio renders code inside a non-active #if/#elif branch in a muted gray. The color/style is controlled via Tools > Options > Environment > Fonts and Colors, display item 'Excluded Code'. Which branch counts as 'active' is driven by the DefineConstants of the project's currently selected configuration/platform (and, for multi-targeted projects, the selected TFM). |
| Inactive #if branches dimmed and unparsed | rider | Rider (via its bundled ReSharper engine) only parses/analyzes the #if branch matching the currently defined preprocessor symbols for the active context. The non-matching branch is shown dimmed with no syntax highlighting and no code inspections running inside it. |
| Active solution configuration/platform dropdowns on Standard toolbar | vs | The 'Solution Configurations' (e.g. Debug/Release) and 'Solution Platforms' (e.g. Any CPU/x64) combo boxes sit permanently on VS's Standard toolbar. Changing them changes which project configuration's DefineConstants drive live editor analysis and which output gets built by F5/Ctrl+Shift+B. |
| Solution configuration + platform selector in top-right toolbar | rider | Rider shows a single combined control in the right part of the main toolbar for choosing the active solution build configuration (Debug/Release) and build target/platform (e.g. Any CPU). An 'Edit Solution Configurations...' entry in the same dropdown opens the add/remove/edit/duplicate configuration UI. |
| Per-file Target Framework selector for multi-targeted projects | vs | When editing a .cs file that belongs to a project with multiple <TargetFrameworks>, VS exposes a dropdown to pick which TFM should drive IntelliSense/live warnings for that file (e.g. flagging an API call that doesn't exist in the selected TFM), independent of which TFM actually gets built. |
| TFM switcher merged into editor breadcrumb bar | rider | For a file in a multi-targeted project, the project-name segment of Rider's breadcrumb bar (bottom of the editor, enabled via Settings \| Editor \| General \| Breadcrumbs) also shows the currently targeted framework; clicking it lets you switch which TFM's context is analyzed. In some versions the same control instead appears as a dedicated widget in the editor's upper-right. |
| Implicit per-TFM preprocessor symbols (NETx_y, NETx_y_OR_GREATER, platform symbols) | both | The .NET SDK itself (not the IDE) auto-defines symbols like NET8_0, NET8_0_OR_GREATER, NETSTANDARD2_0, NETFRAMEWORK, and OS-specific symbols such as WINDOWS/ANDROID/IOS/MACCATALYST only for the TFM(s) actually targeted; both IDEs consume this list as-is when deciding what's 'defined' per branch. Generation can be turned off via the DisableImplicitFrameworkDefines MSBuild property. |
| Framework-aware IntelliSense/UI filtering for older targets | vs | When a project targets an earlier framework version, VS dynamically filters the Add New Item / Add Reference / Add Service Reference dialogs, the Toolbox, IntelliSense completions, the Properties window, and menu options to omit anything unavailable in that target, and compiles with the matching compiler/compiler-options set. |
| Go to Declaration jumps between matching preprocessor directives | rider | Rider's 'Go to Declaration' navigation works on preprocessor directives themselves: invoking it on #if/#elif/#else/#endif jumps between the directives that form the same conditional block, and on #define/#undef jumps to the matching directive for that macro. |
| Sticky lines include preprocessor directives | rider | Rider 2024.2 extended 'sticky lines' (the pinned context lines at the top of the editor while scrolling) to include the enclosing #if/#region directive, alongside showing whether the current member is public/private. |
| Rider is unaware of code inside inactive #if branches for refactoring | rider | Because Rider only parses the active #if branch into its code model (PSI), Find Usages, Rename, and other refactorings do not see or update references that live exclusively inside an inactive branch for the current context; switching build configuration/TFM re-parses and can reveal a different set of usages. |
| Outlining/collapse for #region and #if...#endif blocks | vs | VS's editor outlining (the +/- gutter margin, Edit > Outlining commands, Ctrl+M shortcuts) treats #if...#endif blocks the same as #region blocks, letting you collapse an entire conditional branch to a single line. |
| Conditional compilation symbols field in project properties | vs | Project Properties > Build tab (General section) has a dedicated 'Conditional compilation symbols' textbox (semicolon-separated), plus separate 'Define DEBUG constant' and 'Define TRACE constant' checkboxes. The Configuration and Platform dropdowns at the top of the same page let you view/edit these per Debug/Release and per platform independently, or via 'All Configurations'. |
| No dedicated conditional-compilation-symbols GUI field | rider | Rider's Project Properties dialog (Application/Advanced pages) does not expose a purpose-built 'Conditional compilation symbols' textbox. Symbols are instead set by editing DefineConstants directly in the .csproj, or by locating it through the generic, searchable 'Properties' page that lists every MSBuild property affecting the project. |
| Configuration Manager dialog | vs | Build > Configuration Manager opens a dialog with an 'Active solution configuration' dropdown, an 'Active solution platform' dropdown, and a per-project grid of Configuration/Platform/Build-checkbox/Deploy-checkbox rows, so each project in the solution can be wired to build (or not) under a given solution configuration. |
| 'Edit Solution Configurations' from the toolbar dropdown | rider | Rider manages solution-level configurations by opening 'Edit Solution Configurations' from the same toolbar control used to pick the active configuration, where you add, remove, edit, or duplicate configurations. There is no separate modal grid mapping every project x configuration x build/deploy checkbox the way VS's Configuration Manager does. |
| Platform Target compiler setting (x86/x64/ARM/ARM64/Any CPU) | vs | Project Properties > Build page has a 'Platform Target' dropdown (x86, x64, ARM, Any CPU, etc.) that sets the compiler's /platform option, shown/edited per the Configuration+Platform combo selected at the top of the page. |
| 'Prefer 32-bit' checkbox for Any CPU builds | vs | On the Build page, 'Prefer 32-bit' (visible only when Platform Target = Any CPU, Output type = an application, and Target framework = .NET Framework 4.5+) sets PlatformTarget to anycpu32bitpreferred, forcing the app to run 32-bit even on 64-bit Windows. |
| New Solution Platform dialog / adding custom platforms (e.g. ARM64) | vs | From Configuration Manager's 'Active solution platform' dropdown, choosing 'New...' opens 'New Solution Platform', letting you type/select a processor architecture (e.g. ARM64), optionally copying settings from an existing platform configuration into the new one. |
| Platform selector alongside configuration selector | rider | Rider's toolbar build-configuration control also carries the build target/platform (e.g. Any CPU) next to the Debug/Release choice, switched from the same widget rather than a separate menu. |
| COM-registration architecture defaults tied to VS's own bitness | vs | When registering an Any CPU .NET assembly for COM interop, MSBuild defaults to registering it for MSBuild's own runtime architecture — x64 under VS 2022+ (VS itself runs 64-bit) versus x86 under VS 2019 and earlier — overridable per-project via the RegisterAssemblyMSBuildArchitecture property. |
| Per-configuration/platform x86 vs x64 reference wiring via conditioned ItemGroups | both | To reference different binaries per Configuration+Platform combination (e.g. a native DLL with separate x86/x64/Debug/Release builds), you add multiple <ItemGroup Condition=" '$(Configuration)\|$(Platform)' == 'Debug\|x64'"> blocks in the project file; neither IDE's GUI can express this, so both funnel you to manual .csproj editing for this case. |
| Target multiple frameworks by editing TargetFrameworks in the project file | both | For .NET 5+, multi-targeting is set up by replacing <TargetFramework> with <TargetFrameworks>net8.0;net9.0</TargetFrameworks> in the project file and reloading; after reload, both IDEs' project-properties/Application page lets you edit the resulting list of target frameworks through a GUI rather than raw XML. |
| Quick-fix for using singular TargetFramework with multiple values | rider | Rider detects the mistake of putting a semicolon-separated list into the singular <TargetFramework> element (which produces the error 'The TargetFramework value is not valid. To multi-target, use the TargetFrameworks property instead') and offers a code inspection/quick-fix that rewrites it to the plural <TargetFrameworks> element automatically. |
| Solution filter (.slnf) support with filtered-project indicator | rider | Rider loads .slnf solution filter files (a defined subset of a full solution's projects) and shows a filtered-solution indicator in Solution Explorer with a count of loaded projects, alongside 'not built for this configuration' project entries rendered as clickable links that open the solution configuration dialog. |
| Nullable reference-type project context on the Build page | vs | Project Properties > Build page has a 'Nullable' dropdown controlling the project-wide C# nullable annotation/warning context (enabled for C# 8.0+ projects), independent of but interacting with any #nullable directives and #if-guarded code. |
| Per-configuration 'Treat Warnings as Errors' (None/All/Specific) | vs | The Build page's 'Treat Warnings as Errors' group lets you choose None, All, or a specific semicolon-separated list of warning numbers to escalate to errors, set independently for each Configuration/Platform combination via the same page's top dropdowns. |
| 'Advanced' build settings dialog (language version, deterministic build, etc.) | vs | The Build page's 'Advanced' button opens the Advanced Build Settings dialog for less-common per-configuration compiler switches (e.g. C# language version) not exposed on the main page. |
| Assembly Explorer shows build configuration and TFM-derived directives of decompiled assemblies | rider | Rider 2024.2's Assembly Explorer tool window detects and displays the build configuration (e.g. Debug/Release) an inspected assembly was compiled with, and its decompiler correctly re-derives TFM-based preprocessor directives like NET6_0_OR_GREATER from the target framework or embedded Portable PDB. |
| Active configuration/platform always visible, never build-triggered to discover | both | Both IDEs surface the active configuration and platform as a persistent toolbar control (VS: two separate combo boxes; Rider: one combined widget) rather than requiring a menu dig or a build to find out what's currently selected. |

---

## C# Interactive, scripting, scratch files and ad-hoc evaluation

| Feature | IDE | What it is |
| --- | --- | --- |
| C# Interactive window (REPL) | vs | View > Other Windows > C# Interactive opens a Roslyn-scripting-backed REPL editor built into VS since VS2015 Update 1. Cumulative session state, System/System.Core/Microsoft.CSharp referenced by default. |
| Execute in Interactive / Copy to Interactive | vs | Select code in a .cs or .csx file, right-click (or Ctrl+E, Ctrl+E) to run it directly in the C# Interactive window, or 'Copy to Interactive' to paste without running. |
| Initialize Interactive with Project | vs | Right-click a project in Solution Explorer and choose 'Initialize Interactive with Project': VS builds the project, then adds it and all its dependencies as references (#r) to the session, plus a using for the default namespace. |
| #r assembly reference directive | vs | `#r "System.Web.Extensions"` or `#r ".\bin\Debug\MyLib.dll"` manually references a GAC assembly or an on-disk DLL inside the Interactive window or a .csx file. |
| #load script directive | vs | `#load "C:\Temp\DemoScript.csx"` executes another script file in the current session; variables from the loaded script shadow existing ones with the same name. |
| #reset / #reset noconfig, #cls/#clear, #help | vs | #reset restarts the execution environment while keeping history (noconfig skips re-running init scripts); #cls/#clear wipes the editor buffer but keeps context; #help lists commands and key bindings. |
| IntelliSense, Quick Info, multi-line submissions, top-level await | vs | Interactive window supports full IntelliSense (Ctrl+Space), hover Quick Info, incomplete-statement multi-line editing (Shift+Enter to add a line), Ctrl+J member list, and top-level `await` expressions. |
| Submission history navigation | vs | Alt+Up/Down cycles prior submissions; Ctrl+Alt+Up/Down cycles history filtered by the current line's prefix; Ctrl+Enter forces evaluation or appends the previous submission; Esc clears the current submission (Ctrl+Z undoes the clear). |
| csi.exe command-line REPL | vs | The same Roslyn scripting engine ships as `csi.exe`, runnable from the Developer Command Prompt without VS open, for headless/CI-adjacent ad-hoc scripting. |
| #r "nuget:..." in VS's C# Interactive is unreliable | vs | NuGet package references via `#r "nuget: Package,Version"` are documented to work in .NET Interactive/F# Interactive but have long-standing bug reports (VS Developer Community, dotnet/roslyn#43918) of failing specifically in VS's own C# Interactive window. |
| No native .csx debugging in VS; only a weak 3rd-party extension | vs | VS has no built-in F5/breakpoint debugging for .csx scripts or the Interactive window. The only fix is the free but low-adoption 'C# Script (.csx) Debugger' marketplace extension (kzu.ScriptDebugger, v0.2.0, ~2.8k installs, 2/5 stars), which requires adding the .csx into a .NET Standard library project to get F5 debugging. |
| Immediate Window | vs | Debug > Windows > Immediate (Ctrl+Alt+I) evaluates expressions and executes statements with IntelliSense. `?expr` or `Debug.Print expr` prints a value; `>command` issues a VS command (e.g. `>cmd` switches to the Command window). |
| Design-time expression evaluation (no running app required) | vs | With no debug session active, typing `?MyFunction(2)` in the Immediate window builds and calls into the currently-selected (or startup) project, executing the function/subroutine directly; if it hits a breakpoint, VS drops into break mode so Locals/Call Stack can be inspected. |
| Design-time evaluation project-type restrictions | vs | Design-time Immediate-window evaluation is unsupported for project types that require an execution environment: VSTO/Office add-ins, web projects, Smart Device projects, and SQL projects; context is taken from the Solution Explorer-selected project (falling back to the startup project). |
| Immediate Window pseudovariables and modifiers | vs | During a live debug session the Immediate window supports `$exception` (last thrown exception), `$returnvalue` (last method's return value), `$user` (OS user/process info), a `, nse` suffix to evaluate an expression without side effects, and 'Make Object ID' to pin an object to a stable `$1`/`$2` handle across breakpoints. |
| Immediate Window bypasses access modifiers | vs | Expressions typed in the Immediate window can call private, protected, and internal members that would not compile as regular code. |
| C# Interactive tool window (REPL) | rider | Tools \| C# Interactive \| Start New C# Interactive Session opens a REPL tab in the Run window. On Windows it auto-detects and drives `csi.exe` from the MSBuild toolset; on macOS/Linux it defaults to Mono's `csharp` (both paths/args are configurable under Languages & Frameworks \| C# Interactive). |
| Send Selection to C# Interactive | rider | Select code (Ctrl+W to extend selection, Ctrl+A for whole file) and press Alt+Enter > 'Send Selection to C# Interactive'; it appends to an existing session or starts a new one. |
| 'Reference in C# Interactive' context menu | rider | Right-clicking a project or assembly in the Solution/Project Explorer offers 'Reference in C# Interactive', which auto-generates the #r/#load statement and executes it immediately, then requires a manual `using Namespace;`. |
| Reference Project toolbar button + auto-build setting | rider | The C# Interactive tool window has its own 'Reference Project' toolbar button to pick any project in the current solution; a Languages & Frameworks \| C# Interactive setting can auto-build the referenced project first so the loaded assembly is always current. |
| Attach debugger to C# Interactive session | rider | Since Rider 2018.2, you can attach Rider's debugger to the C# Interactive session and step through your own project's code when it's invoked from the REPL (set a breakpoint in project code, call the method from the Interactive window). Historically documented as Windows-only. |
| Export transcript to scratch file | rider | A toolbar button in the C# Interactive window, 'Export transcript to scratch file', dumps the whole REPL session (inputs and outputs) into a new scratch file. |
| Scratch files | rider | Ctrl+Alt+Shift+Insert (or Solution Explorer > Add > Scratch File) creates a scratch file in any supported language including C#, with full syntax highlighting/completion for that file type. Scratch files live in the IDE's config directory (a 'scratches' subfolder), not inside any project, so the same scratch is visible across every project opened with that IDE instance. |
| Create scratch file from selection | rider | Select code in any editor, press Alt+Enter, choose 'Create new scratch file from selection' to spin the selection out into a new scratch file. |
| Scratch files run only via C# Interactive (no standalone debug run) | rider | A C# scratch file has no independent Run/Debug configuration of its own; executing it means selecting its code (or Ctrl+A for all) and sending it to the C# Interactive session, same as any other code selection. |
| Promote a scratch file into a project | rider | A scratch file can be dragged, or cut/pasted (Ctrl+X/Ctrl+V), into a real project directory in Solution Explorer to turn it into a permanent project file. |
| Evaluate Expression / Quick Evaluate during debug | rider | Alt+F8 opens 'Evaluate Expression...'; Ctrl+Alt+F8 does 'Quick Evaluate Expression'; both work from the Debug tool window (Alt+5) while suspended at a breakpoint, and are also reachable via Alt+Enter. |
| dotnet-script global tool: run/REPL .csx from the CLI | external | `dotnet tool install -g dotnet-script`, then `dotnet script foo.csx` runs a script, or `dotnet script` (optionally `dotnet script foo.csx -i` to preload a file) drops into an interactive REPL supporting `#load`, `#r`, `#reset`, `#cls`, `#exit`, with the same default references/usings as normal .csx execution. |
| dotnet-script reliably resolves #r "nuget:..." | external | `#r "nuget: PackageName, Version"` resolves through `dotnet restore` and caches results for pinned versions, working consistently — in contrast to VS's own Interactive window where the same syntax is unreliable. |
| Full F5 breakpoint debugging of .csx via dotnet-script in VS Code | external | Running `dotnet script init` scaffolds an `omnisharp.json` and a `.vscode/launch.json` (type coreclr, program pointing at the dotnet-script executable, args the .csx file); breakpoints set in the .csx are then hit by pressing F5 like any other VS Code debug target. |
| Compile a .csx to a standalone executable/DLL | external | dotnet-script can package a script into a self-contained executable or DLL via `--output`, `--name`, and `--dll` flags, with Debug/Release configuration. |
| Polyglot Notebooks in VS Code (.dib / .ipynb) | external | The ms-dotnettools Polyglot Notebooks extension (successor to '.NET Interactive Notebooks') runs C#, F#, PowerShell, JavaScript, SQL, KQL, HTML and Mermaid cells side by side in one .dib or .ipynb file, with a per-cell language picker/magic command and cross-language variable sharing (e.g., query SQL, hand the result to a JS cell for charting). |
| Polyglot Notebooks for Visual Studio — third-party, not official Microsoft | vs | A VS Marketplace extension ('Interactive Notebooks', publisher Mads Kristensen, MIT/Apache-2.0, ~100+ commits) brings .dib/.ipynb notebooks into VS proper: multi-language cells (C#/JS/SQL/...), rich HTML/image/table output, IntelliSense, a Variable Explorer, a Document Outline, and — notably — cell-level debugging of C# and F# cells with the full VS debugger (step line by line, real breakpoints). |
| Rider has no notebook / Polyglot Notebooks support | rider | No official or widely-used third-party equivalent to Polyglot/.NET Interactive notebooks (.dib/.ipynb, multi-language cells, cross-language variable sharing) exists for Rider; its ad-hoc story stops at the C# Interactive REPL and scratch files. |
| LINQPad Dump() extension method | external | Calling `.Dump()` on any object renders its full object graph with heuristic formatting — including bitmaps, XML, and even WPF/WinForms controls — and lazily-expandable, clickable hyperlinks for EF/DB navigation-property associations. |
| LINQPad interactive LINQ/SQL querying against live databases | external | LINQPad connects directly to SQL Server, Azure SQL, SQLite, Postgres, Oracle, MySQL and more, and lets you write and run LINQ or raw SQL queries against them interactively in the same scratch session, browsing the schema alongside the query. |
| LINQPad built-in NuGet package manager, first-class in snippets | external | Query Properties (F4) > 'Add NuGet' lets a query pull in any NuGet package; references are saved with the query/snippet and available to 'My Extensions' and new-query defaults — a more reliable story than VS's own #r nuget in Interactive. |
| LINQPad lightweight integrated debugger (all editions, incl. free) | external | LINQPad ships a built-in, VS-style debugger for queries/scripts that is available even in the non-expiring free 'Standard' edition, alongside Dump(), DB querying, NuGet, and script caching. |
| LINQPad Premium tier — AI features paywalled | external | LINQPad's free Standard edition is fully-featured for core scripting/DB/dump/debug and has no expiry; the paid Premium tier adds AI code completion, chat, SQL-to-LINQ conversion, a schema-aware coding agent, and support for multiple AI providers (e.g., GitHub Copilot). |
| csi.exe / Interactive Window responds live to top-level `await` | vs | Both the Interactive window and csi.exe support awaiting a Task directly at the top level of a submission (e.g. `await Task.Delay(1000)`), unlike a normal C# method body pre-C#-9 top-level-statements context. |

---

## T4, custom tools, single-file generators and designer regeneration

| Feature | IDE | What it is |
| --- | --- | --- |
| TextTemplatingFileGenerator (design-time T4) | vs | Setting a .tt file's Custom Tool property to TextTemplatingFileGenerator makes it a design-time template: it runs on save and writes a nested output file (e.g. .cs) straight into the project. Created via Add New Item > Text Template. |
| TextTemplatingFilePreprocessor (run-time T4) | vs | Setting Custom Tool to TextTemplatingFilePreprocessor turns a .tt into a compiled class with a TransformText() method your app calls at run time, instead of generating a static file at design time. |
| Debug T4 Template | vs | Insert debug="true" into the <#@ template #> directive, set breakpoints in the .tt file, then choose 'Debug T4 Template' from its Solution Explorer context menu. VS steps through the template's generating code with full variable inspection. |
| Transform All Templates | vs | Build menu > Transform All Templates re-runs every T4 template in the solution in one action, regardless of which file changed. |
| Run Custom Tool (per-file) | vs | Right-click any item with a Custom Tool assigned (a .tt file or any other single-file-generator input like .resx) and choose 'Run Custom Tool' to force just that one file to regenerate. |
| TransformOnBuild MSBuild property | vs | Importing Microsoft.TextTemplating.targets and setting <TransformOnBuild>true</TransformOnBuild> in the project file makes every T4 template in the project transform automatically on every build, not just on save. |
| Host-specific directives & EnvDTE access from templates | vs | With hostspecific="true", template code can call this.Host.ResolvePath()/this.Host.TemplateFile, and via the assembly/import directives plus IServiceProvider.GetCOMService(typeof(EnvDTE.DTE)) a template can query the live Visual Studio solution (project counts, references, etc.) while generating. |
| Custom T4 directive processors | vs | VS's T4 engine supports pluggable, user-authored directive processors (beyond the built-in template/output/assembly/import/parameter directives) registered as VS components. |
| T4 Error()/Warning() surfaced in the Error List | vs | Calling Error("msg") or Warning("msg") from generating code places entries directly in the VS Error List window, tied to the template. |
| T4 editing with full C# IntelliSense (ForTea) | rider | Rider has shipped ForTea's T4 support built in since 2019.3: code completion, Find Usages, refactorings and context actions work inside the C# code blocks of a .tt/.ttinclude file, plus a Structure tool window and quick-fixes for misplaced blocks. |
| Execute Template / Preprocess Template actions | rider | Rider exposes 'Execute Template' (design-time transform) and 'Preprocess Template' (run-time class generation) as explicit actions on a .tt file, mirroring VS's TextTemplatingFileGenerator vs TextTemplatingFilePreprocessor split. |
| Debug Template | rider | Rider's 'Debug Template' action runs a .tt under the debugger with breakpoint support, stack traces, variable inspection, and the ability to move the execution pointer — the ForTea/Rider analog of VS's Debug T4 Template. |
| No VB.NET T4 or custom directive processor support in Rider | rider | ForTea's own docs state Visual Basic T4 templates are unsupported, and custom (user-authored) T4 directive processors are unsupported, in both the Rider-bundled and ReSharper plugin builds. |
| IVsSingleFileGenerator / Custom Tool project-item property | vs | The general 'custom tool' (single-file generator) mechanism: any project item can have its Custom Tool property set to a registered COM component implementing IVsSingleFileGenerator, which VS invokes automatically whenever that file is saved, writing a dependent output file. |
| ResXFileCodeGenerator | both | The default Custom Tool for .resx files; generates an internal strongly-typed accessor class (Resources.Designer.cs) from the resource entries. Rider recognizes and runs this generator natively (built-in association, no plugin needed). |
| PublicResXFileCodeGenerator | both | Swap-in variant of the resx generator that emits a public (instead of internal) accessor class, set the same way (Properties window Custom Tool field in VS; recognized the same way by Rider). |
| Tools \| Custom Tools (generic MSBuild/exe hook) | rider | Rider's own analog of arbitrary single-file generators, configured in Settings (Ctrl+Alt+S) > Tools > Custom Tools: a Name, a comma-separated list of file Extensions, a Path to an MSBuild invocation or external executable, and Args with IDE macros. It fires automatically on every edit of a matching file, or on demand via right-click > Execute [tool name]. |
| No native 'Custom Tool' file-properties field (RIDER-14860) | rider | Open JetBrains tracker request: Rider has no per-file 'Custom Tool' property in its file Properties the way VS does, so arbitrary third-party IVsSingleFileGenerator COM components written for VS (Redesigner, custom code generators, etc.) can't be pointed at a specific file inside Rider without the generic extension-wide Custom Tools workaround. |
| MSDataSetGenerator | vs | Custom Tool for typed-DataSet .xsd files; generates the strongly-typed DataSet/TableAdapter code behind the classic 'Add > New Item > DataSet' designer. |
| SettingsSingleFileGenerator + Settings Designer | vs | The Project Properties 'Settings' tab is a grid UI over a .settings file; saving it runs the SettingsSingleFileGenerator custom tool to (re)write Settings.Designer.cs, exposing typed Properties.Settings.Default.* members and, for VB, the My.Settings object. |
| WCF 'Add Service Reference' / WCF Web Service Reference Provider | vs | For .NET Framework projects, right-click > Add Service Reference generates a Reference.cs client proxy from WSDL. For .NET/.NET Core, the equivalent is the 'Microsoft WCF Web Service Reference Provider' surfaced through the Connected Services node (Configure WCF Web Service Reference dialog), which pulls metadata from a WSDL/URL and regenerates the proxy on demand. |
| Connected Services gallery (OpenAPI, gRPC, Azure) | vs | A unified 'Connected Services' project node (Solution Explorer > right-click project/Dependencies > Add > Connected Service) that hosts multiple provider wizards — OpenAPI/Swagger client generation, Azure service wiring, and gRPC/OpenAPI service references, described by Microsoft as consolidating 'the existing OpenAPI & gRPC Service References table.' |
| 'Add new gRPC service reference' wizard | vs | Under Connected Services > Service References > Add new gRPC service reference, you Browse to a local .proto file (or supply a URL), pick Client/Server/Both, and VS adds the corresponding <Protobuf Include=... GrpcServices=.../> entry to the csproj for you instead of hand-editing XML. |
| LINQ to SQL O/R Designer (.dbml) | vs | A visual drag-and-drop design surface (install via VS Installer > Individual Components > 'LINQ to SQL tools') for building entity classes and a typed DataContext by dragging tables/views/stored procedures from Server Explorer onto the .dbml canvas. It is a strictly one-way generator: hand edits to the generated code are overwritten on the next regenerate. |
| No LINQ to SQL visual designer in Rider | rider | Rider has no O/R Designer equivalent — a .dbml file opens as plain XML; there is no drag-and-drop entity/relationship canvas, no Server Explorer-style table dragging, and no built-in DataContext code regeneration UI. |
| ADO.NET Entity Data Model Designer (.edmx, EF6) | vs | Installed via the 'Data storage and processing' workload, the Entity Designer visually creates/edits entities, associations, mappings and inheritance for EF6, backed by an .edmx file, with an Entity Data Model Wizard (database-first), Create Database Wizard (model-first) and Update Model Wizard (resync with schema changes). Note: not applicable to EF Core. |
| EF6 T4 code-generation templates (DbContext / EntityObject / POCO / STE Generator) | vs | Right-click the EDMX design surface > 'Add Code Generation Item...' installs one of the EF-team-provided T4 templates (ADO.NET DbContext Generator is the modern default; EntityObject, POCO, and Self-Tracking-Entities generators are legacy alternatives), nesting the resulting <model>.tt/.context.tt files under the .edmx and letting you edit the T4 itself to customize output shape. |
| No EDMX designer in Rider | rider | Rider has no visual Entity Data Model designer; .edmx opens as XML with no diagramming surface, and JetBrains' own Entity Framework support page covers only EF Core tooling, not EF6/EDMX. |
| EF Core migrations GUI (Add/Remove Migration, Update Database, Generate SQL Script, Drop Database, Scaffold DbContext) | rider | Rider ships a visual interface over the EF Core CLI commands (equivalent to Add-Migration/Update-Database/Script-Migration/Scaffold-DbContext) plus EF-specific code inspections. Visual Studio has no bundled equivalent — the native VS workflow is typing these same commands into the Package Manager Console (or dotnet-ef on the command line); GUI wrappers exist only as third-party extensions like… |
| Windows Forms Designer (VS) | vs | The classic drag-and-drop WinForms design surface: Toolbox, Properties grid, event double-click-to-handler wiring, all writing into the paired Form.Designer.cs partial class. Supports both .NET Framework and modern .NET (6+) targets, and both C# and VB.NET. |
| Windows Forms Designer (Rider) | rider | Rider has its own WinForms designer (component selection/placement, adorners for move/resize, property grid, event list, double-click to add default event handler, Toolbox with 'Manage Components' to pull in GAC/NuGet/on-disk controls). Support for .NET 6+ projects (not just .NET Framework) landed in Rider 2024.3, described by JetBrains as 'nearly the same functionality as the classic ...… |
| WPF XAML Designer (VS) | vs | Full split/design-view XAML designer: drag-and-drop from the Toolbox, a live Properties window, a visual tree/document outline, and design-time data support, all synchronized with the XAML markup and the code-behind partial class. |
| XAML Preview (Rider) — not a full designer | rider | Rider offers a live-rendering preview pane docked beside the XAML text editor for WPF files (position/orientation configurable, remembers layout per document), but explicitly no drag-and-drop editing, no Toolbox, and no property grid — it is read-only visualization of hand-written XAML, WPF only (no Avalonia/Uno in this feature). |
| Designer.cs / partial-class pairing with dependent-file nesting | both | Generated companion files (Form1.Designer.cs, Resources.Designer.cs, Settings.Designer.cs) are shown nested under their owning file in the project tree in both IDEs for standard WinForms/resx/settings item types. However, Rider's nesting engine is documented as pattern/suffix-based (configurable under a File Nesting setting) rather than reading arbitrary MSBuild DependentUpon metadata the way… |
| Grpc.Tools MSBuild-driven protobuf codegen | both | Adding Grpc.Tools plus a <Protobuf Include="..." GrpcServices="Server\|Client\|Both\|None"/> item is IDE-agnostic: protoc runs as an MSBuild step on every build, writing generated C# straight into obj/<Config>/<TFM>/... as an uncommitted build artifact (not added to the project or nested in the tree). Both VS and Rider simply build the project via MSBuild, so codegen behavior is identical… |
| No bundled .proto language support in Rider | rider | Rider has no built-in .proto syntax highlighting, navigation, or completion; users install the third-party 'Protocol Buffers' plugin from the JetBrains Marketplace for that. |
| Automatic regeneration is per-file-save only, cross-file triggering needs a 3rd-party extension (VS) | vs | VS's built-in custom-tool trigger is strictly 'rerun when this exact file is saved.' When a template's output actually depends on a separate input file (a shared model, a data file read via File.ReadLines), VS has no native way to cascade the regeneration — documented workarounds rely on a community extension (AutoRunCustomTool) that adds a 'Run custom tool on' property listing trigger files. |
| Extension-wide trigger granularity for Custom Tools (Rider) | rider | Rider's Custom Tools setting triggers per file-extension, not per specific input file: any edit to any file matching the configured extension list fires the tool, rather than VS's single-item 'this exact file was saved' model. |
| No Community/Professional/Enterprise gating in this domain (VS) | vs | None of T4, Custom Tool/single-file generators, ResX/Settings generators, the WinForms/WPF designers, LINQ to SQL tools, or the EF6 Entity Designer are restricted to paid VS SKUs — all are available in the free Visual Studio Community edition (LINQ to SQL tools require enabling the optional 'Individual Component' but at no extra cost). |
| Rider has no free tier | rider | Unlike VS Community, JetBrains Rider itself is a fully commercial product (subscription-priced) with no perpetually free edition — only a time-limited trial, plus free licenses for students, classroom teaching, and qualifying open-source projects. |

---

## Non-C# files inside a C# solution: MSBuild, XML, config, resources

| Feature | IDE | What it is |
| --- | --- | --- |
| MSBuild Editor — property/item/task completion | vs | The free, open-source 'MSBuild Editor' extension (mhutch.MSBuildEditor, MIT licensed) adds rich contextual completion for MSBuild elements, attributes and expressions in .csproj/.props/.targets — including property names, item names, item metadata, task names/parameters, condition comparisons, culture names, property functions and item functions. It scans every imported file (including the SDK's… |
| MSBuild Editor — Go to Definition / Find References across imports | vs | Ctrl+Click or F12 on a property, item, target, SDK reference or <Import> jumps to where it is declared, resolving through the full import chain (Directory.Build.props, SDK targets, NuGet-restored .props/.targets). 'Find References' does a precise cross-file search for all usages of a property/item/metadata/task, and Quick Info tooltips show file paths for SDK/import references. When an import… |
| MSBuild Editor — analyzers, quick fixes and refactorings | vs | Roslyn-style analyzers run over MSBuild files: warns when PackageReference conditions pivot on things other than TargetFramework, flags redundant TargetFrameworks vs TargetFramework usage, flags unused properties/items, offers spelling-correction quick fixes for mistyped property/item names, and an 'Extract Expression' refactoring to pull a literal out into a new property. |
| MSBuild sidecar JSON schema format | vs | MSBuild Editor is powered by a JSON-based schema format (distinct from XSD) that documents properties, items, metadata and targets with types and descriptions. Built-in schemas cover the .NET SDK and common targets, and NuGet package authors can ship a sidecar schema file alongside their package so consumers get IntelliSense for that package's custom MSBuild properties/items. |
| Rider baseline .csproj/.props/.targets editing (no plugin) | rider | Without any plugin, Rider edits MSBuild files as XML with SDK-aware highlighting/formatting and enough understanding of well-known SDK properties/items to drive its own project-loading logic, but it does not offer the same breadth of inferred completion, target/task completion, or cross-import Go to Definition that the VS MSBuild Editor extension provides. Users have reported Rider not always… |
| MSBuild DevKit plugin (third-party) | rider | A community plugin ('MSBuild DevKit', JetBrains Marketplace) adds a dedicated Run Configuration type for running/debugging individual MSBuild targets/tasks from inside Rider, plus file templates for creating new .props/.targets ('MSBuild Item') files. It is maintained separately from JetBrains and updated for current Rider versions (2026.1.x at last check). |
| MSBuild script debugging (breakpoints in .targets/.props) | vs | An old but still-functional and undocumented VS capability: after enabling 'Just My Code' and setting debuggerenabled=true under HKLM\SOFTWARE\Microsoft\MSBuild\4.0, running msbuild /debug lets you set breakpoints directly on XML elements (properties, items, imports) inside project files, step line-by-line with F10/F11, inspect Locals/Watch windows, see a call stack of the import chain, and use… |
| Evaluated items/properties via -getProperty/-getItem CLI | both | MSBuild 17.8+ supports -getProperty:<Name> and -getItem:<Name> command-line switches to print evaluated (or, with -target, post-build) property/item values without a full build. Neither VS nor Rider exposes this as a GUI property inspector — both effectively delegate 'what is this property's evaluated value' to the command line or the unsupported MSBuild debugger's Immediate-window functions. |
| MSBuild binary log / Preprocess view (external tool) | both | Neither VS nor Rider ships a built-in binlog viewer. Both communities rely on the free, independently-maintained MSBuild Structured Log Viewer (msbuildlog.com / MSBuild.StructuredLogger) to open a .binlog, browse the executed target/task/property/item tree, full-text search embedded files, and right-click a project → 'Preprocess' to see it with all imports inlined (equivalent to msbuild /pp). |
| .slnx — new XML solution file format | both | A new, much more concise XML-based solution format (.slnx) reached stable/GA support in Visual Studio 17.14 and .NET SDK 9.0.200 (dotnet sln migrate converts an existing .sln). JetBrains describes Rider's support as 'preliminary.' The old .sln text format is explicitly documented by Microsoft as 'not intended to be edited manually.' |
| Generic XML editor with XSD/DTD schema association | both | Both IDEs provide a full XML editor with schema-based completion/validation once an XSD or DTD is associated with a document — VS via the file's Properties window 'Schemas' entry (or an inferred/xsi:schemaLocation reference), Rider via Settings/Preferences → Languages & Frameworks → Schemas and DTDs, which lets you map a namespace/root-element to a local schema file solution-wide. |
| app.config / web.config editing | both | Standard <system.web>, <appSettings>, <connectionStrings> etc. sections get schema-driven completion/validation from machine.config-derived schemas in both IDEs out of the box. Custom <configSections> types get no automatic IntelliSense in either IDE unless the developer manually supplies/associates a matching XSD. |
| Native Web.config transforms (Add Config Transform / Preview Transform) | vs | Built into VS at no extra cost for ASP.NET Web Application / Web Site projects: right-click Web.config → 'Add Config Transform' generates Web.<Configuration>.config files per build configuration; right-click a transform file → 'Preview Transform' shows a side-by-side diff of the untransformed vs. transformed config before publishing. |
| SlowCheetah — XDT transforms for any XML file/project type | vs | Free, open-source VS extension (vscps.SlowCheetah-XMLTransforms) that generalizes the Web.config transform mechanism (XDT) to app.config and arbitrary XML files in non-web project types (console apps, class libraries, WinForms/WPF) — something VS's native transform support doesn't cover. Adds its own 'Preview' command and auto-adds the required NuGet package when a transform file is added. |
| appsettings.json well-known schema IntelliSense | vs | Visual Studio (and VS Code with the C# extension 1.23.8+) natively recognizes appsettings.json and offers completion/validation for well-known ASP.NET Core configuration keys (Logging levels/categories, Kestrel, AllowedHosts, etc.) without any setup, driven by a JSON schema. Any appsettings.json file can also opt into a custom/company schema via a top-level "$schema" property, and appending… |
| Rider/ReSharper options-binding navigation for appsettings.json | rider | Rider's ASP.NET Core tooling can connect configuration keys read in appsettings.json to the C# types/classes that bind to them via the options pattern (IOptions<T>, configuration.GetSection), enabling navigation from a JSON key toward the consuming code rather than only validating against a static schema. |
| .NET User Secrets management | both | Both IDEs offer a native, no-extension-required entry point to user secrets: VS via right-click project → 'Manage User Secrets' (adds a UserSecretsId to the project file and opens secrets.json), Rider via right-click project → Tools → '.NET User Secrets'. Both simply open the underlying JSON file in the user profile for direct editing; neither offers a masked/secure-entry UI. |
| Visual Studio Resource Explorer (.resx) | vs | Revamped native .resx editor shipped by default starting VS 17.11 Preview 2 (View → Other Windows → Resource Explorer, or double-click a Resources folder/file). Loads multiple .resx files at once, shows all cultures side by side in a filterable grid, supports in-place editing with per-translation comments, search that highlights matches across files, validation warnings for missing… |
| Rider Localization Manager (.resx) | rider | Native Rider tool window (since 2020.2) that scans all .resx files in the solution, groups them by resource bundle, and shows one row per resource key with one column per culture in an editable grid. Supports checkbox selection/grouping (by project, solution folder, bundle) to build a custom working set, and — distinctively — CSV import/export so translators without an IDE or source access can… |
| Rider code↔resx navigation and 'move to resource' refactoring | rider | Rider offers dedicated navigation between a Resources.Designer-generated reference in code and its underlying .resx entry, plus a context action that turns a raw string literal in C# into a new/existing resource entry (with a corresponding code-side reference) in one step. |
| Rider native .editorconfig authoring support | rider | Rider understands standard EditorConfig properties plus its own large set of custom properties — every single formatting/syntax-style/naming/inspection-severity option exposed in Rider's Settings dialog has a corresponding .editorconfig property, all with completion and validation when editing the file directly. Rider can also analyze a code sample and export the detected style as .editorconfig… |
| VS .editorconfig editing gap (third-party extension required) | vs | VS fully applies/enforces .editorconfig settings when formatting code, but the editor for the .editorconfig file itself has no native IntelliSense out of the box. The commonly-used fix is Mads Kristensen's free 'EditorConfig Language Service' extension (VS 2022/17.0+), which adds keyword/value completion and code snippets (Ctrl+K, Ctrl+X) for authoring the file. |
| VS .runsettings native IntelliSense + active-file selection UI | vs | VS ships the vstest_runsettings.xsd schema in-box (default path C:\Program Files\Microsoft Visual Studio\2022\<Edition>\Xml\Schemas\1033\vstest_runsettings.xsd), giving native element/attribute completion when editing a .runsettings file. The Test menu also has a UI to select which .runsettings file is 'active' for a solution (including an autodetect mode), rather than requiring the developer to… |
| Rider .runsettings manual configuration (no solution-root auto-detect) | rider | Rider supports pointing unit test runs at a .runsettings/.testsettings file via Settings → Build, Execution, Deployment → Unit Testing → Test Runner ('Use specific .runsettings/.testsettings file'), or via a RunSettingsFilePath MSBuild property in the test project. Unlike VS, it does not automatically pick up a .runsettings file simply because it sits at the solution root — it must be pointed to… |
| VS launchSettings.json GUI (Debug launch profiles) | vs | Project Properties → Debug tab (or Debug menu → '{Project} Debug Properties') opens a form-based UI backed directly by launchSettings.json: create/rename/duplicate profiles, set command-line args, environment variables, launch browser, app URL, etc. Not every setting is exposed in the UI (e.g. useSSL) — those still require hand-editing the JSON. |
| Rider launchSettings.json auto-import as Run/Debug configurations | rider | On opening a project, Rider automatically imports every profile in launchSettings.json as a native Run/Debug configuration. Fields sourced from the JSON are shown read-only in the configuration dialog (edit the file to change them, with syntax highlighting/completion available); a few Rider-added fields (target framework, extra runtime args) are editable directly in the dialog. |
| Language injection in C# strings — Rider's full IDE experience | rider | Rider treats a string literal as containing another language (SQL, JSON, HTML, CSS, regex, JavaScript, XML, YAML, even MSBuild) and gives it real syntax highlighting, completion, and code analysis inline. Activation is via the JetBrains.Annotations [LanguageInjectionAttribute] on a parameter/property/field, a `//language=X` comment placed before the literal, automatic detection for unambiguous… |
| VS embedded-language support — StringSyntaxAttribute (coloring only) | vs | Since .NET 7, a parameter/property/field can be tagged with [StringSyntaxAttribute("Json"\|"Regex"\|"DateOnlyFormat"\|...)] and Visual Studio will apply matching syntax highlighting to string literals passed there. Unlike Rider's injection system, this is coloring-focused; VS does not run a full embedded language service (completion, diagnostics, an editable fragment view) over the tagged… |
| .sln / SDK-style .csproj round-trip and in-place editing | both | Both IDEs let you edit an SDK-style .csproj in place without unloading the project first (VS: right-click → 'Edit Project File'; Rider: open the file directly from the project node or Explorer). Microsoft's own docs explicitly warn the legacy .sln text format 'isn't intended to be edited manually' — both IDEs mediate .sln changes through Solution Explorer UI actions (add/remove project, reorder,… |

---

## Service references, WCF, SOAP and connected services

| Feature | IDE | What it is |
| --- | --- | --- |
| WCF Web Service Reference connected service (Add Service Reference wizard) | vs | For .NET 5+/.NET Core/.NET Standard/ASP.NET Core projects: double-click the Connected Services node (or right-click project > Add > Connected Service) > Add Service Reference > choose WCF Web Service. Opens the 'Add new WCF Web Service service reference' wizard, downloads WCF metadata, and generates a client proxy in Reference.cs nested under Connected Services. |
| Three metadata-discovery paths in the WCF wizard: Discover / Address+Go / Browse WSDL | vs | In the service-selection step, Discover finds WCF/OData services already in the current solution, Address+Go queries a live service URL (prompting for credentials if secured), and Browse lets you point at a local WSDL file on disk. |
| Data type options page: 'Reuse types in referenced assemblies' | vs | Second wizard step for the .NET Core WCF Web Service Reference; lets generated proxy code reuse DTOs already defined in referenced assemblies instead of re-emitting duplicates. The Finish button is disabled while type info loads unless this box is unchecked. |
| Generated Reference.cs + auto NuGet references under Connected Services node | vs | On Finish, Visual Studio writes the proxy to Connected Services > ServiceReference > Reference.cs and updates the .csproj with the NuGet package references (System.ServiceModel.* etc.) needed to compile/run. |
| Legacy 'Add Service Reference' dialog (.NET Framework) | vs | Right-click a .NET Framework project > Add Service Reference. Address box + Go to query a URL, Discover > 'Services in Solution' to find in-solution services, Namespace box for the generated namespace, OK generates the proxy and writes endpoint metadata into app.config. Requires the 'Windows Communication Foundation' individual component from the VS Installer. |
| 'Add Web Reference' (legacy ASMX / .NET 2.0 proxy) via the Advanced button | vs | Since VS2008, the old ASMX-era Add Web Reference dialog is hidden: click Advanced in the Add Service Reference dialog to reveal an 'Add Web Reference' button, which opens the classic .NET 2.0-style web reference dialog (wraps wsdl.exe-generated proxies under a Web References node). |
| Configure Service Reference dialog — full option set | vs | Right-click a service reference > Configure Service Reference (or Project > Configure Service Reference). Exposes: Address (re-point endpoint), Access level for generated classes, Generate asynchronous operations, Generate task-based operations, Always generate message contracts, Collection type / Dictionary collection type pickers, and Reuse types in referenced assemblies with… |
| Update Service Reference (right-click, .NET Framework) | vs | Solution Explorer > expand Connected Services/Service References > right-click the reference > Update Service Reference. Re-fetches metadata from the original address/WSDL and regenerates the proxy in place, showing a progress dialog. |
| Remove service reference cleans app.config | vs | Right-click the reference > Delete removes the generated client/proxy files and strips the corresponding <client>/<endpoint> metadata from app.config automatically. Code that used the reference must still be removed manually (VS warns about this). |
| dotnet-svcutil global CLI tool + '-u' update workflow | both | `dotnet tool install --global dotnet-svcutil`, then `dotnet-svcutil <url-or-wsdl>` generates ServiceReference/Reference.cs plus a ConnectedService.json describing the source. `dotnet-svcutil -u ./path/to/reference/folder` reads that ConnectedService.json and regenerates in place. Cross-platform (Windows/macOS/Linux); this is what Microsoft's own docs point .NET Core users to for updating a WCF… |
| Legacy svcutil.exe (.NET Framework SDK tool) | both | Ships with the Windows SDK / installed via the VS 'Windows Communication Foundation' individual component. Command-line proxy generator underlying the classic Add Service Reference dialog and WCF Test Client; supports switches like /serializer, /reference, /importXmlTypes, /out, /config, /noConfig, /language. |
| WCF Service Host (WcfSvcHost.exe) auto-starts on F5 | vs | Pressing F5 on a WCF service project (class library or website) automatically enumerates the services, loads the project's config, and hosts each service locally without requiring a dedicated host process to be written. |
| WCF Test Client (WcfTestClient.exe) | vs | Auto-launches alongside WCF Service Host on F5 (also runnable standalone from Common7\IDE, or via `wcfTestClient.exe URI1 URI2…`). Lists services/endpoints/operations in a tree, lets you enter Formatted or raw-XML request parameters and Invoke, view responses, toggle session/proxy reuse ('Start a new proxy'), edit the generated client config via SvcConfigEditor, and Add/Remove/Refresh services… |
| Step into WCF services across projects in the same solution | vs | With the WCF client project set as startup and debugging enabled in app.config/web.config, the VS debugger steps directly from a client call into the WCF service implementation living in a different project of the same solution, hitting breakpoints there. |
| WCF project/item templates (Service Library, Service Application, WCF Service item, AJAX-enabled WCF Service) | vs | Add New Project offers 'WCF Service Library' and 'WCF Service Application' (website); Add New Item offers a 'WCF Service' item template that scaffolds a paired IService.cs contract interface, Service.cs implementation, and .svc host file with attributes pre-applied. |
| Data Sources window binding of service-reference composite types | vs | Data > Show Data Sources lists composite types returned by an added service reference; dragging a type or field onto a WinForms/WPF form auto-generates bound controls (DataGridView or per-field Details) plus BindingSource/BindingNavigator components. |
| WCF Data Services (OData) client reference generation | vs | The same Add Service Reference / Connected Services dialogs handle WCF Data Services (OData) endpoints identically to SOAP WCF services — Discover/Address/Browse, entity-set selection, namespace entry. |
| Connected Services hub covering OpenAPI, gRPC, WCF, Azure services, and databases in one UI | vs | Right-click project > Add > Connected Service (or the Connected Services Solution Explorer node) opens a single gallery listing providers for Azure services, OpenAPI endpoints, gRPC endpoints, WCF endpoints, and databases/data providers, each with a tailored wizard. |
| OpenAPI connected service via NSwag / Microsoft.dotnet-openapi | vs | Add Connected Service > OpenAPI accepts a local spec file or live URL, invokes the Microsoft.dotnet-openapi global tool (NSwagCSharp by default, NSwagTypeScript optional) and adds an <OpenApiReference Include="..." Namespace="..."> item to the .csproj. Generated client is a partial class specifically so hand customizations survive regeneration. The same dialog's Add/Remove/Refresh commands… |
| gRPC connected service (proto-based client/server scaffolding) | vs | Add Connected Service > gRPC generates C# client or server stubs from a .proto file via Grpc.Tools, wired into the Connected Services node alongside WCF/OpenAPI references. |
| Azure connected-service catalog (App Config, App Insights, App Service, Functions, Storage, SignalR, Key Vault, SQL DB, Redis Cache, Cosmos DB, Microsoft Identity Platform) | vs | Each Azure service has a dedicated Connected Services provider that wires up SDK NuGet packages and configuration/connection info with a guided wizard; many are usable with free Dev/Test Azure credits. |
| Local service-dependency emulators + serviceDependencies.json local/cloud transition | vs | Connected Services can wire local containerized emulators (Azurite, Cosmos DB emulator, MongoDB/PostgreSQL/RabbitMQ/Redis on container, SQL Server Express LocalDB, Secrets.json). Config is tracked in Properties/serviceDependencies.json and serviceDependencies.local.json (safe to commit) plus a gitignored serviceDependencies.local.json.user for secrets. After Publish, the Connected Services tab… |
| Third-party VS Connected Service OpenAPI extensions (Unchase, dmitry-pavlov) adding more generators | vs | Free VSIX extensions ('OpenAPI (Swagger) Connected Service', 'Unchase OpenAPI (Swagger) Connected Service') register additional providers in the same Connected Services gallery, letting you pick NSwag, Refitter, OpenAPI Generator, Swagger Codegen CLI, Kiota, or AutoRest as the code generator instead of being limited to the built-in NSwag-only provider. |
| Endpoints Explorer + .http file editor | vs | View > Other Windows > Endpoints Explorer lists ASP.NET Core controller and Minimal API endpoints, discovered statically at design time and (since VS 17.11 Preview) dynamically at run time for endpoints in class libraries. Right-click a listed endpoint > Generate Request writes a request into a project-named .http file, which the built-in .http editor can send with Send Request/Debug links,… |
| Rider Endpoints tool window | rider | Discovers ASP.NET Core controller-based endpoints, Minimal API endpoints (including route groups, since 2022.3), OpenAPI-described endpoints, and gRPC endpoints (with the gRPC plugin installed). F12/double-click an endpoint navigates to its source (a .proto location for gRPC); it can generate a runnable HTTP/gRPC request for the selected endpoint, and Rider's rename refactoring keeps endpoint… |
| Rider built-in OpenAPI language support | rider | Languages & Frameworks \| OpenAPI Specifications lets Rider create/edit YAML or JSON specs with live templates (opnp, swag), $ref keyword completion/validation with Ctrl+B navigation, endpoint-URL and request-body completion, and an integrated Swagger UI / Redoc preview pane (splittable horizontally or vertically with the editor). |
| Rider OpenAPI client/server code generation (gutter run icon) | rider | Clicking the run icon in the gutter of a valid OpenAPI file offers 'Run openapi file', invoking OpenAPI Generator (versions 7, 6, 5, 4) or Swagger Codegen (versions 3, 2) to produce client code. The run configuration (Run \| Edit Configurations) exposes output directory, generator version, target language, custom Mustache template paths, and generator-specific parameters. |
| Rider OpenAPI spec comparison and remote-spec reload | rider | Rider can diff two OpenAPI specifications and generate a Markdown compatibility report, and offers a 'Reload' action to re-pull a registered remote spec that has changed. |
| Rider gRPC plugin (separate from the bundled Protocol Buffers plugin) | rider | Rider's bundled Protocol Buffers plugin gives .proto syntax highlighting only; the full gRPC experience (endpoint discovery, navigation) requires separately installing the gRPC plugin via Settings/Preferences > Plugins > Marketplace. |
| Rider 'Windows Communication Foundation (WCF) tools' plugin (svcutil.exe wrapper) — archived | rider | Community plugin (by Maarten Balliauw) on the JetBrains Marketplace that generates a WCF client by invoking a local svcutil.exe given a WSDL URL, with configurable namespace/output filename and svcutil.exe path, and logs errors to the event log. Its source repo was archived January 2019 and the Marketplace listing itself is tagged 'deprecated' with 811 total downloads. |
| Rider 'Add Web reference' (Solution menu, ASP.NET projects only) | rider | From Rider 2018.1, right-click the solution and choose Add > Add Web reference — but JetBrains support documentation states this only applies to ASP.NET (Framework) projects, not general WCF/SOAP consumption. |
| Rider gap: no native Add Service Reference / WCF Web Service Reference wizard | rider | Rider has no built-in equivalent of VS's Add Service Reference or WCF Web Service Reference dialogs. JetBrains support threads direct users either to the archived svcutil.exe plugin or to running the cross-platform dotnet-svcutil CLI tool manually from Rider's terminal. |
| Rider interop gap: doesn't reproduce VS's multi-file ServiceReference output | rider | When Visual Studio adds a legacy web reference it downloads and stores multiple supporting files under the ServiceReference/Web Reference directory; Rider does not replicate that file set when opening or working with such a project, which can leave classes from the reference inaccessible. |
| Rider gap: no .svc/.asmx-aware scaffolding or WCF Framework tutorial coverage | rider | Rider's official 'Get started with ASP.NET' documentation covers only Razor Pages, MVC, Blazor Web App, and Web API project types — WCF, ASMX, SOAP, .svc files, and Connected Services are not mentioned; there is no bundled WCF/ASMX item-template scaffolding analogous to VS's WCF Service item template. |
| REST API Client Code Generator plugin for Rider (third-party, free) | rider | JetBrains Marketplace plugin adding a context-menu code-generation action on OpenAPI/Swagger JSON and YAML files, supporting NSwag, Refitter, OpenAPI Generator, Kiota, Swagger Codegen CLI, and AutoRest as pluggable back-ends for C#/TypeScript client generation, configurable per-project (namespace, output directory). |

---

## Debugger visualizers, the exception helper, and thread control

| Feature | IDE | What it is |
| --- | --- | --- |
| Built-in String Visualizer (Text/XML/HTML/JSON) | vs | Hover a DataTip on a Text/XML/HTML/JSON-shaped string and click the magnifying-glass icon to open a dedicated viewer per format. XML/JSON viewers show blank if the string doesn't parse as that format; HTML renders as a browser would. |
| Text Visualizer string manipulation (Base64/URL/JWT decode) | vs | Inside the Text Visualizer, a 'String manipulation' dropdown offers Base64 Encode/Decode, URL Encode/Decode, and JWT Decode transformations applied in place, plus Ctrl+F highlighted search. |
| Auto-detect and format (Copilot-powered) | vs | A button in the Text Visualizer that auto-identifies a string's encoding/compression (e.g. GZip+Base64) and decodes it in one click. |
| DataSet/DataTable/DataView tabular visualizer | vs | Built-in grid visualizer for System.Data.DataSet, DataTable, DataView, and DataViewManager objects, opened the same way as the string visualizer. |
| IEnumerable Visualizer | vs | Introduced in VS 17.2: a sortable, column-based grid for any IEnumerable<T> (arrays, lists, complex objects). Supports expand/hide column for nested properties, picks between multiple IEnumerable implementations on one object, and exports to Excel (CSV planned). |
| Legacy DebuggerVisualizerAttribute model (VSSDK) | vs | Author a DialogDebuggerVisualizer subclass in a .NET Framework class library, mark the debuggee-side type with [assembly: DebuggerVisualizer(...)], and it appears via the magnifying-glass icon. Supports a VisualizerDevelopmentHost test harness to run without installing into VS. |
| New out-of-process VisualStudio.Extensibility visualizer model | vs | Starting VS 2022 17.9, visualizers can be .NET 6+ and run out-of-process via DebuggerVisualizerProvider/CreateVisualizerAsync, with a VisualizerTarget.ObjectSource for chunked RequestDataAsync calls on large objects, and an optional Style=ToolWindow to stay open non-modally across debug-state changes. |
| Rider 'Pin to Top' object presentation customization | rider | Right-click a member in the Variables view and pin it; pinned members surface at the top of the object's node and contribute to its inline string presentation, overriding a type's own ToString()/[DebuggerDisplay] result. Pins persist with the solution. |
| Rider Natvis support (C++/Unreal only) | rider | Rider's LLDB-based native debugger loads .natvis files (Inheritable, format specifiers, CustomListItems) for C++/Unreal Engine type visualization, extended to Linux/macOS in Rider 2026.2. |
| Rider has no dedicated String/JSON/XML/HTML visualizer | rider | Rider's debugger docs show no equivalent to VS's magnifying-glass Text/XML/HTML/JSON viewer, Base64/URL/JWT decode toolbar, or the IEnumerable/DataSet tabular visualizer — inspection is generic tooltip/value-view plus 'truncate long strings' and property-evaluation settings under Data Views. |
| Exception Helper inline, non-modal card | vs | When the debugger breaks on an exception, a non-modal card appears anchored to the exception icon on that source line, showing the exception type, message, and whether it was thrown vs unhandled, with a 'View Details' link to the full exception object. |
| Null-reference / AccessViolation root-cause analysis | vs | For NullReferenceException/AccessViolation, the Exception Helper shows plain-text analysis under the message (e.g. 's was null'). Requires .NET 4.6.2+ managed code without JIT optimizations; unsupported on UWP/other .NET Core targets historically. |
| Inner-exception navigation in Exception Helper | vs | If the exception has an InnerException, left/right arrows above the call stack let you page through the chain of nested exceptions from within the same Exception Helper card. |
| Async exception auto-break (.NET 9+) | vs | Starting with .NET 9, VS automatically breaks when an async Task method throws inside .NET framework code, even across await boundaries. |
| Exception stack frames inside Call Stack window | vs | Since VS 2022 17.3, exception stack frames (including for async exceptions) appear directly in the Call Stack window with the usual symbol-load/decompile/navigate features. |
| Copilot exception analysis ('Analyze with Copilot') | vs | An 'Analyze with Copilot'/'Ask Copilot' button in the Exception Helper opens Copilot Chat pre-loaded with the exception's runtime context; with an Azure DevOps repo it also cross-references past PRs/issues for similar fixes. |
| Exception Settings window granularity | vs | Debug > Windows > Exception Settings lists exceptions by category (Common Language Runtime, Win32, JavaScript Runtime, GPU Memory Access, etc.); you can check a whole category or one type, search/filter (including by namespace), add custom/generic exception types (`Namespace.Generic\`1` syntax for generics), delete types, and restore defaults. Checking a type breaks on it as a first-chance… |
| Per-module exception conditions ('Except when thrown from') | vs | Edit Conditions on a given exception type lets you add module-name Equals/Not-Equals rules (with wildcards) so the debugger only breaks — or explicitly skips breaking — when that exception originates from specific modules. |
| 'Continue When Unhandled in User Code' | vs | With Just My Code on, an 'Additional Actions' column in Exception Settings can be set per-type or per-category to 'Continue when unhandled in user code' — the debugger won't break if the exception is unhandled in your code but is caught externally (e.g. ASP.NET's top-level handler converting it to a 500). |
| First-chance vs unhandled vs user-unhandled semantics | vs | VS distinguishes a first-chance exception (any throw, before the debugger knows if it'll be caught), a fully unhandled exception, and — only under Just My Code — a 'User-Unhandled' exception (caught somewhere in External Code but not in your code). |
| Rider 'Stop On Exception' / CLR Exception Breakpoints | rider | Run \| Stop On Exception (or Ctrl+Shift+F8 > add > CLR Exception Breakpoints) creates a breakpoint keyed to an exception type name rather than a source line; it suspends before any handler runs. A built-in 'Any exception' breakpoint matches all CLR exceptions and can be selectively overridden per type. |
| Rider exception breakpoint suspend conditions | rider | Each exception breakpoint has Enabled, Suspend execution, and 'Suspend if' checkboxes scoped by where the exception is thrown (user code vs external code) and its handled/unhandled state, plus log-hit / log-stack-trace options and dependent ('disable until another breakpoint is hit') chaining. |
| Rider exception popup actions | rider | When a matching exception is hit, Rider shows a popup with Stop, Mute and Resume (creates an ignore-breakpoint for that case), Resume, Stack Trace Explorer, View Breakpoint, and Create breakpoint (scoped to that exception/context). |
| VS Threads window: flag/unflag, freeze/thaw, naming | vs | The Threads window supports flagging threads of interest, freezing (suspending) and thawing (resuming) individual threads to control execution order for deadlock/race investigation, grouping/sorting/searching, and shows names set via SetThreadDescription (or a special-exception trick). |
| 'Show Threads in Source' | vs | A gutter marker (twisted-thread icon) appears next to any source line where another thread is currently stopped; hovering shows a DataTip with thread name/ID, and right-clicking offers 'Switch to' that thread. |
| Parallel Stacks window (Threads/Tasks/Method View) | vs | A graph view of all call stacks. Threads view groups stacks by shared call paths; Tasks view (async/await, TPL) does the same for tasks; Method View pivots the graph on the current method to show all callers/callees across threads. Toolbar adds Filter, 'Show Only Flagged', deadlock/blocked/waiting status icons, a Bird's Eye View for large graphs, zoom, search, and export-as-image. |
| Parallel Stacks AI thread descriptions / 'Generate Insights' | vs | With Copilot installed and active (17.13+), each thread node gets an AI-generated one-line description, and a 'Summarize'/'Generate Insights' button opens Copilot Chat preloaded with the thread's state, calls, and suggested fixes. |
| Tasks window | vs | Thread-window analog for async/await Task-based-Asynchronous-Pattern work: columns for Status (Scheduled/Active/Blocked/Awaiting/Deadlocked via Wait Chain Traversal), AsyncState, Parent (for a Parent/Child hierarchical grouping of managed tasks), Thread Assignment, Start/Duration/Completion time. Supports flag/freeze/thaw of a task's assigned thread and Switch to Task/Switch to Frame. |
| Parallel Watch window | vs | Evaluates a single watch expression simultaneously across every thread/task, one row per matching stack frame, so you can compare a variable's value across all concurrently running contexts at once. |
| Rider Threads & Variables pane + Parallel Stacks/Tasks tabs | rider | The Debug tool window's Threads & Variables pane lists all threads and, per selected thread, its stack frames and locals; a Parallel Stacks tab renders threads/stacks as a diagram (Rider's analog to VS's Parallel Stacks Threads view); a Tasks tab shows Task/ValueTask objects at the current break point. |
| Rider Freeze/Unfreeze Thread | rider | Right-click a thread for Freeze Thread / Freeze Other Threads (and the inverse Unfreeze commands); frozen threads get a dedicated icon. Documented as supported on .NET Framework and .NET Core but not Mono/Unity. |
| Rider async call stack reconstruction | rider | Rider expands the physical call stack into the full async causality chain (continuations, not just the live thread's frames) so you can answer 'how did execution get here' across await boundaries; Rider 2026.2 EAP specifically reduces noise from compiler-generated async/task infrastructure frames in that view. |
| VS Code proposed DebugVisualization API | vscode | A still-proposed (vscode.proposed.debugVisualization.d.ts) extension API: registerDebugVisualizationProvider/registerDebugVisualizationTreeProvider let an extension supply a DebugVisualization (name + icon + either a command or a DebugVisualizationTree) for a DAP Variable matched by a package.json 'debugVisualizers' when-clause; DebugVisualizationTree implements getTreeItem/getChildren/editItem… |
| VS Code generic per-view focus commands (Threads view) | vscode | VS Code exposes generic 'Focus on <X> View' command-palette commands for Run-and-Debug sub-views (Variables, Watch, Call Stack, Threads, Breakpoints) that move keyboard focus into that panel for accessibility/keyboard-driven navigation. |
| Just My Code toggle interaction with exceptions | vs | Just My Code (Tools > Options > Debugging) divides running code into 'your code' vs system/external code; it gates whether the 'User-Unhandled' exception classification and the 'Continue When Unhandled in User Code' Exception Settings column exist at all — turning it off hides that column. |
| Live Share in Exception Helper | vs | A 'Start Live Share session...' link inside the Exception Helper lets a joined collaborator see the same Exception Helper card and debug context. |

---

## Workspace trust, analyzer/generator sandboxing and supply-chain safety

| Feature | IDE | What it is |
| --- | --- | --- |
| Workspace Trust prompt + Restricted Mode | vscode | Opening a folder VS Code hasn't seen before shows a trust prompt; declining puts the window in Restricted Mode, a UI state (not a sandbox) that disables or limits features geared toward safe code browsing. |
| untrustedWorkspaces capability declaration | vscode | Extensions declare support in package.json via capabilities.untrustedWorkspaces.supported: true (fully works), false (disabled entirely in Restricted Mode, with a required description string), or 'limited' (partial function, trust-sensitive features gated). |
| restrictedConfigurations for settings | vscode | Extensions list setting IDs (e.g. a path to an external tool or MSBuild binary) in untrustedWorkspaces.restrictedConfigurations; VS Code then ignores the workspace-level value of those settings in Restricted Mode and only honors the user/global value. |
| workspace.isTrusted API and onDidGrantWorkspaceTrust event | vscode | Extensions read workspace.isTrusted synchronously and subscribe to onDidGrantWorkspaceTrust to detect the moment a user trusts a previously-restricted workspace, so they can start deferred work (e.g. spin up a language server) only then. |
| isWorkspaceTrusted context key gates tasks/debug/extensions | vscode | VS Code uses the isWorkspaceTrusted when-clause context key to hide/disable tasks.json execution, launch.json debugging, and workspace-level extension recommendations; the docs explicitly warn a command can still be invoked programmatically even if hidden from UI, so extensions must gate in code too. |
| Mark of the Web (MotW) integrated trust warning | vs | Since VS 2022 17.12, opening content tagged with Windows' Mark of the Web (internet-zone) identifier triggers a security warning integrated into the general Trust Settings flow, rather than a separate legacy dialog. |
| Trust Settings / Trusted Locations list | vs | Tools > Options > Environment > Security > Trust (VS 2022) / All Settings > Environment > Security > Trust (newer 'visualstudio' channel) maintains a Trusted Locations list; everything opened is untrusted until its folder is added, either from the warning dialog's 'Trust and continue' button or manually via '+ Add'. |
| Trust level granularity in the warning dialog | vs | The trust warning dialog's 'Trust level' dropdown lets the user pick the scope to trust — the specific folder or its parent folder — before continuing, rather than an all-or-nothing choice. |
| 'Always require a trust decision' policy + roaming settings | vs | An opt-in stricter mode ('Require a trust decision before opening content' + 'Trust decision prompt: Always ask' in the newer channel, or 'Always require a trust decision' in VS 2022) forces a prompt for any non-trusted content; a separate 'Skip trust checks for temporary locations Visual Studio creates automatically' toggle (off by default) only matters once always-ask is on. Trust settings… |
| Group Policy centralized trust management | vs | Organizations can use Windows Group Policy to centrally manage VS's trust experience, including restricting users to a pre-approved list of repositories/locations they're allowed to trust. |
| Untrusted Git repository detection | vs | Following Git's own ownership-verification check (Git ≥2.35.2, CVE-2022-24765), VS flags a repo as untrusted when it was created by a different Windows account than the current one; a bottom-right notification and the Git Changes window (which shows no changes) surface it, with 'Trust repository' / 'View Trust Details' > 'Trust and continue' to resolve. |
| Restricted Mode for untrusted code (execution-disabling) | vs | When content is untrusted, VS disables execution-driving features — debugging/launch (to stop accidental execution of a downloaded binary), workspace-defined tasks, and places extensions/analyzers that could run code into a limited state — conceptually mirroring VS Code's Restricted Mode. |
| T4 template transformation Security Warning | vs | Because a T4 (.tt) text template embeds arbitrary C#/VB control-logic that runs at design time when transformed, VS shows a 'Security Warning' message box asking the user to confirm before running the template (individually or via Build > Transform All Templates). |
| MCP server trust fingerprinting (Copilot Chat) | vs | As of the June 2026 update, VS validates any configured MCP server's identity/config before it starts and re-validates the fingerprint of its advertised tools/prompts/resources/instructions after each start, prompting the user to Trust / 'Do not trust' when something changed since the last-trusted baseline (toggle: Tools > Options > GitHub > Copilot > Copilot Chat > 'Show trust dialog before… |
| Roslyn analyzers run out-of-process (ServiceHub) | vs | Design-time Roslyn analyzer execution is isolated into ServiceHub.RoslynCodeAnalysisService.exe / ServiceHub.RoslynCodeAnalysisService32.exe rather than running inside devenv.exe directly. |
| 'Run code analyzers only on build' option | vs | Tools > Options > Text Editor > C# > Advanced lets a user disable live/background analyzer execution and defer analysis to build time only. |
| Trusted/Untrusted solution dialog (Trust Project / Preview in Safe Mode / Don't Open) | rider | On first opening a solution/project, Rider (built on the IntelliJ platform trust model) presents three choices: Trust Project (full init: MSBuild evaluation, NuGet restore, plugin resolution, all features), Preview in Safe Mode (browse-only), or Don't Open. The decision persists per location so it isn't re-asked on subsequent opens. |
| Safe Mode restrictions (build-script import disabled) | rider | Per the shared IntelliJ platform docs, Preview/Safe Mode disables: build-tool import (no MSBuild/Gradle/Maven/sbt evaluation, so no build scripts execute and no dependencies resolve), startup tasks/scripts, VCS integration (fully disabled), GDSL script execution, and File Watchers. |
| Trusted Locations settings page | rider | Settings/Preferences (Ctrl+Alt+S) > Appearance & Behavior \| Trusted Locations lets a user pre-register directories; solutions opened from a listed path are implicitly trusted with no prompt. |
| 'Trust all projects in this folder' quick action | rider | From the trust dialog, the user can select 'Trust all projects in <parent folder>' to add that whole directory to Trusted Locations in one step rather than trusting solutions one at a time. |
| In-editor 'Trust project…' promotion from Safe Mode | rider | While in Preview/Safe Mode, Rider shows a persistent notification over the editor with a 'Trust project…' link, letting the user upgrade to full trust at any later point without reopening the solution. |
| Design-time code execution risk model (MSBuild/analyzers/generators/T4) | both | Opening a .sln/.csproj causes MSBuild target/task evaluation, Roslyn analyzer and incremental source-generator assemblies to load into the compiler/design-time-build host, and (in VS) T4 templates to be eligible for transformation — all of which is unsandboxed, full-user-privilege .NET/native code execution triggered merely by loading the project, before any 'Run' or 'Build' action. |
| Generated-source visibility (EmitCompilerGeneratedFiles) | both | Setting <EmitCompilerGeneratedFiles>true</EmitCompilerGeneratedFiles> plus <CompilerGeneratedFilesOutputPath> writes generator output to disk for review/source control; both IDEs' Solution Explorer / project views can drill into the 'Analyzers' node per-generator to inspect emitted code without this flag. |
| NuGet Audit (NuGetAudit) vulnerability scanning on restore | both | Since NuGet 6.8 / VS 17.8 / .NET 8 SDK, every `restore` (triggered on first project load, package add/update/remove, or CI) checks the resolved package graph against a vulnerability database and emits warnings NU1901–NU1904 (low/moderate/high/critical) or NU1900/NU1905 for source errors. Controlled via MSBuild properties NuGetAudit (on/off), NuGetAuditLevel (min severity, default low), and… |
| Audit Sources decoupled from package sources | both | The <auditSources> nuget.config element (NuGet 6.12+) lets vulnerability-data lookups use a source (e.g. nuget.org's vulnerability-only endpoint https://data.nuget.org/v3/index.json) independent of the package sources used for actual downloads — recommended specifically for orgs using a single upstream-proxying feed instead of nuget.org directly. |
| Per-advisory suppression (NuGetAuditSuppress) | both | An `<ItemGroup><NuGetAuditSuppress Include="https://github.com/advisories/XXXX" /></ItemGroup>` entry (VS 17.11+ for PackageReference, 17.12+ for packages.config) suppresses a specific advisory repo/project-wide after it's been triaged and accepted. |
| CLI vulnerability tooling: dotnet nuget why / list --vulnerable / update --vulnerable | both | `dotnet nuget why` traces why a transitive package is pulled in; `dotnet list package --vulnerable --include-transitive` lists vulnerable packages; `dotnet package update --vulnerable` (new in .NET 10 SDK) auto-updates all vulnerable packages to their first fixed version. |
| CI audit-completeness enforcement (RestoreProjectsAuditedCount) | both | MSBuild 17.13/.NET 9.0.200+ restore exposes RestoreProjectCount, RestoreSkippedCount, and RestoreProjectsAuditedCount output properties; a Directory.Solution.targets AfterTargets="Restore" check can assert every project was actually audited (not silently skipped because it was already up to date), failing CI otherwise. |
| Package Source Mapping (dependency-confusion defense) | both | The <packageSourceMapping> nuget.config element (NuGet 6.0+) restricts, per package-ID-prefix pattern, which configured source(s) NuGet is even allowed to search for a given package — including transitive packages, which must also match a pattern once mapping is enabled for a repo. |
| Package Manager UI: dedicated mapping page + auto-mapping on install | vs | VS 17.5+ adds a Package Source Mappings page under Tools > NuGet Package Manager > Package Manager Settings (and a 'Configure' shortcut from the package details pane, 17.7+ shows live mapping status). VS 17.8+ auto-creates a mapping for a package (and its dependencies) on install/update when a single source is selected and mapping is already enabled, with a preview-and-apply step. |
| Global Packages Folder caveat undermines mapping guarantees | both | If a requested package version is already present in the local Global Packages Folder (from any prior source), NuGet skips source lookup entirely and reuses the cached copy — meaning Package Source Mapping's per-source guarantee only holds if the repo declares its own dedicated global packages folder. |
| Signed packages: author signing + repository signing | both | NuGet 4.6+/VS 2017 15.6+ supports author signatures (X.509, 2048-bit+ RSA, must chain to a Windows-trusted root, self-issued certs rejected on nuget.org) that prove a package hasn't changed since the author signed it, and repository signatures (all nuget.org uploads are auto repository-signed) that vouch for integrity regardless of where a package was subsequently obtained from. |
| Enforced signature validation (signatureValidationMode=require) | both | Setting <config><add key="signatureValidationMode" value="require" /></config> in nuget.config makes restore fail for any package that isn't signed by a certificate present in the config's <trustedSigners> section, rather than merely accepting signed-or-unsigned packages by default. |
| Deprecated-package reporting | vs | Since VS 2019 16.3, the NuGet Package Manager's Installed tab shows a warning icon plus the author-supplied deprecation reason (Legacy / has critical bugs / Other) and recommended alternate package for any deprecated dependency; `dotnet list package --deprecated` (since .NET SDK 3.0) surfaces the same data on the CLI. Rider's documentation does not describe an equivalent dedicated UI indicator… |

---

## Scale, performance and responsiveness on large solutions

| Feature | IDE | What it is |
| --- | --- | --- |
| Out-of-process backend over the Rd protocol | rider | Rider's UI (an IntelliJ-Platform frontend written in Kotlin/Java) runs as a separate OS process from the .NET/ReSharper backend that does all parsing, indexing, code analysis and refactoring; the two talk over JetBrains' 'Rd' (Reactive Distributed) protocol, each with its own memory space and GC. |
| Roslyn analysis in an out-of-process ServiceHub host | vs | C#/VB compiler diagnostics, analyzers, and CodeLens's per-method 'Find All References' run inside a separate ServiceHub.RoslynCodeAnalysisService(64).exe process rather than inside devenv.exe. |
| ReSharper Out-of-Process mode | ReSharper (separate paid VS add-in, not bundled VS) | JetBrains' ReSharper — a commercial VS extension distinct from VS/Roslyn's own tooling — now defaults to running its analysis engine in its own process outside devenv.exe, mirroring Rider's architecture. |
| Remote Development (thin client, backend on a remote host) | rider | A lightweight JetBrains Client runs locally while the full backend — indexing, solution-wide analysis, compilation — runs on a remote machine (build server, beefy workstation, cloud VM); only UI state is synced over the wire. |
| 64-bit devenv.exe | vs | Visual Studio 2022's main process, devenv.exe, is a native 64-bit application (VS 2019 and earlier were 32-bit, effectively capped near 4 GB of address space). |
| Solution Filters (.slnf) | vs | File > Save Solution As Filter writes a JSON .slnf file that references the parent .sln but records a subset of projects to load; opening the .slnf loads only that subset (shown with a funnel glyph and project count in Solution Explorer) and can be checked into source control or shared with teammates. |
| Open with no projects loaded, and remembered load set | vs | The Open Project/Solution dialog's 'Do not load projects' option (or the /donotloadprojects command-line switch) opens a .sln with zero projects loaded; you then selectively 'Reload Project' on what you need, and VS remembers that exact load set the next time you open the same solution locally. |
| Load Direct Dependencies / Load Entire Dependency Tree | vs | In a partially-loaded solution, right-clicking loaded project(s) in Solution Explorer offers 'Load Direct Dependencies' or 'Load Entire Dependency Tree' to pull in exactly the projects they reference. |
| Show/Hide Unloaded Projects toggle | vs | Right-clicking the solution node offers 'Show Unloaded Projects'/'Hide Unloaded Projects', and the 'Show All Files' Solution Explorer button also toggles visibility of unloaded projects. |
| 'Reopen documents on solution load' toggle (disable automatic file restore) | vs | Tools > Options > Projects and Solutions > General > 'Reopen documents on solution load' controls whether VS reopens every document left open in the previous session; VS proactively shows a yellow info bar telling you when this restore is slowing the current load. |
| Cached solution load, 8x faster reopen (v17.6) | vs | A redesigned diagnostics/caching pipeline for solution open shipped in VS 2022 17.6; reopening the 11,000-project Chromium repository from cache dropped to roughly 30 seconds. |
| Faster solution close for large C++ solutions (v17.6) | vs | Solution close time for solutions with 1,000+ C++ projects improved 20-50% in VS 2022 17.6; the Chromium repo closed about 50% faster, saving roughly 20 seconds. |
| Background analysis scope: Current Document / Open Documents / Entire Solution / None | vs | Tools > Options > Text Editor > C# (or Visual Basic) > Advanced > 'Run background code analysis for:' selects how widely live analyzers run; default in VS 2022 is 'Current Document' for analyzers, while compiler diagnostics still run on all open documents. |
| Automatic feature suspension under memory pressure | vs | If VS detects roughly 200 MB or less of available system memory, it automatically drops the live-analysis scope to 'Current Document' and shows an alert explaining that features were disabled to keep the IDE responsive. |
| Solution-wide analysis with Pause/Continue | rider | Editor \| Inspection Settings enables solution-wide analysis, shown via a status-bar circle indicator (green/orange/red); clicking it (or the Errors in Solution toolbar) offers Pause Analysis / Continue Analysis so you stop the CPU-heavy full-solution scan temporarily rather than fully disabling it. |
| Per-folder 'Stop indexing' | rider | Right-clicking a folder in Solution Explorer (e.g. generated code, vendored dependencies, bin/obj) offers Tools > Stop indexing to exclude that subtree from indexing and solution-wide analysis entirely. |
| Errors in Solution tool window with framework/severity filters | rider | View \| Tool Windows \| Errors in Solution lists solution-wide analysis results filterable by project, target framework and severity, with its own Pause/Continue toolbar buttons independent of inline editor squiggles. |
| On-demand, incremental C# indexing (v17.11) | vs | VS 2022 17.11 changed C# code indexing from aggressive/preemptive to on-demand execution, and made the indexers themselves more incremental so they touch less data per update. |
| Source generators moved to a separate, on-demand host process (v17.11) | vs | Source-generator execution moved from the main VS/Roslyn workspace into a separate out-of-process .NET 8 host, and generators now run only on save/build instead of on every keystroke. |
| Immediate Delay Tracker: continuous keystroke-latency telemetry | vs | Microsoft built an internal, fine-grained measurement system (Immediate Delay Tracker, successor to the earlier PerfWatson) that watches how long every keystroke and other high-frequency editor action takes, bucketing and reporting the data centrally to drive an ongoing 'typing responsiveness' engineering effort. |
| Cancellable, status-bar-hosted background operations | vs | VS's status bar exposes a dedicated animation/progress region for long-running background work, and operations built on it (e.g. IVsFindCancelDialog for a solution-wide Find/Replace) can present a cancel affordance instead of blocking until completion. |
| Transient cache folder + 'Invalidate Caches' | rider | Rider persists indexing and solution-wide-analysis state to %LOCALAPPDATA%\JetBrains\Transient so reopening a previously-opened solution doesn't start from zero; File \| Invalidate Caches forces a clean rebuild of that state when it drifts from source or gets corrupted. |
| .vs folder: cached design-time builds + IntelliSense database + session state | vs | The hidden .vs folder in the solution root caches design-time build (DTB) output to disk — the project system skips re-running a DTB on project load if the cache is up to date — plus the IntelliSense/symbol database and .suo session state (open tabs, breakpoints, window layout). |
| ComponentModelCache (MEF composition cache) | vs | %localappdata%\Microsoft\VisualStudio\<version>\ComponentModelCache stores the MEF composition graph VS uses to wire up editor and language services, so it doesn't need to be recomputed from every installed extension/assembly on each launch. |
| Solution-open cache tuning: ~50% smaller cache, ~10% faster open (v17.10) | vs | VS 2022 17.10 retuned the on-disk caches used during solution open, cutting cache size by nearly half and reducing measured solution-open time by about 10% on the OrchardCore solution. |
| Build Acceleration for SDK-style .NET projects | vs | An opt-in feature that skips unnecessary MSBuild work on incremental builds of SDK-style .NET projects by determining more precisely what actually needs to rebuild. |
| Incremental source generators (IIncrementalGenerator) | both (Roslyn compiler feature, applies under any IDE that compiles the code) | The modern Roslyn generator API lets a source generator declare a pipeline of cacheable steps, so a small edit re-runs only the pipeline stages affected by that edit instead of the whole generator re-executing over the entire compilation. |
| Configurable, self-growing backend memory heap | rider | Help \| Change Memory Settings lets you raise the backend's heap limit directly; Rider can also detect it's about to run out of memory, add roughly 1000 MB, and restart the language service in the background rather than crash outright. |
| Status-bar memory indicator | rider | An optional status-bar widget shows live backend heap usage in real time so you can watch consumption trend and decide when to raise the memory limit manually. |
| CodeLens on/off toggle | vs | Tools > Options > Text Editor > CodeLens > 'Enable CodeLens' turns off the inline reference-count/history indicators, each of which triggers a Find All References computation (run in the ServiceHub.RoslynCodeAnalysisService process) per visible method. |
| Documented Roslyn performance registry keys | vs | Under HKCU\SOFTWARE\Microsoft\VisualStudio\<ver>\Roslyn, documented DWORD values let you tune large-solution behavior beyond the Options UI: raising 'RecoverableTreeLengthThreshold' (Internal\Performance\Cache, default 4096 bytes) changes when small file syntax trees become recoverable/paged-to-disk, and setting 'Solution Crawler' to 0 (Internal\OnOff\Components) fully disables background… |
| Roslyn/MSBuild-based project system with deferred, per-file-type language server loading | C# Dev Kit (VS Code extension, not Rider or Visual Studio proper) | C# Dev Kit layers a Visual-Studio-derived solution/project system (solution explorer, test/debug integration) on top of the open-source C# extension's Roslyn-based language server rather than replacing it, supports only modern SDK-style projects (not .NET Framework/legacy formats), and defers loading auxiliary language servers — e.g. Razor — until you open the first relevant file type. |

---

## The AI layer, framed as a competitive axis

| Feature | IDE | What it is |
| --- | --- | --- |
| Copilot Chat slash commands (/explain, /fix, /tests, /doc) | vs | In GitHub Copilot Chat (VS 2022 17.8+), typing a slash command in the chat box or inline assistant triggers a targeted action: /explain explains selected code, /fix proposes a correction for the error/warning under the cursor, /tests generates unit tests for the selection, /doc inserts an XML doc comment at the cursor. |
| Ask Copilot inline code assistant | vs | An inline chat popover invoked directly in the editor (vs. the docked Copilot Chat window) that lets you ask a question or issue an edit command right where the cursor is, with the diff shown inline for accept/reject. |
| Copilot agent mode | vs | A chat mode (selected from the mode dropdown) where you give a high-level natural-language task and Copilot autonomously plans steps, edits files, runs terminal commands, invokes tools, and iterates against build/test results until the goal is met or it needs input. |
| Copilot Edits / multi-file summary diff view | vs | Copilot Edits (VS 2022 17.13+) lets you iterate across several files at once from chat; VS 2026 18.6 added a multi-file summary tab listing every changed file with granular per-chunk accept/undo controls. |
| Plan agent (Copilot planning, preview) | vs | A dedicated planning mode where Copilot explores the codebase read-only, asks clarifying questions, and drafts an implementation plan saved as a markdown file before any code is written. |
| Copilot Completions (ghost-text, whole-line/whole-function) | vs | Inline gray-text suggestions ranging from a single completion to a whole function, generated from surrounding code context or a natural-language comment; accepted with Tab. |
| IntelliCode whole-line completions & context-aware IntelliSense ranking | vs | A separate, free, locally-run transformer model (trained on ~500k public repos) that reranks IntelliSense with starred suggestions and offers whole-line gray-text completions for C#, C++, XAML, JS/TS, and VB, working fully offline. |
| IntelliCode API usage examples | vs | Shows inferred, real-world example snippets of how a given API/type is commonly used, sourced from IntelliCode's model rather than a live LLM call. |
| AI-generated Git commit messages | both | VS: a sparkle-pen icon in the Commit tool window (17.9+, requires Copilot + Copilot Chat) drafts a commit message from the staged diff, customizable via custom prompt instructions. Rider: the built-in AI Assistant icon in the Commit tool window does the same, driven by whatever model is set as the core profile under Settings \| Tools \| AI Assistant \| Providers. |
| Fix with Copilot (lightbulb quick action) | vs | From the Quick Actions light bulb on a diagnostic squiggle (or double-clicking an Error List entry), choosing "Fix with Copilot" opens inline chat with a proposed fix for cases the built-in Roslyn code-fix providers can't handle. |
| AI-powered rename suggestions | vs | A sparkle icon next to the Rename dialog calls Copilot to propose identifier names based on usage and codebase naming conventions; shipped first for C++ (17.10 Preview 1) under Options > GitHub > Copilot Chat > Enable rename suggestions. |
| Copilot Next Edit Suggestions (NES) | vs | Detects a pattern of repeated manual edits and proactively suggests the next, related edit elsewhere in the file/codebase, distinct from ghost-text completion. |
| Debug with Copilot / Ask Copilot in Exception Helper | vs | When execution is paused on an exception, an "Ask Copilot" button opens Copilot Chat pre-loaded with the call stack, frame variables, and values; Copilot can also proactively analyze an unhandled exception against repo history (past bugs/PR fixes) to point at the likely faulty code path. |
| Debugger Agent (autonomous bug-fix loop) | vs | An agentic debugging workflow (VS 2026) that reproduces a failing test/bug against real runtime behavior, instruments the app, isolates a root cause, applies a fix, reruns the test, and iterates — validating against live execution rather than static analysis alone. |
| Profile with Copilot Agent | vs | An AI-assisted profiling workflow that helps interpret performance-profiler output and suggests optimizations for the .NET code involved. |
| GitHub Copilot Testing for .NET (Test Explorer agent) | vs | A purpose-built test agent (VS 18.3+, invoked with @Test in chat or Copilot Actions > Generate Tests) that creates a test project if needed, generates tests grounded in the C# compiler/language semantics for MSTest, xUnit, or NUnit, then builds, runs, and iterates on failures automatically, integrated with Test Explorer. |
| Copilot code review | both | Stage changes and click the Copilot code review button (VS 17.14+) to get logic/security/performance feedback with one-click-appliable suggestions before opening a PR. As of September 2025 this same GitHub Copilot code-review feature also ships as a plugin surface inside JetBrains IDEs including Rider. |
| Semantic/workspace code search indexing | vs | Copilot builds a vector-embedding index of the workspace (remote index for GitHub/Azure DevOps-hosted repos, local index otherwise) so chat and agent requests can retrieve semantically relevant files by meaning, not just keyword match; indexing status is visible in the Copilot status dashboard. |
| Did You Mean Copilot search suggestions | vs | In All-In-One Search, Copilot infers intent behind a mistyped or approximate file-search query and suggests the likely intended file. |
| MCP servers as a Copilot extension mechanism (client role) | vs | Visual Studio (17.14+) acts as an MCP client: servers are added via a one-click web install, the chat tool-picker's "+" button, the Extensions > MCP Registries manager, or a hand-edited .mcp.json (auto-discovered from user profile, solution .vs/.mcp.json/.vscode/.cursor locations). VS 2026 18.7 added a trust dialog re-prompting when a trusted server's config or capabilities change, plus… |
| VS Code Chat Participant API & Language Model Tool API | vs | In VS Code (not Visual Studio proper), extension authors can register a @-mentionable chat participant that owns a slice of natural-language conversation, and/or contribute LanguageModelTool implementations that the Copilot agent orchestrator can invoke directly against editor/workspace/debug APIs — a first-party alternative to MCP for extensions that need deep VS Code integration. |
| AI Assistant Chat with contextual references | rider | Rider's built-in AI Assistant chat panel lets you attach files, folders, symbols, or commits as context and pick among cloud models (Claude, Gemini, GPT, etc.) or local models for the conversation. |
| Junie autonomous coding agent | rider | A full agent (bundled in Rider) that plans a multi-step task, edits files across the solution, runs commands/tests, and iterates on the result with reduced turn-by-turn supervision; also ships as a standalone CLI and for CI/CD (GitHub Actions, GitLab). As of January 2026, JetBrains added OpenAI's Codex as a second selectable agent alongside Junie in the same panel. |
| Multi-file Edit mode in AI chat | rider | A chat mode (introduced 2025.1, since matured) that proposes edits spanning multiple files at once with per-file reviewable diffs before anything is applied. |
| Local/offline LLM support (Ollama, LM Studio) | rider | AI Assistant can be pointed at a locally-hosted model server (Ollama or LM Studio) for chat, so prompts and code never leave the machine; usable even on the free AI Free license tier. |
| Full Line Code Completion (Mellum-powered) | rider | A bundled-by-default (since Rider 2024.2), fully local completion engine covering C#, CSS-like languages, and JS/TS, running entirely on-device with no network call, currently powered by JetBrains' own Mellum model. |
| Mellum (JetBrains' own completion model) | rider | A 4B-parameter code-focused LLM JetBrains trained in-house (4T+ tokens on 256 H200 GPUs) and open-sourced on Hugging Face; it's the model behind Full Line Code Completion and available as a selectable completion backend. |
| Generate Unit Tests with AI | rider | Alt+Enter (or Ctrl+T,C / Generate menu) on a class/member offers "Create Unit Tests"; checking "Generate test content with AI" fills in actual test bodies (valid/invalid-input cases) rather than empty skeletons. |
| Explain with AI for build errors and exceptions | rider | Introduced in Rider 2026.2 EAP 5: an AI-explanation action surfaced directly at the point Rider reports a build error or runtime exception, instead of requiring you to copy the diagnostic into chat manually. |
| Built-in MCP server (Rider as an MCP host, not just client) | rider | Since Rider 2025.2, Rider bundles and enables by default an MCP Server plugin that runs inside the ReSharper/Rider backend and exposes Rider's own code-intelligence tools over MCP (stdio transport) so external MCP clients — Claude Desktop, Claude Code, Cursor, VS Code, Codex, Windsurf — can drive Rider from outside the IDE; configured under Settings \| Tools \| MCP Server. |
| GitHub Copilot as a natively integrated agent option in Rider | rider | Rider 2026.2 integrates GitHub Copilot as a first-class, OAuth-sign-in agent selectable from the same in-IDE agent picker as Junie and other providers, alongside official Microsoft .NET/Aspire/Azure "skills" that are directly discoverable in the IDE. |
| Agent Skills Manager & built-in skills | rider | Settings \| Tools \| AI Assistant \| Skills exposes a library of discrete, invokable skills (e.g., dottrace-analyze for profiling analysis, finding-tests for test-placement guidance) that any connected agent can call. |
| AI Agent Hooks / code quality check hooks | rider | Tools \| AI Agent Hooks lets Rider validate every change an autonomous agent makes — catching inspection findings, code issues, and formatting inconsistencies — before the agent is allowed to continue to its next step. |
| AI Assistant / Copilot pricing model contrast | both | JetBrains AI ships as AI Free ($0, 3 credits/30 days, full local-model access), AI Pro ($10/mo, 10 credits), AI Ultimate ($30/mo, 35 credits), and AI Enterprise; completions and Next Edit Suggestions are free and don't consume credits. GitHub Copilot (since its June 2026 usage-based switch) ships as Free, Pro ($10/mo), Pro+ ($39/mo), Max ($100/mo), Business ($19/user/mo), and Enterprise… |
